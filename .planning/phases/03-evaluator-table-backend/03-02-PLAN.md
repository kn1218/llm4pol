---
phase: 03-evaluator-table-backend
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/llm4pol/evaluate/cache.py
  - src/llm4pol/evaluate/budget.py
  - src/llm4pol/evaluate/evaluator.py
  - src/llm4pol/evaluate/__init__.py
  - src/llm4pol/evaluate/__main__.py
  - tests/test_evaluate_cache_budget.py
  - tests/test_evaluate_table.py
  - tests/test_evaluate_cli.py
  - docs/governance/INVARIANTS.md
autonomous: true
requirements: [EVAL-02, EVAL-04]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A second identical request answered by the same `Evaluator` returns every result with `cached: true` and `cost.evals` 0, the response `cost.evals` is 0 and `Evaluator.meter` is unchanged — both with the in-memory `JsonlCache()` and with a fresh `JsonlCache(path)` that replayed the JSONL written by the first request (D-04, R-4, EVAL-04, charter section 13 M2 (2))."
    - "The cache key is `(candidate_id, property, backend.name, backend.source)`: after a request the key with the live `source` hits and the same key with any other `source` misses; `error` results are never stored; `unsupported` never reaches the cache; stored records keep `cached: false` and the hit is returned through `dataclasses.replace(hit, cached=True, cost=Cost(0, 0.0))` (D-04, R-4, Pattern 5)."
    - "`evals` is charged once per distinct candidate per request on its first non-cached `ok` result: three properties of one candidate charge 1, an all-`missing` candidate charges 0, and a new property of an already-cached candidate in a later request charges 1 again (D-04, R-2, Pitfall 5)."
    - "`BudgetMeter(evals, cpu_hours)` is frozen, `charge` returns a new meter, and no attribute, method or schema field combines the two currencies; `remaining(limit)` and `exhausted(limit)` read `evals` only; the A-3 row of `docs/governance/INVARIANTS.md` names the meter, the schema `cost` definition and the test (A-3, D-04, D-08)."
    - "When `evals_limit` is set and the request's distinct uncached `ok` candidates would exceed `meter.remaining(limit)`, `evaluate` raises `BudgetExceeded(requested, remaining, limit)` and neither the cache (in memory or on disk) nor the meter changes (D-04, Pattern 4)."
    - "A `TableBackend` pointed at an absent parquet answers `error` with `reason` `FileNotFoundError`, `cost.evals` 0, nothing cached; after the parquet appears the same evaluator answers `ok` on the next call; a parquet whose `llm4pol.snapshot` metadata differs answers `error` with `reason` `SnapshotMismatch` (D-02, R-4, EVAL-02, charter section 13 M2 (1))."
    - "`python -m llm4pol.evaluate --request R --root T [--cache C] [--evals-limit N]` exits 0 on success, 2 on an unreadable or invalid request or an unreadable cache file, 3 on `BudgetExceeded` with `BUDGET:` on stderr; the JSONL at `C` is LF-only, one canonical JSON object per line, appended once per request with `flush()` and no per-line fsync, and never contains `NaN` (D-07, R-4, F-33..F-36)."
  artifacts:
    - path: "src/llm4pol/evaluate/cache.py"
      provides: "CacheKey, CacheError, canonical_line, JsonlCache(path=None) with get/put_many/__len__/path, replay on open"
      contains: "class JsonlCache"
    - path: "src/llm4pol/evaluate/budget.py"
      provides: "BudgetMeter (frozen: evals, cpu_hours; charge/remaining/exhausted), BudgetExceeded(requested, remaining, limit)"
      contains: "class BudgetMeter"
    - path: "src/llm4pol/evaluate/evaluator.py"
      provides: "Evaluator(backend, properties, *, cache=None, meter=BudgetMeter(), evals_limit=None) with the two-phase evaluate: compute all results (cache hits first), check the budget, then commit cache + meter"
      contains: "BudgetExceeded"
    - path: "src/llm4pol/evaluate/__main__.py"
      provides: "--cache, --evals-limit; exit 3 on BudgetExceeded; exit 2 on CacheError/OSError"
      contains: "--evals-limit"
    - path: "tests/test_evaluate_cache_budget.py"
      provides: "cache hit (memory + replay), key includes backend and source, per-candidate charging incl. Pitfall 5, A-3 two currencies, BudgetExceeded leaves state untouched, LF/canonical/NaN-free lines, malformed line -> CacheError, stored records keep cached false"
      contains: "test_a3_budget_meter_keeps_evals_and_cpu_hours_as_separate_currencies"
    - path: "docs/governance/INVARIANTS.md"
      provides: "A-3 row in the Architecture (charter section 4) table naming BudgetMeter, the schema cost definition and the test"
      contains: "test_a3_budget_meter_keeps_evals_and_cpu_hours_as_separate_currencies"
  key_links:
    - from: "src/llm4pol/evaluate/evaluator.py"
      to: "src/llm4pol/evaluate/cache.py"
      via: "cache.get(key) before backend.lookup; cache.put_many(new ok/missing results) only after the budget check passes"
      pattern: "put_many"
    - from: "src/llm4pol/evaluate/evaluator.py"
      to: "src/llm4pol/evaluate/budget.py"
      via: "total evals compared with meter.remaining(evals_limit) before commit; meter replaced by meter.charge(total) on commit"
      pattern: "remaining("
    - from: "src/llm4pol/evaluate/cache.py"
      to: "src/llm4pol/evaluate/contract.py"
      via: "EvalResult.to_json / from_json define the JSONL record; from_json re-validates replayed records"
      pattern: "from_json"
    - from: "docs/governance/INVARIANTS.md"
      to: "tests/test_evaluate_cache_budget.py"
      via: "the A-3 guard cell names the test the gate runs"
      pattern: "A-3"
  prohibitions:
    - statement: "No `error` result is ever written to the cache and no `unsupported` key ever reaches it; a cached `error` would make the status non-retryable (D-02, Pattern 5)."
      status: unverified
      verification: null
    - statement: "`budget.py` defines no attribute or method that combines `evals` and `cpu_hours` into one number, and `Cost` / `BudgetMeter` carry exactly those two fields (A-3)."
      status: unverified
      verification: null
    - statement: "The JSONL writer never calls a per-line fsync; the cache is a derived artifact and the Phase 4 ledger is the durable record (R-4, F-36)."
      status: unverified
      verification: null
    - statement: "No task writes `cached: true` into a stored record; `cached` is a property of the response set on return (R-4)."
      status: unverified
      verification: null
---

<objective>
Expand the tracer of 03-01 with the two stateful pieces the charter puts around the evaluator:
a cache keyed by `(candidate_id, property, backend, source)` whose hit leaves `evals` unchanged
(in memory, and replayed from an append-only JSONL when a path is given), and a two-currency
`BudgetMeter` with a structured `BudgetExceeded` refusal that leaves nothing committed (Task 1).
Then pin the fourth status: `error` from an absent or mismatched parquet, retryable and never
cached, and expose `--cache` / `--evals-limit` with exit code 3 on the CLI (Task 2). A-3 is
promoted to a guard in the same change as the meter (D-08, promotion policy).

Purpose: charter section 13 M2 requires "cache hit -> `evals` unchanged" and each status tested,
`error` retried; charter section 4 A-3 requires the two currencies to be counted separately; the
loop's 400-eval budget (Phase 5) counts candidates, which is what per-candidate charging pins.

Output: `cache.py`, `budget.py`, the extended `Evaluator`, the extended CLI, the new
`tests/test_evaluate_cache_budget.py`, three `error` tests in `tests/test_evaluate_table.py`, three
CLI tests in `tests/test_evaluate_cli.py`, and the A-3 row in INVARIANTS.md.

Grounding facts (03-RESEARCH.md; cite, never re-derive): F-33 `path.open("a", encoding="utf-8",
newline="\n")` + `json.dumps(record, sort_keys=True, separators=(",", ":"), ensure_ascii=False,
allow_nan=False) + "\n"` writes LF-only lines on Windows and replays byte-equal; F-34 `allow_nan=False`
raises `ValueError` on NaN; F-35 `sort_keys=True` makes the bytes key-order independent; F-36 a
per-append fsync costs 2.1 ms (so persist per request, flush only); F-37 frozen dataclass mutation
raises `FrozenInstanceError`; F-40 synthetic cases (`7ec8cb49ff317efc` three `ok` properties;
`81b997b85ccd2069` TC `missing`; `d24805b4ce4c381c` TC `ok` n 1 / `tg` `missing`; `0123456789abcdef`
unknown); F-43 the calfloop precedent has no two-currency accounting — the test here is the guard;
F-48 no size bound is needed at M2; Pattern 4 (two-phase evaluate), Pattern 5 (cache policy),
Pattern 6 (meter), Code Example 4 (JSONL line and replay), Code Example 5 (exit codes), Pitfalls
2, 3, 4, 5. Workstation guard (Phase 1): a hook refuses Bash commands containing git's `-n` short
flag — use `--max-count`; write commit messages to a scratchpad file and commit with `-F`. Ad-hoc
commands need `PYTHONPATH=src`.
</objective>

<execution_context>
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-evaluator-table-backend/03-CONTEXT.md
@.planning/phases/03-evaluator-table-backend/03-RESEARCH.md
@.planning/phases/03-evaluator-table-backend/03-01-SUMMARY.md
@CLAUDE.md
@docs/MASTER-PLAN.md
@docs/governance/INVARIANTS.md
@src/llm4pol/evaluate/contract.py
@src/llm4pol/evaluate/evaluator.py
@src/llm4pol/evaluate/backends/table.py
@src/llm4pol/evaluate/__main__.py
@protocol/schemas/eval-response.json
@tests/conftest.py
@tests/test_evaluate_table.py
@tests/test_evaluate_cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JsonlCache and BudgetMeter, the two-phase evaluate, A-3 promoted (D-04, R-2, R-4, D-08, EVAL-04, A-3, charter section 13 M2 (2))</name>
  <read_first>
    - `.planning/phases/03-evaluator-table-backend/03-CONTEXT.md` — D-04 (key, JSONL, per-candidate charging, `BudgetMeter`, `BudgetExceeded`), R-2, R-4, Claude's Discretion (persistence off unless `cache_path`)
    - `.planning/phases/03-evaluator-table-backend/03-RESEARCH.md` — Pattern 4, Pattern 5, Pattern 6, Code Example 4, Pitfalls 3, 4, 5, Anti-Patterns (never cache `error`; never write `cached: true`; response cost is the sum), F-33..F-38, F-43, F-48, Security Domain (cache poisoning row)
    - `src/llm4pol/evaluate/evaluator.py` and `contract.py` as left by 03-01 (`Evaluator.evaluate`, the `charged` set, `EvalResult.from_json` type checks, `Cost`)
    - `docs/governance/INVARIANTS.md` — the `## Architecture (charter section 4) — guards landed` table (rows are added in ID order before A-7; the charter text is not edited) and the `## Promotion policy`
    - `docs/MASTER-PLAN.md` section 4 row A-3 (guard "ledger schema (M3)" — this plan lands the M2 guard; the ledger guard is Phase 4's) and section 13 M2
  </read_first>
  <behavior>
    - Test 1 `tests/test_evaluate_cache_budget.py::test_cache_hit_leaves_evals_unchanged_in_memory`: one `Evaluator` (default in-memory cache) answers the same three-property request for `7ec8cb49ff317efc` twice; first response `cost.evals == 1`, second response has every `cached is True`, every `cost.evals == 0`, `cost == Cost(0, 0.0)`; `evaluator.meter.evals == 1` after both calls; the second response validates against the schema (F-28: `cached == true` forces `evals` 0).
    - Test 2 `test_cache_hit_leaves_evals_unchanged_after_jsonl_replay`: an evaluator with `JsonlCache(tmp_path / "cache.jsonl")` answers the request once; a *new* `Evaluator` with a fresh `JsonlCache(same path)` and a fresh meter answers it again -> all `cached`, response `evals` 0, the new meter stays at `BudgetMeter()`; `len(cache) == 3` before and after.
    - Test 3 `test_cache_key_includes_backend_and_source`: after one request, `cache.get(("7ec8cb49ff317efc", "tg", "table", PropertyTable.load().snapshot))` is an `EvalResult` with `cached is False`, while the same tuple with source `"other"` or backend `"radonpy"` is None.
    - Test 4 `test_evals_charged_once_per_distinct_candidate_per_request`: three properties of `7ec8cb49ff317efc` -> per-result `evals` `[1, 0, 0]`; the same candidate listed twice in one batch -> the second entry's results all 0; two distinct `ok` candidates -> response `evals` 2.
    - Test 5 `test_all_missing_candidate_charges_zero`: `81b997b85ccd2069` / `["thermal_conductivity"]` -> response `evals` 0 and the `missing` result is stored in the cache (`len(cache) == 1`).
    - Test 6 `test_new_property_of_a_cached_candidate_charges_one_eval` (Pitfall 5): request A `7ec8cb49ff317efc` / `["thermal_conductivity"]` -> `evals` 1; request B same candidate / `["thermal_conductivity", "tg"]` -> first result `cached`, `evals` 0; second result not cached, `evals` 1; response `evals` 1; meter `evals` 2.
    - Test 7 `test_a3_budget_meter_keeps_evals_and_cpu_hours_as_separate_currencies`: `BudgetMeter().charge(Cost(3, 0.0)).charge(Cost(0, 2.5)) == BudgetMeter(evals=3, cpu_hours=2.5)`; `remaining(10) == 7`, `remaining(2) == 0`, `exhausted(3) is True`, `exhausted(4) is False`; `[f.name for f in dataclasses.fields(BudgetMeter)] == ["evals", "cpu_hours"]` and the same for `Cost`; `not hasattr(BudgetMeter, "total")`; the source text of `llm4pol.evaluate.budget` (via `inspect.getsource`) contains neither `__float__` nor `__add__` nor `total`; the response schema's `$defs/cost` has `required == ["evals", "cpu_hours"]` and `additionalProperties is False`; assigning `meter.evals = 1` raises `FrozenInstanceError` (F-37).
    - Test 8 `test_budget_exceeded_leaves_cache_and_meter_untouched`: `Evaluator(..., cache=JsonlCache(tmp path), evals_limit=1)` on a request with two distinct `ok` candidates raises `BudgetExceeded` with `requested == 2`, `remaining == 1`, `limit == 1` and a message naming all three; afterwards `len(cache) == 0`, the JSONL path does not exist, `evaluator.meter == BudgetMeter()`; with `evals_limit=2` the same request succeeds and `meter.evals == 2`; a later request when `meter.exhausted(2)` and one more `ok` candidate raises again.
    - Test 9 `test_jsonl_lines_are_lf_only_canonical_and_nan_free`: after one request whose results include `d24805b4ce4c381c` / `thermal_conductivity` (n 1, `spread` None), the file bytes contain no `\r`, end with `\n`, have one line per stored result, each line equals `canonical_line(key, result)` and parses to `{"key": [...4 strings...], "result": {...}}`, and no line contains `NaN`; the stored `spread` is `null`.
    - Test 10 `test_malformed_cache_line_raises_cache_error_with_line_number`: append the text `{not json` as a third line to a two-line cache file; `JsonlCache(path)` raises `CacheError` whose message contains `:3:` and the path; a record whose `result.cached` is the string `"yes"` also raises `CacheError` (replayed records are re-validated through `EvalResult.from_json`).
    - Test 11 `test_stored_records_keep_cached_false`: after a hit, every `result.cached` in the file is `False` and the returned hit is `cached True` with `cost == Cost(0, 0.0)`.
  </behavior>
  <files>src/llm4pol/evaluate/cache.py, src/llm4pol/evaluate/budget.py, src/llm4pol/evaluate/evaluator.py, src/llm4pol/evaluate/__init__.py, tests/test_evaluate_cache_budget.py, docs/governance/INVARIANTS.md</files>
  <action>
    STEP 1 — RED: write Tests 1–11 in a new `tests/test_evaluate_cache_budget.py` (reuse the `_evaluator` / `_request` helper pattern of `tests/test_evaluate_table.py`; accept `cache`, `meter`, `evals_limit` keyword arguments in the helper); run `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_cache_budget.py -x -q` -> RED (import errors on `cache` / `budget`); commit `test(03-02): add failing cache-hit, per-candidate charging, A-3 and BudgetExceeded tests (D-04, R-2, R-4, EVAL-04)`.

    STEP 2 — `budget.py` (Pattern 6): `BudgetMeter` frozen dataclass with `slots=True`, fields `evals: int = 0` and `cpu_hours: float = 0.0`, methods `charge(cost: Cost) -> BudgetMeter` (returns a new meter; the coding-style immutability rule), `remaining(evals_limit: int) -> int` (`max(evals_limit - self.evals, 0)`), `exhausted(evals_limit: int) -> bool`; `BudgetExceeded(RuntimeError)` with attributes `requested`, `remaining`, `limit` and a message of the form `requested N evals, M remaining of limit L`. The module docstring cites charter section 4 A-3 and says the two fields are never combined into one number by this package; the test greps the source for the combining names, so keep them out of comments too. `cpu_hours` is carried and summed but never limited at M2 (the `table` backend costs 0.0; M9 sets it).

    STEP 3 — `cache.py` (Pattern 5, Code Example 4): `CacheKey = tuple[str, str, str, str]`; `key_for(candidate_id, property_key, backend: Backend) -> CacheKey`; `CacheError(RuntimeError)`; `canonical_line(key, result) -> str` = `json.dumps({"key": list(key), "result": result.to_json()}, sort_keys=True, separators=(",", ":"), ensure_ascii=False, allow_nan=False) + "\n"`; `JsonlCache(path: Path | None = None)`: an internal dict; when `path` is given and exists, replay it on construction with `open("r", encoding="utf-8", newline="\n")`, skipping blank lines, raising `CacheError(f"{path}:{lineno}: {message}")` on a JSON decode error, a record without the `key`/`result` shape, a key that is not four strings, or an `EvalResult.from_json` `ValueError`; `get(key) -> EvalResult | None`; `put_many(items: Iterable[tuple[CacheKey, EvalResult]]) -> None` stores every item, then — only when a path is set and the list is non-empty — `mkdir(parents=True, exist_ok=True)` on the parent and one `open("a", encoding="utf-8", newline="\n")` block writing every line and calling `flush()` only — no per-line disk-sync call of any kind, and no comment naming one, because the verify block greps the module for it (R-4, F-36); `__len__`; `path` property. Nothing in this module decides charging or statuses.

    STEP 4 — `evaluator.py` (Pattern 4): extend the constructor to `Evaluator(backend, properties, *, cache: JsonlCache | None = None, meter: BudgetMeter | None = None, evals_limit: int | None = None)` — `cache` defaults to a new in-memory `JsonlCache()`, `meter` to `BudgetMeter()`; keep `backend`, `properties`, `cache`, `meter`, `evals_limit` as public attributes. `evaluate` becomes two-phase. Phase 1, per `(entry, key)` in request order: `key_for(...)`; a cache hit appends `dataclasses.replace(hit, cached=True, cost=Cost(0, 0.0))` and continues; otherwise `_lookup` as in 03-01, charging per the `charged` set, `EvalResult.from_lookup(...)`, and — when the lookup status is `ok` or `missing` — the pair `(cache key, result)` is queued for commit (never `error`, never `unsupported`). Phase 2: `total = Cost(sum evals, sum cpu_hours)`; if `evals_limit is not None and total.evals > self.meter.remaining(evals_limit)` raise `BudgetExceeded(requested=total.evals, remaining=..., limit=evals_limit)` before touching anything; then `self.cache.put_many(queued)` and `self.meter = self.meter.charge(total)`; return `EvalResponse(request.run_id, request.iteration, tuple(results), total)`. Write in the class docstring: a duplicate of a candidate inside one request is not a cache hit (the commit happens after the request) but is charged 0 by the `charged` set; a new property of an already-cached candidate in a later request is charged again (D-04 literal reading, Pitfall 5, Open Question 2). Export `BudgetMeter`, `BudgetExceeded`, `JsonlCache`, `CacheError` from `llm4pol/evaluate/__init__.py` (still no backend import).

    STEP 5 — INVARIANTS.md (D-08, promotion policy): insert an `A-3` row before the `A-7` row of the `## Architecture (charter section 4) — guards landed` table: invariant text "The budget keeps `evals` and `cpu_hours` as two separately counted currencies; no combined score exists"; guard cell naming `llm4pol.evaluate.budget.BudgetMeter` (two fields; `charge` returns a new meter), `protocol/schemas/eval-response.json` `$defs/cost` (exactly `evals` and `cpu_hours`, `additionalProperties: false`), `tests/test_evaluate_cache_budget.py::test_a3_budget_meter_keeps_evals_and_cpu_hours_as_separate_currencies`, and the note that the ledger-schema guard named by the charter lands at M3 (Phase 4). Do not edit the charter.

    STEP 6 — GREEN: the test file passes; `pixi run --manifest-path env/pixi.toml check` -> `SUMMARY: 7/7 steps passed`; commit `feat(03-02): JsonlCache keyed by (candidate_id, property, backend, source), BudgetMeter with two currencies, two-phase evaluate with BudgetExceeded; promote A-3 (D-04, R-2, R-4, D-08, EVAL-04, A-3)`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_cache_budget.py -v</automated>
    <fails_when>exit code is non-zero, or fewer than 11 tests are listed as PASSED, or `test_cache_hit_leaves_evals_unchanged_after_jsonl_replay`, `test_a3_budget_meter_keeps_evals_and_cpu_hours_as_separate_currencies` or `test_budget_exceeded_leaves_cache_and_meter_untouched` is absent</fails_when>
    <automated>grep -cE "__float__|__add__|total" src/llm4pol/evaluate/budget.py</automated>
    <fails_when>prints anything other than 0 — a combining name is in the budget module (A-3)</fails_when>
    <automated>grep -c "fsync" src/llm4pol/evaluate/cache.py</automated>
    <fails_when>prints anything other than 0 — a per-line fsync entered the writer (R-4, F-36)</fails_when>
    <automated>grep -c "allow_nan=False" src/llm4pol/evaluate/cache.py</automated>
    <fails_when>prints 0 — the NaN tripwire is missing from the JSONL writer (F-34)</fails_when>
    <automated>sed -n '/^| A-3 /p' docs/governance/INVARIANTS.md | grep -c "test_a3_budget_meter_keeps_evals_and_cpu_hours_as_separate_currencies"</automated>
    <fails_when>prints anything other than 1 — the A-3 row is missing or does not name the test</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits in order: `test(03-02): add failing cache-hit, per-candidate charging, A-3 and BudgetExceeded tests (D-04, R-2, R-4, EVAL-04)` then `feat(03-02): JsonlCache keyed by (candidate_id, property, backend, source), BudgetMeter with two currencies, two-phase evaluate with BudgetExceeded; promote A-3 (D-04, R-2, R-4, D-08, EVAL-04, A-3)`.
    - A repeated request returns every result `cached: true` with `evals` 0 both in memory and after replay from a fresh `JsonlCache(path)`; `Evaluator.meter.evals` equals the number of distinct charged candidates and does not grow on hits (charter section 13 M2 (2)).
    - `BudgetExceeded` carries `requested`, `remaining`, `limit`; after it the cache length is 0, the JSONL file does not exist and the meter equals `BudgetMeter()`.
    - The cache file is LF-only with one canonical line per stored `ok`/`missing` result and no `NaN`; a malformed third line raises `CacheError` naming `:3:` (F-33, F-34).
    - The three greps on `budget.py` / `cache.py` and the `sed` on INVARIANTS.md print the stated values; the gate prints `SUMMARY: 7/7 steps passed` with at least 146 tests collected (135 after 03-01 + 11).
  </acceptance_criteria>
  <done>Charter section 13 M2 (2) is reproducible by command: a cache hit on `(candidate_id, property, backend, source)` leaves `evals` unchanged in memory and across a JSONL replay; A-3 is a test the gate runs and INVARIANTS.md names it; a refused request commits nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The `error` status — absent or mismatched parquet, retryable, never cached — and the CLI's --cache / --evals-limit with exit 3 (D-02, D-07, R-4, EVAL-02, charter section 13 M2 (1))</name>
  <read_first>
    - `.planning/phases/03-evaluator-table-backend/03-CONTEXT.md` — D-02 (`error`: backend raised, retryable, `reason` = exception class name, no budget), D-07 (exit 0 / 2 / 3), D-08 (`error` path by pointing the backend at a missing parquet)
    - `.planning/phases/03-evaluator-table-backend/03-RESEARCH.md` — Pattern 1 (exception -> `Lookup("error", reason=type(exc).__name__)`), Pattern 3 (lazy load; snapshot assertion), Code Example 5 (exit codes; a backend read failure is exit 0 with per-result `error`), Security Domain (snapshot substitution row), F-03, F-39, F-45
    - `src/llm4pol/evaluate/backends/table.py` (`_frame` retries after a failed load), `evaluator.py` (Task 1), `__main__.py` (03-01), `tests/test_evaluate_cli.py` (03-01 helpers), `tests/test_data_cli.py` (in-process `main` + `capsys` pattern)
  </read_first>
  <behavior>
    - Test 1 `tests/test_evaluate_table.py::test_error_when_the_parquet_is_absent_consumes_no_evals_and_is_not_cached`: `TableBackend(tmp_path / "absent.parquet", properties=...)` inside an `Evaluator` with a JSONL cache; the example request yields `status == "error"` and `reason == "FileNotFoundError"` on every result except the `unsupported` one (decided before the backend), `value`/`unit`/`n_replicates`/`spread` None, `cost.evals` 0, `cached False`; response `cost == Cost(0, 0.0)`; `len(cache) == 0`, no JSONL file; `evaluator.meter == BudgetMeter()`; the response validates against the schema (`error` requires `reason`, F-26).
    - Test 2 `test_error_is_retried_once_the_parquet_appears`: after Test 1's call, copy the synthetic candidate parquet to the absent path and call the same evaluator again -> the statuses of the tracer expectation (`ok, ok, ok, missing, missing, ok, missing, unsupported, ok`), `cost.evals` 3, `len(cache) == 8` (eight `ok`/`missing` results stored; `unsupported` never cached).
    - Test 3 `test_error_on_snapshot_mismatch_names_the_exception_class`: write the synthetic table to a new path with `replace_schema_metadata({b"llm4pol.snapshot": b"polyomics:general_polymers@deadbeef"})`; every backend-reaching result is `error` with `reason == "SnapshotMismatch"`, evals 0, nothing cached.
    - Test 4 `tests/test_evaluate_cli.py::test_cli_exit_2_on_missing_request_bad_json_and_schema_failure` (parametrised): an absent `--request` path, a file containing `{not json`, and a schema-invalid request (duplicate properties) each return 2 with stderr starting `ERROR:` and empty stdout; an unreadable `--cache` file (a directory path, or a file with a malformed line) returns 2 with `ERROR:`.
    - Test 5 `test_cli_exit_3_when_evals_limit_would_be_exceeded_and_nothing_is_committed`: a request with two distinct `ok` candidates, `--evals-limit 1`, `--cache tmp/cache.jsonl` -> 3, stderr starts `BUDGET:`, stdout empty, the cache file does not exist; the same with `--evals-limit 2` -> 0 and the cache file has one line per `ok`/`missing` result.
    - Test 6 `test_cli_cache_round_trip_marks_second_run_cached_with_zero_evals`: two runs of the example request with the same `--cache` path: run 1 stdout has `cost.evals` 3; run 2 stdout has every `cached` true, every result `evals` 0, response `cost.evals` 0, and the cache file did not grow (line count unchanged).
  </behavior>
  <files>tests/test_evaluate_table.py, tests/test_evaluate_cli.py, src/llm4pol/evaluate/__main__.py</files>
  <action>
    STEP 1 — RED: append Tests 1–3 to `tests/test_evaluate_table.py` and Tests 4–6 to `tests/test_evaluate_cli.py`; run both files -> RED on the missing flags (Tests 1–3 may already pass through the tracer's `error` conversion — that is fine; they pin it); commit `test(03-02): add failing error-path, retry, snapshot-mismatch and CLI --cache/--evals-limit tests (D-02, D-07, EVAL-02)`.

    STEP 2 — `__main__.py` (Code Example 5): add `--cache PATH` (optional; `JsonlCache(Path)` when given, else `JsonlCache()`) and `--evals-limit N` (optional non-negative int). Construction of the cache (replay) and the evaluation are wrapped so that `BudgetExceeded` prints `BUDGET: {exc}` to stderr and returns 3, and `CacheError` / `OSError` print `ERROR: {exc}` and return 2; the request-parsing branch of 03-01 keeps returning 2. Backend read failures stay per-result `error` with exit 0 (D-02). Update the module docstring's exit-code table (0 / 2 / 3) and the `evaluate` pixi task comment if the flag list is mentioned there. Should Tests 1–3 reveal that `TableBackend._frame` memoises a failed load or that `_lookup` misses an exception class, fix `backends/table.py` / `evaluator.py` in this task.

    STEP 3 — GREEN: both files pass; `pixi run --manifest-path env/pixi.toml check` -> `SUMMARY: 7/7 steps passed`; commit `feat(03-02): CLI --cache and --evals-limit with exit 3 on BudgetExceeded; error status pinned as retryable and never cached (D-02, D-07, R-4, EVAL-02)`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_table.py tests/test_evaluate_cli.py -v</automated>
    <fails_when>exit code is non-zero, or any of `test_error_when_the_parquet_is_absent_consumes_no_evals_and_is_not_cached`, `test_error_is_retried_once_the_parquet_appears`, `test_error_on_snapshot_mismatch_names_the_exception_class`, `test_cli_exit_2_on_missing_request_bad_json_and_schema_failure`, `test_cli_exit_3_when_evals_limit_would_be_exceeded_and_nothing_is_committed`, `test_cli_cache_round_trip_marks_second_run_cached_with_zero_evals` is not listed as PASSED</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml evaluate --help | grep -c -- "--evals-limit\|--cache"</automated>
    <fails_when>prints a number below 2 — a flag is missing from the CLI (D-07)</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits in order: `test(03-02): add failing error-path, retry, snapshot-mismatch and CLI --cache/--evals-limit tests (D-02, D-07, EVAL-02)` then `feat(03-02): CLI --cache and --evals-limit with exit 3 on BudgetExceeded; error status pinned as retryable and never cached (D-02, D-07, R-4, EVAL-02)`.
    - An absent parquet yields `error` / `FileNotFoundError` with `evals` 0 and nothing cached; the same evaluator answers `ok` once the file exists; a wrong `llm4pol.snapshot` yields `error` / `SnapshotMismatch` (D-02, F-03).
    - CLI exit codes: 0 on success, 2 for an absent request, invalid JSON, a schema failure or an unreadable cache, 3 on `BudgetExceeded` with `BUDGET:` on stderr and no cache file written (D-07).
    - The gate prints `SUMMARY: 7/7 steps passed`.
  </acceptance_criteria>
  <done>Charter section 13 M2 (1) is complete for all four statuses: `error` is tested as retryable, budget-free and uncached, and the CLI exposes the cache and the evals limit with the documented exit codes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSONL cache file -> `JsonlCache` replay | A hand-editable file on disk becomes served results |
| candidate parquet -> `TableBackend` | A substituted or absent table becomes `error` or wrong values |
| `evals_limit` -> `Evaluator` | The refusal must leave no partial state |
| `--cache` / `--request` paths -> CLI | Caller-supplied filesystem paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | `JsonlCache` replay | high | mitigate | every replayed record passes `EvalResult.from_json` type checks; a malformed line is a loud `CacheError` with path and line number (no silent skipping); the key includes `source`, so a record for another snapshot is a miss, not a hit |
| T-03-08 | Tampering | `TableBackend._frame` | high | mitigate | metadata `llm4pol.snapshot` must equal `PropertyTable.snapshot` else `SnapshotMismatch` -> `error` (Test 3) |
| T-03-09 | Repudiation | `error` cached as a fact | medium | mitigate | `error` is never queued for `put_many`; Test 1/2 prove the retry |
| T-03-10 | Denial of Service | unbounded in-memory cache | low | accept | F-48: 400 evals x 3 properties is 1,200 entries; a full-table sweep is not an M2 use; `len(cache)` is exposed for monitoring |
| T-03-11 | Information Integrity | NaN in JSONL | medium | mitigate | backend maps NaN -> None; `allow_nan=False` on every `json.dumps` (F-34); Test 9 asserts no `NaN` token |
| T-03-12 | Tampering | partial commit on `BudgetExceeded` | medium | mitigate | two-phase evaluate: the check precedes `put_many` and `charge` (Test 8) |
| T-03-13 | Elevation of Privilege | `--cache` path | low | accept | developer CLI, local user, path opened as given; documented in the module docstring |
| T-03-SC | Tampering | package installs | low | accept | This plan installs nothing |
</threat_model>

## Artifacts this phase produces

**New files (this plan):** `src/llm4pol/evaluate/cache.py` (`CacheKey`, `CacheError`, `key_for`,
`canonical_line`, `JsonlCache`), `src/llm4pol/evaluate/budget.py` (`BudgetMeter`, `BudgetExceeded`),
`tests/test_evaluate_cache_budget.py` (11 tests).

**Modified files:** `src/llm4pol/evaluate/evaluator.py` (constructor keyword arguments; two-phase
`evaluate`; docstring on charging edges), `src/llm4pol/evaluate/__init__.py` (exports),
`src/llm4pol/evaluate/__main__.py` (`--cache`, `--evals-limit`, exit 3), `tests/test_evaluate_table.py`
(+3 `error` tests), `tests/test_evaluate_cli.py` (+3 tests), `docs/governance/INVARIANTS.md` (A-3 row).

**Commits (in order):** `test(03-02): add failing cache-hit …` -> `feat(03-02): JsonlCache … BudgetMeter …
promote A-3` -> `test(03-02): add failing error-path …` -> `feat(03-02): CLI --cache and --evals-limit …`.

**Produced by the other plans:** 03-01 (contract, schemas, examples, backend, evaluator skeleton, CLI
`--request`/`--root`); 03-03 (import-linter contracts, A-1/A-2 rows, real-file batch proof, evidence, CI).

**Not produced, by decision:** a cache size bound (F-48); per-line fsync (R-4); a combined budget
score (A-3); `backends/radonpy.py` (M9).

## Source audit (whole phase; the plan column names the task)

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | one contract backed by `table`; four statuses; cache that never double-charges; two-currency budget | 03-01 T1; 03-02 T1 (cache, budget), T2 (`error`) | COVERED | |
| REQ | EVAL-01 | schemas; suite loads them | 03-01 T1, T2 | COVERED | |
| REQ | EVAL-02 | one status each; `unsupported`/`missing` free; `error` retryable | 03-01 T1, T3; 03-02 T2 | COVERED | |
| REQ | EVAL-03 | provenance fields on every response | 03-01 T1, T3 | COVERED | |
| REQ | EVAL-04 | cache key; hit leaves `evals` unchanged | 03-02 T1 | COVERED | memory + replay |
| REQ | EVAL-05 | batch of 100 in order | 03-01 T3; 03-03 T2 | COVERED | |
| REQ | EVAL-06 | import-linter contracts | 03-03 T1 | COVERED | third clause flagged (R-1 vs EVAL-06) |
| CONTEXT | D-01 | shapes, examples, ordering | 03-01 T1, T2, T3 | COVERED | |
| CONTEXT | D-02 | taxonomy; `error` retryable with exception class name | 03-01 T1, T3; 03-02 T2 | COVERED | |
| CONTEXT | D-03 | value semantics; no population filter | 03-01 T1, T3 | COVERED | |
| CONTEXT | D-04 | cache key, JSONL, per-candidate charging, `BudgetMeter`, `BudgetExceeded` | 03-02 T1 (+ 03-01 T1 charging) | COVERED | |
| CONTEXT | D-05 | contract, `Backend`, `TableBackend`, no radonpy | 03-01 T1 | COVERED | |
| CONTEXT | D-06 | import-linter clauses | 03-03 T1 | COVERED | |
| CONTEXT | D-07 | CLI flags and exit codes 0/2/3 | 03-01 T1; 03-02 T2 | COVERED | |
| CONTEXT | D-08 | fixture, skip, contract tests, INVARIANTS rows | 03-01, 03-02 T1 (A-3), 03-03 | COVERED | |
| CONTEXT | R-1 | A-2 layers form | 03-03 T1 | COVERED | |
| CONTEXT | R-2 | per-candidate-per-request charging | 03-02 T1 (Tests 4–6) | COVERED | |
| CONTEXT | R-3 | tier enum; malformed id exit 2 | 03-01 T1, T2 | COVERED | |
| CONTEXT | R-4 | flush per request; never cache `error`; stored `cached: false`; NaN -> None | 03-02 T1 (Tests 9, 11), T2 (Tests 1–3) | COVERED | |
| CONTEXT | R-5 | pandas indexed by `candidate_id`, loaded once | 03-01 T1 | COVERED | |
| CONTEXT | Discretion | persistence off unless `cache_path` | 03-02 T1 (`JsonlCache()` default) | COVERED | |
| RESEARCH | Pattern 4, 5, 6 | two-phase, cache policy, meter | 03-02 T1 | COVERED | |
| RESEARCH | Pitfalls 3, 4, 5 | replay `cached`/`evals`; CRLF; per-request charging | 03-02 T1 | COVERED | |
| RESEARCH | Security: cache poisoning, snapshot substitution | re-validate replay; metadata assertion | 03-02 T1, T2 | COVERED | |
| CHARTER | section 13 M2 (1) | each status; `error` retried | 03-01 T3; 03-02 T2 | COVERED | |
| CHARTER | section 13 M2 (2) | cache hit leaves `evals` unchanged | 03-02 T1; 03-03 T2 | COVERED | |
| CHARTER | section 13 M2 (3), (4) | batch order; provenance | 03-01 T3; 03-03 T2 | COVERED | |
| CHARTER | section 13 M2 (5) | schema + import-linter in the gate | 03-01 T2; 03-03 T1 | COVERED | |
| CHARTER | section 4 A-3 | two currencies | 03-02 T1 | COVERED | promoted here |
| SCOPE | — | CI green without data; evidence | 03-03 T2, T3 | COVERED | |
| DEFERRED | — | radonpy, hpc.json, second tables, populations | — | EXCLUDED | 03-CONTEXT Deferred Ideas |

<verification>
- A repeated request is fully cached with `evals` 0 (in memory and after JSONL replay); `BudgetExceeded` commits nothing; A-3's test runs in the gate and INVARIANTS.md names it.
- `error` from an absent or mismatched parquet is budget-free, uncached and retried; the CLI exits 0 / 2 / 3 as documented.
- The gate prints `SUMMARY: 7/7 steps passed`; at least 152 tests collected (135 after 03-01 + 11 + 6).
</verification>

<success_criteria>
ROADMAP Phase 3 success criteria 1 (now including `error`) and 2 hold and are evidenced by named tests
on the synthetic table; the cache, the meter and the refusal are wired into the same `Evaluator` the
tracer proved, without restructuring it; A-3 is promoted.
</success_criteria>

<output>
Create `.planning/phases/03-evaluator-table-backend/03-02-SUMMARY.md` when done. Record the four commit
shas, the cache-line count and byte check of Test 9, the `BudgetExceeded` message format, the A-3 row
text, the test count collected, and any tracer defect Tests 1–3 of Task 2 revealed with its fix commit.
Note for 03-03: the INVARIANTS.md architecture table now has A-3 and A-7; A-1 and A-2 are inserted
before A-3 there.
</output>
