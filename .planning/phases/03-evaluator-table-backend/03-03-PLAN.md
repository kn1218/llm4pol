---
phase: 03-evaluator-table-backend
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - pyproject.toml
  - scripts/check.py
  - tests/test_import_boundary.py
  - tests/test_evaluate_real_file.py
  - docs/governance/INVARIANTS.md
  - .planning/phases/03-evaluator-table-backend/03-EVIDENCE.md
autonomous: true
requirements: [EVAL-05, EVAL-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`pyproject.toml [tool.importlinter]` declares four contracts — the Phase 2 `llm4pol.data` independence contract unchanged, `A-1: llm4pol.evaluate never imports llm4pol.loop, llm4pol.llm or llm4pol.run` (forbidden), `A-2: llm4pol.loop never imports llm4pol.evaluate.backends.radonpy` in the optional-layers form `[\"(llm4pol.evaluate.backends.radonpy)\", \"(llm4pol.loop)\"]`, and `A-1: only llm4pol.loop.agents may import llm4pol.llm` (forbidden, wildcard source, four `ignore_imports`) — and the gate's `import-linter` step prints `Contracts: 4 kept, 0 broken.` (D-06, R-1, F-18, F-22, F-23, EVAL-06, charter section 13 M2 (5))."
    - "The guards are live, not vacuous: a scratch copy of `src/llm4pol` in which `loop/__init__.py` imports `llm4pol.evaluate.backends.radonpy` makes the A-2 contract report `BROKEN` with the chain `llm4pol.loop -> llm4pol.evaluate.backends.radonpy`; a copy in which `loop/problem.py` imports `llm4pol.llm` breaks the llm rule while a copy where only `loop/agents` imports it stays KEPT (Pattern 7, F-22, F-23)."
    - "`docs/governance/INVARIANTS.md` carries A-1 and A-2 rows (before A-3) naming the contracts, the schema `provenance_tier` field and the tests; no charter text is edited (D-08, F-46, promotion policy)."
    - "On the pinned candidate parquet a 100-entry request with three properties each returns 300 results in request order, every status `ok` or `missing/value_absent`, no `error`, in under one second after the table is loaded; a second identical request is fully cached with `evals` 0; the CLI run on the same request exits 0 with a schema-valid response — tests that skip with a stated reason when the parquet is absent (D-07, D-08, F-09, F-10, F-41, EVAL-05, charter section 13 M2 (2), (3))."
    - "The seven-step gate is green locally and in a `workflow_dispatch` CI run on windows-latest and ubuntu-latest where the real-file tests skip by design, and `03-EVIDENCE.md` pairs each ROADMAP Phase 3 success criterion (charter section 13 M2 (1)–(5)) with the command and its verbatim output, containing no real candidate id or value (charter section 9; Phase 1 D-04/D-05 convention; data policy)."
  artifacts:
    - path: "pyproject.toml"
      provides: "four [[tool.importlinter.contracts]] blocks (Code Example 3 of 03-RESEARCH.md with ASCII names)"
      contains: "(llm4pol.evaluate.backends.radonpy)"
    - path: "tests/test_import_boundary.py"
      provides: "contract declaration test, gate-argv KEPT test, two negative guard-on-the-guard probes, evaluate/__init__ no-backend-import AST test, no radonpy stub"
      contains: "test_a2_layers_guard_breaks_on_a_violating_import"
    - path: "docs/governance/INVARIANTS.md"
      provides: "A-1 and A-2 rows in the Architecture (charter section 4) table"
      contains: "test_a2_layers_guard_breaks_on_a_violating_import"
    - path: "tests/test_evaluate_real_file.py"
      provides: "real-file batch of 100, cached second batch, CLI batch, snapshot metadata — all skipping without the parquet"
      contains: "test_real_batch_of_100_returns_300_results_in_request_order"
    - path: ".planning/phases/03-evaluator-table-backend/03-EVIDENCE.md"
      provides: "command-and-output evidence for charter section 13 M2 (1)–(5), the CI run id, head sha and both job conclusions, the owner flag on R-1 vs EVAL-06"
      contains: "CI run id:"
  key_links:
    - from: "pyproject.toml"
      to: "scripts/check.py"
      via: "step_import_linter runs lint_imports_argv() over [tool.importlinter]; the step output carries the Contracts: line the evidence cites"
      pattern: "lint_imports_argv"
    - from: "tests/test_import_boundary.py"
      to: "scripts/check.py"
      via: "the positive test runs the exact argv the gate uses; the negative probes run the same tool with --config on a scratch copy"
      pattern: "lint_imports_argv"
    - from: "tests/test_evaluate_real_file.py"
      to: "tests/conftest.py"
      via: "the real_candidates session fixture skips with the stated reason when the parquet is absent"
      pattern: "real_candidates"
    - from: ".planning/phases/03-evaluator-table-backend/03-EVIDENCE.md"
      to: ".github/workflows/ci.yml"
      via: "the evidence cites the workflow_dispatch run id and both matrix-job conclusions"
      pattern: "check \\(ubuntu-latest\\): success"
  prohibitions:
    - statement: "No task creates `src/llm4pol/evaluate/backends/radonpy.py`, `src/llm4pol/loop/`, `src/llm4pol/llm/` or `src/llm4pol/run/` in the repository; the negative probes write them only inside a temporary copy (D-05, F-18, CLAUDE.md: a stub is a blocker)."
      status: unverified
      verification: null
    - statement: "The A-2 contract is not written as `type = \"forbidden\"` with `source_modules = [\"llm4pol.loop\"]` (F-19: hard error while the module is absent) nor with a wildcard source `llm4pol.loop.*` (F-21: misses `loop/__init__.py`)."
      status: unverified
      verification: null
    - statement: "No real candidate id or property value is written into `03-EVIDENCE.md`, a test file or a committed request; the real-file request lives in a temporary directory and the evidence quotes counts and PASSED lines only (data policy, Phase 2 aggregates-only rule)."
      status: unverified
      verification: null
    - statement: "No push happens before the local seven-step gate prints `SUMMARY: 7/7 steps passed` and the history scan reports zero hits (R-3, R-4)."
      status: unverified
      verification: null
---

<objective>
Freeze the import boundary and prove M2 on the real table. Task 1 lands the four `[tool.importlinter]`
contracts exactly as research probed them (A-1 forbidden; A-2 in the optional-layers form R-1 fixed;
the "only `loop.agents` may import `llm`" rule in the wildcard form; the Phase 2 data contract
untouched), with a guard-on-the-guard test that proves the absent-module contracts break on a violating
import, and promotes A-1 and A-2 in INVARIANTS.md. Task 2 adds the real-file tests: 100 pinned
candidates x 3 properties -> 300 ordered results, a fully cached second batch, the CLI run — skipping
without the parquet so CI stays green. Task 3, split at the push boundary as 02-05 did, pushes,
dispatches CI on both platforms and writes `03-EVIDENCE.md` mapping charter section 13 M2 (1)–(5) to
commands and outputs.

Precedence flag for the owner (not resolved here): REQUIREMENTS.md EVAL-06 names three clauses
including "restrict `llm4pol.llm` to `llm4pol.loop.agents`"; CONTEXT R-1 says that rule "enters with
Phase 6, not now". REQUIREMENTS.md outranks a phase CONTEXT file (CLAUDE.md precedence; ADR-0002), the
F-23 form is proven KEPT today and BROKEN on a violation, and it costs one TOML block — so this plan
adds it and records the deviation from R-1 in the task, the summary and the evidence. If the owner
prefers R-1, removing one contract block and one test is the whole reversal.

Purpose: charter section 7 freezes the import boundary at M2; section 13 M2 (5) requires the first
import-linter contracts inside the gate; section 9 makes a milestone done when its exit criterion is
reproduced, not when tasks close.

Output: the four contracts, `tests/test_import_boundary.py`, the A-1 / A-2 rows, `tests/test_evaluate_real_file.py`,
a corrected comment in `scripts/check.py`, `03-EVIDENCE.md`, a green CI run.

Grounding facts (03-RESEARCH.md; cite, never re-derive): F-17 today's `[tool.importlinter]` (one
contract) and `check.lint_imports_argv()`; F-18 absent *forbidden* modules are skipped (KEPT); F-19
an absent *source* module is a hard error (`Module 'llm4pol.loop' does not exist.`); F-21 wildcard
sources miss `loop/__init__.py`; F-22 the optional-layers form is KEPT while absent and BROKEN with
`llm4pol.loop -> llm4pol.evaluate.backends.radonpy` when `loop/__init__.py` imports it; F-23 the
wildcard-source llm rule is KEPT today, BROKEN on `llm4pol.loop.problem -> llm4pol.llm`, and the
`loop.agents` import is ignored; F-49 layers contracts flag indirect chains; F-02 the real parquet
(78,676 rows; 23,880,837 bytes); F-03 its `llm4pol.snapshot` metadata; F-05 TC null medians 9,635;
F-09 load 0.05 s + `to_pandas` 0.003 s; F-10 a 100 x 9 batch is <= 10 ms; F-41 the skip pattern;
F-42 test baseline and CI `104 passed, 12 skipped` before this phase; F-47 `gh` 2.96.0, origin
`kn1218/llm4pol`, CI `workflow_dispatch` on two OSes. Today's gate line is `Contracts: 1 kept, 0
broken.`; after Task 1 it is `Contracts: 4 kept, 0 broken.` Contract names for the three new blocks
use ASCII (`charter section 6`) so the console rendering of the step output is clean on a cp949
locale; the Phase 2 name keeps its existing text. Workstation guard (Phase 1): a hook refuses Bash
commands containing git's `-n` short flag — use `--max-count`; write commit messages to a scratchpad
file and commit with `-F`. Ad-hoc commands need `PYTHONPATH=src`.
</objective>

<execution_context>
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-evaluator-table-backend/03-CONTEXT.md
@.planning/phases/03-evaluator-table-backend/03-RESEARCH.md
@.planning/phases/03-evaluator-table-backend/03-01-SUMMARY.md
@.planning/phases/03-evaluator-table-backend/03-02-SUMMARY.md
@.planning/phases/02-data-foundation/02-05-PLAN.md
@.planning/phases/02-data-foundation/02-EVIDENCE.md
@CLAUDE.md
@docs/MASTER-PLAN.md
@docs/governance/INVARIANTS.md
@docs/governance/ADR/0003-development-methodology.md
@pyproject.toml
@scripts/check.py
@.github/workflows/ci.yml
@tests/conftest.py
@tests/test_data_real_file.py
@tests/test_check_inventory.py
@src/llm4pol/evaluate/__init__.py
@src/llm4pol/evaluate/__main__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four import-linter contracts, the guard-on-the-guard test, A-1 and A-2 promoted (D-06, R-1, D-08, EVAL-06, A-1, A-2, charter section 13 M2 (5))</name>
  <read_first>
    - `.planning/phases/03-evaluator-table-backend/03-RESEARCH.md` — Code Example 3 (the exact TOML; every line probed), Pattern 7 (the negative probe recipe: `shutil.copytree(..., ignore=shutil.ignore_patterns("__pycache__"))`, temp TOML, `--config`, `PYTHONPATH=<tmp>/src`), Pitfall 1, Anti-Patterns (no radonpy stub; no wildcard source for A-2), F-17..F-23, F-49, Open Question 1
    - `.planning/phases/03-evaluator-table-backend/03-CONTEXT.md` — D-06 (three clauses; `evaluate` may import `data`), R-1 (layers form; the llm-rule deferral this plan deviates from per EVAL-06 — see the objective), D-08 (INVARIANTS rows)
    - `.planning/REQUIREMENTS.md` EVAL-06 (three clauses) and `CLAUDE.md` § Source of truth and precedence
    - `pyproject.toml` `[tool.importlinter]` (the Phase 2 contract and its comment: "Phase 3 (EVAL-06) widens this to the full boundary table"); `scripts/check.py::lint_imports_argv` (accepts `extra` argv) and `step_import_linter` (its comment still says zero contracts — stale since Phase 2)
    - `docs/governance/INVARIANTS.md` — the architecture table (A-3 and A-7 rows exist after 03-02; insert A-1, A-2 before A-3), the preamble sentence on A-1..A-7, `## Promotion policy`
    - `docs/MASTER-PLAN.md` section 4 rows A-1, A-2, section 6 package-boundary table, section 7 freeze table; `docs/governance/ADR/0003-development-methodology.md` (A-2's origin)
    - `tests/test_check_inventory.py::test_check_steps_include_schema_inventory_after_import_linter` (step order stays)
  </read_first>
  <behavior>
    - Test 1 `tests/test_import_boundary.py::test_a1_a2_contracts_are_declared_with_exact_modules`: `tomllib` parse of `pyproject.toml`; `[tool.importlinter]` has `root_package == "llm4pol"`, `include_external_packages is True`, exactly four contracts; the contract whose name starts with `A-1: llm4pol.evaluate never imports` has `type == "forbidden"`, `source_modules == ["llm4pol.evaluate"]`, `forbidden_modules == ["llm4pol.loop", "llm4pol.llm", "llm4pol.run"]`; the one starting `A-2:` has `type == "layers"` and `layers == ["(llm4pol.evaluate.backends.radonpy)", "(llm4pol.loop)"]`; the one starting `A-1: only llm4pol.loop.agents` has `type == "forbidden"`, `source_modules == ["llm4pol.*"]`, `forbidden_modules == ["llm4pol.llm"]`, `ignore_imports == ["llm4pol.loop.agents -> llm4pol.llm", "llm4pol.loop.agents -> llm4pol.llm.*", "llm4pol.loop.agents.* -> llm4pol.llm", "llm4pol.loop.agents.* -> llm4pol.llm.*"]`, `unmatched_ignore_imports_alerting == "none"`; the Phase 2 contract still has `source_modules == ["llm4pol.data"]` and `"sklearn" in forbidden_modules` and `"llm4pol.evaluate" in forbidden_modules`.
    - Test 2 `test_a1_contracts_are_declared_and_kept_by_the_gate_argv`: `subprocess.run(check.lint_imports_argv(), cwd=REPO_ROOT, env={**os.environ, "PYTHONPATH": "src"}, capture_output=True, text=True, encoding="utf-8", errors="replace")` returns 0; the combined output contains `Contracts: 4 kept, 0 broken.`, `output.count(" KEPT") == 4`, and `"BROKEN" not in output`.
    - Test 3 `test_a2_layers_guard_breaks_on_a_violating_import` (negative, F-22): copy `src/llm4pol` to `tmp_path / "src" / "llm4pol"` ignoring `__pycache__`; write `loop/__init__.py` containing `from llm4pol.evaluate.backends import radonpy` and an empty `evaluate/backends/radonpy.py` inside the copy; write `tmp_path / "probe.toml"` holding `[tool.importlinter]` with `root_package = "llm4pol"` and the A-2 contract block read back from `pyproject.toml` (same name, type, layers); run `check.lint_imports_argv(["--config", str(probe)])` with `cwd=tmp_path` and `PYTHONPATH=<tmp>/src`; returncode is 1, output contains `BROKEN` and `llm4pol.loop -> llm4pol.evaluate.backends.radonpy`. The repository's own `src/llm4pol/evaluate/backends/radonpy.py` does not exist before or after the test.
    - Test 4 `test_llm_rule_breaks_when_a_non_agents_module_imports_llm` (negative, F-23): the same scratch-copy recipe with an empty `llm/__init__.py`, an empty `loop/__init__.py`, `loop/agents/__init__.py` containing `import llm4pol.llm` and `loop/problem.py` containing `import llm4pol.llm`, and a probe TOML holding the llm-rule contract: returncode 1, output contains `BROKEN` and `llm4pol.loop.problem -> llm4pol.llm`; a second copy where only `loop/agents/__init__.py` imports `llm4pol.llm` (no `problem.py`) returns 0 with `KEPT` (the exemption works).
    - Test 5 `test_evaluate_package_init_imports_no_backend_and_no_radonpy_stub_exists` (F-49, D-05): `ast.parse` of `src/llm4pol/evaluate/__init__.py` has no `Import`/`ImportFrom` node whose module or alias names contain `backends`; `Path("src/llm4pol/evaluate/backends/radonpy.py")` does not exist; neither `src/llm4pol/loop` nor `src/llm4pol/llm` nor `src/llm4pol/run` exists.
  </behavior>
  <files>pyproject.toml, scripts/check.py, tests/test_import_boundary.py, docs/governance/INVARIANTS.md</files>
  <action>
    STEP 1 — RED: write Tests 1–5 in a new `tests/test_import_boundary.py` (import `check` through the `tests/conftest.py` `sys.path` insertion, as `tests/test_check_inventory.py` does); run `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_import_boundary.py -x -q` -> RED (Tests 1–4 fail on the missing contracts; Test 5 passes); commit `test(03-03): add failing import-boundary contract, gate-argv and guard-on-the-guard tests (D-06, R-1, EVAL-06)`.

    STEP 2 — `pyproject.toml`: after the Phase 2 contract block append the three blocks of Code Example 3 of 03-RESEARCH.md, keeping every `type`, `source_modules`, `forbidden_modules`, `layers`, `ignore_imports` and `unmatched_ignore_imports_alerting` value verbatim, with ASCII names — `A-1: llm4pol.evaluate never imports llm4pol.loop, llm4pol.llm or llm4pol.run (charter section 6)`, `A-2: llm4pol.loop never imports llm4pol.evaluate.backends.radonpy (ADR-0003, charter section 6)`, `A-1: only llm4pol.loop.agents may import llm4pol.llm (charter section 6, EVAL-06)` — and the explanatory comments of Code Example 3 (absent forbidden modules are skipped, F-18; the layers form exists because a forbidden contract errors on an absent source, F-19; both layers optional; Phase 5 may rewrite A-2 as `forbidden` once `llm4pol.loop` exists; the converse `llm` rule lands in Phase 6). Add one comment line on the third block: added per REQUIREMENTS.md EVAL-06, which outranks CONTEXT R-1's deferral (flagged for the owner). Update the Phase 2 block's trailing comment sentence from "Phase 3 (EVAL-06) widens this …" to a past-tense pointer at the three blocks below. In `scripts/check.py::step_import_linter` replace the stale "zero contracts" comment with one stating that `[tool.importlinter]` declares four contracts (Phase 2 D-8; Phase 3 A-1, A-2 and the llm rule) and that the step prints each contract's KEPT/BROKEN line and the `Contracts:` summary the evidence cites — no code change.

    STEP 3 — INVARIANTS.md (D-08, F-46): insert before the A-3 row: `A-1` — "The evaluator is an interface: `table` and `radonpy` share one contract and no code assumes equal accuracy (`provenance_tier`)" with guard cell naming `protocol/schemas/eval-response.json` (`provenance_tier` required on every result; enum `md_simulated` at M2, extended by a new schema version per A-5), the `[tool.importlinter]` contracts `A-1: llm4pol.evaluate never imports …` and `A-1: only llm4pol.loop.agents may import llm4pol.llm` (`import-linter` step), `tests/test_import_boundary.py::test_a1_contracts_are_declared_and_kept_by_the_gate_argv`, `tests/test_evaluate_table.py::test_every_result_carries_backend_source_and_provenance_tier`; `A-2` — "The loop package never imports the simulation backend (ADR-0003)" with guard cell naming the `[tool.importlinter]` layers contract `A-2: …` (optional layers: KEPT while `llm4pol.loop` is absent, BROKEN on a violating import including from `loop/__init__.py`), `tests/test_import_boundary.py::test_a1_a2_contracts_are_declared_with_exact_modules` and `::test_a2_layers_guard_breaks_on_a_violating_import`. Update the preamble sentence "they are prose until that milestone" to add that A-1, A-2 (M2), A-3 (M2 guard; M3 ledger guard pending) and A-7 (M1) have landed below. Do not edit the charter.

    STEP 4 — GREEN: the test file passes; `pixi run --manifest-path env/pixi.toml check` prints the four KEPT lines and `Contracts: 4 kept, 0 broken.` inside the `import-linter` step and `SUMMARY: 7/7 steps passed`; commit `feat(03-03): freeze the import boundary — A-1 forbidden, A-2 optional-layers, the loop.agents-only llm rule; guard-on-the-guard tests; promote A-1 and A-2 (D-06, R-1, D-08, EVAL-06, charter section 13 M2 (5))`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_import_boundary.py -v</automated>
    <fails_when>exit code is non-zero, or any of `test_a1_a2_contracts_are_declared_with_exact_modules`, `test_a1_contracts_are_declared_and_kept_by_the_gate_argv`, `test_a2_layers_guard_breaks_on_a_violating_import`, `test_llm_rule_breaks_when_a_non_agents_module_imports_llm`, `test_evaluate_package_init_imports_no_backend_and_no_radonpy_stub_exists` is not listed as PASSED</fails_when>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml lint-imports | grep -c "Contracts: 4 kept, 0 broken."</automated>
    <fails_when>prints anything other than 1 — the gate's import-linter step does not report four kept contracts (F-18, F-22, F-23)</fails_when>
    <automated>grep -c '(llm4pol.evaluate.backends.radonpy)' pyproject.toml</automated>
    <fails_when>prints anything other than 1 — the A-2 contract is not in the optional-layers form (R-1, F-22)</fails_when>
    <automated>ls src/llm4pol/evaluate/backends/ src/llm4pol/ | grep -cE "^(radonpy\.py|loop|llm|run)$"</automated>
    <fails_when>prints anything other than 0 — a stub module or a Phase 5/6 package was created in the repository (D-05)</fails_when>
    <automated>sed -n '/^| A-1 /p;/^| A-2 /p' docs/governance/INVARIANTS.md | grep -c "tests/test_import_boundary.py"</automated>
    <fails_when>prints a number below 2 — an A-1 or A-2 row is missing or does not name the test module</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits in order: `test(03-03): add failing import-boundary contract, gate-argv and guard-on-the-guard tests (D-06, R-1, EVAL-06)` then `feat(03-03): freeze the import boundary — A-1 forbidden, A-2 optional-layers, the loop.agents-only llm rule; guard-on-the-guard tests; promote A-1 and A-2 (D-06, R-1, D-08, EVAL-06, charter section 13 M2 (5))`.
    - `[tool.importlinter]` has exactly four contracts with the values of Test 1; `lint-imports` prints `Contracts: 4 kept, 0 broken.` (F-18, F-22, F-23); the two negative probes print `BROKEN` with the exact chains `llm4pol.loop -> llm4pol.evaluate.backends.radonpy` and `llm4pol.loop.problem -> llm4pol.llm`, and the agents-only copy stays KEPT.
    - INVARIANTS.md has A-1, A-2, A-3, A-7 rows in that order in the architecture table, each guard cell naming at least one test; the charter file is untouched — `git show --stat --format= <sha>` for each of this task's two commits lists no `docs/MASTER-PLAN.md` path, and `git status --porcelain -- docs/MASTER-PLAN.md` prints nothing.
    - No `radonpy.py`, `loop/`, `llm/` or `run/` exists under `src/llm4pol/`; the gate prints `SUMMARY: 7/7 steps passed`.
    - The summary records the R-1 deviation (third contract added per EVAL-06) as an owner flag.
  </acceptance_criteria>
  <done>Charter section 13 M2 (5) is complete: the first import-linter contracts run inside the gate, the absent-module contracts are proven live by negative probes, and A-1 / A-2 are guards named in INVARIANTS.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The real-file proof — 100 pinned candidates x 3 properties in order, cached second batch, CLI run (D-07, D-08, EVAL-05, charter section 13 M2 (2), (3))</name>
  <precondition>The pinned candidate parquet is present locally at `data/processed/candidates-041e5834.parquet` (23,880,837 bytes, F-02); if absent, run `pixi run --manifest-path env/pixi.toml python -m llm4pol.data fetch` then `... load` first (16–19 s). Nothing under `data/` is ever written by a test.</precondition>
  <read_first>
    - `tests/test_data_real_file.py` (module docstring: wall times, "no expected number is ever edited"; the `real_csv` skip wording) and `tests/conftest.py::real_candidates` (03-01; read-only; skips when the parquet is absent)
    - `.planning/phases/03-evaluator-table-backend/03-RESEARCH.md` — F-02, F-03, F-05, F-09, F-10, F-41, Validation Architecture row EVAL-05 (integration, skips in CI), Security Domain (data leakage row)
    - `src/llm4pol/evaluate/__main__.py` (03-02 flags), `tests/test_evaluate_cli.py` (in-process `main` pattern), `tests/test_evaluate_table.py` (helpers)
    - `docs/MASTER-PLAN.md` section 13 M2 ("배치 100건 순서·개수") and section 10 (aggregates only leave the machine)
  </read_first>
  <behavior>
    - Test 1 `tests/test_evaluate_real_file.py::test_real_table_backend_source_matches_the_parquet_snapshot_metadata`: `pq.ParquetFile(path).schema_arrow.metadata[b"llm4pol.snapshot"].decode() == "polyomics:general_polymers@041e5834" == TableBackend(path, properties=PropertyTable.load()).source` (F-03).
    - Test 2 `test_real_batch_of_100_returns_300_results_in_request_order`: the first 100 `candidate_id` values of the parquet (`pq.read_table(path, columns=["candidate_id"])`, `[:100].to_pylist()`), each with `["thermal_conductivity", "dielectric_const_dc", "tg"]`; one warm-up `backend.lookup(ids[0], "tg")` forces the load; then `time.perf_counter()` around `evaluate` is below 1.0 s (F-10: <= 10 ms expected); `len(results) == 300`; `[(r.candidate_id, r.property)] == flattened batch`; every status in `{"ok", "missing"}` and every `missing` has `reason == "value_absent"` (the ids come from the table); no `error`; every `ok` has `n_replicates >= 1` and `spread is None` exactly when `n_replicates < 2` (F-06/F-07); every result has `backend == "table"`, `source == "polyomics:general_polymers@041e5834"`, `provenance_tier == "md_simulated"`, `cached is False`; `response.cost.evals == len({r.candidate_id for r in results if r.status == "ok"})` and `<= 100`; `response.cost.cpu_hours == 0.0`; the response validates against `eval-response.json`. The test prints nothing and stores nothing.
    - Test 3 `test_real_second_identical_batch_is_fully_cached_with_zero_evals`: the same evaluator answers the same request again -> every `cached is True`, every `cost.evals == 0`, `response.cost == Cost(0, 0.0)`, `evaluator.meter.evals` unchanged from Test 2's value (charter section 13 M2 (2) on the real table).
    - Test 4 `test_real_cli_batch_of_100_exits_0_with_a_schema_valid_response`: write the 100-entry request to `tmp_path / "request.json"`; `cli.main(["--request", str(p), "--root", str(REPO_ROOT), "--cache", str(tmp_path / "cache.jsonl")])` returns 0; stdout parses and validates; 300 results in order; the cache file has exactly 300 lines (every result is `ok` or `missing`); a second `cli.main` with the same arguments returns 0 with every `cached` true and the line count unchanged.
  </behavior>
  <files>tests/test_evaluate_real_file.py</files>
  <action>
    STEP 1 — write the four tests in a new `tests/test_evaluate_real_file.py` whose module docstring states: the parquet is never committed, every test depends on `real_candidates` and skips with its stated reason when absent, locally every test must PASS, no expected number is edited to make a test pass, and the tests never print or persist a candidate id or value outside `tmp_path`. Record the measured wall time of Test 2 (load + evaluate) in a comment, as `test_data_real_file.py` does, for later sizing. Run `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_real_file.py -v` -> all four PASSED locally. The skip path cannot be exercised on this machine (the parquet is present): `pytest --collect-only -q tests/test_evaluate_real_file.py` must list four tests, and the CI run of Task 3 (no data) is the proof that they skip — record its `skipped` count there.

    STEP 2 — the gate: `pixi run --manifest-path env/pixi.toml check` -> `SUMMARY: 7/7 steps passed` (the real-file tests run locally; the pytest step takes roughly 40 s + this file's load time). Commit `test(03-03): prove the batch of 100 in request order, the cached second batch and the CLI run on the pinned candidate table (D-07, D-08, EVAL-05, charter section 13 M2 (2), (3))`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_real_file.py -v</automated>
    <fails_when>exit code is non-zero, or any of `test_real_table_backend_source_matches_the_parquet_snapshot_metadata`, `test_real_batch_of_100_returns_300_results_in_request_order`, `test_real_second_identical_batch_is_fully_cached_with_zero_evals`, `test_real_cli_batch_of_100_exits_0_with_a_schema_valid_response` is not listed as PASSED on this machine (a SKIPPED here means the precondition was not met)</fails_when>
    <automated>grep -cE "(^|[^0-9a-f])[0-9a-f]{16}([^0-9a-f]|$)" tests/test_evaluate_real_file.py</automated>
    <fails_when>prints anything other than 0 — a real candidate id was written into the test file (data policy; ids are read from the parquet at run time)</fails_when>
    <automated>git status --porcelain -- data/</automated>
    <fails_when>prints anything — a test wrote under `data/`</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - One commit `test(03-03): prove the batch of 100 in request order, the cached second batch and the CLI run on the pinned candidate table (D-07, D-08, EVAL-05, charter section 13 M2 (2), (3))`.
    - On the pinned parquet: 300 results for 100 entries in request order, statuses only `ok`/`missing` (`value_absent`), `evaluate` under 1.0 s after load, `cost.evals <= 100` equal to the distinct `ok` candidates, the second batch fully cached with `evals` 0, the CLI run exit 0 with a 300-line cache file (F-09, F-10).
    - The test file contains no 16-hex candidate id; nothing under `data/` changed; the gate prints `SUMMARY: 7/7 steps passed` with at least 161 tests collected (152 after 03-02 + 5 + 4).
  </acceptance_criteria>
  <done>Charter section 13 M2 (2) and (3) are reproduced on the pinned table by command, by tests that skip with a stated reason where the data is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Push, dispatch the CI gate on both platforms and record the M2 evidence (charter section 9, section 13 M2 (1)–(5); Phase 1 D-04/D-05 convention)</name>
  <reversibility rating="one-way">`git push origin main` publishes every reachable commit to the public repository `kn1218/llm4pol`; nothing published can be unpublished. REVERSIBILITY_GATES is off, so no checkpoint is inserted — the substitute guard is the history scan of the seven-step gate, re-run immediately before the push, which must print zero hits and `dotenv-path-in-history: 0 hit(s)`; the committed examples carry invented values only (03-01 Task 2 Test 2) and the real-file test file carries no candidate id (Task 2 grep).</reversibility>
  <precondition>`gh auth status` reports a logged-in account for github.com and `git remote get-url origin` names `kn1218/llm4pol` (F-47); Tasks 1–2 of this plan and every 03-01 / 03-02 commit are on local `main`; `git rev-parse --is-shallow-repository` prints `false`.</precondition>
  <read_first>
    - `.planning/phases/02-data-foundation/02-05-PLAN.md` Task 4 and `02-EVIDENCE.md` — the push -> dispatch -> watch -> evidence sequence, the section-per-criterion layout, the data-policy section, and the note that the evidence commit post-dates the cited run
    - `.planning/ROADMAP.md` § Phase 3 — Success Criteria 1–5 (charter section 13 M2) the evidence cites by number
    - `docs/MASTER-PLAN.md` section 9 (VERIFICATION cites exit-criterion numbers; milestone done = reproduction) and section 13 row M2
    - `.github/workflows/ci.yml` — workflow name `check`; the verifier reads `SUMMARY:` lines from its job logs
    - `protocol/examples/eval-response.example.json` (the synthetic response the evidence may quote — invented values) and `03-01-SUMMARY.md` / `03-02-SUMMARY.md` (commit shas, test counts, owner flags)
  </read_first>
  <files>.planning/phases/03-evaluator-table-backend/03-EVIDENCE.md</files>
  <action>
    STEP 1 — pre-push guard and push. Run the full gate once more and require `Steps: ruff check, ruff format --check, mypy, import-linter, schema-inventory, history-secret-scan, pytest`, `SUMMARY: 7/7 steps passed`, `Contracts: 4 kept, 0 broken.`, `llm4polcheck inventory: 3 entries, 3 instances processed`, zero hits per pattern class and `dotenv-path-in-history: 0 hit(s)`. Only then `git push origin main`; record `git rev-parse HEAD` as the pushed tip.

    STEP 2 — dispatch and watch (Phase 1 D-05 convention). `gh workflow run check --ref main`; then `gh run list --workflow=check --event workflow_dispatch --limit 1 --json databaseId,headSha,status --jq '.[0]'` until `headSha` equals the pushed tip (re-run the list command; never fall back to a push-triggered run); `gh run watch <RUN_ID> --exit-status`. On a red job read `gh run view <RUN_ID> --log-failed`, fix the cause with a new conventional commit (a CI-only failure is most likely a path or line-ending difference on ubuntu-latest, a real-file test that failed to skip — a fixture bug, fix the fixture — or the negative probe's scratch copy behaving differently on Linux; never edit the workflow to skip a step), re-run the local gate, push, dispatch again. Capture `gh run view <RUN_ID> --json databaseId,headSha,conclusion,url,jobs --jq '{databaseId,headSha,conclusion,url,jobs:[.jobs[]|{name,conclusion}]}'` and `gh run view <RUN_ID> --log | grep -E "SUMMARY:|llm4polcheck inventory|Contracts:|skipped|passed in|scanned [0-9]+ commit"` — both platforms must show `SUMMARY: 7/7 steps passed`, `llm4polcheck inventory: 3 entries, 3 instances processed`, `Contracts: 4 kept, 0 broken.` and a pytest line with `skipped` (the real-file tests of Phase 2 and Phase 3 and the PoLyInfo manifest test skip in CI; record the skipped count — expected 16 = 12 + 4).

    STEP 3 — evidence. Write `.planning/phases/03-evaluator-table-backend/03-EVIDENCE.md`: header `# Phase 3 — charter section 13 M2 evidence`, the UTC date, the pushed tip sha, the snapshot id, and the sentence that each section maps to a ROADMAP Phase 3 success criterion = charter section 13 M2 exit criterion. Sections, each a list of `command` -> fenced verbatim output pairs (the `-v` runs are `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest <file> -v -k <expr>`): `## (1) Each status has a passing test; unsupported and missing consume no evals; error is retried` — the PASSED lines of `test_ok_with_spread_when_n_ge_2`, `test_ok_with_null_spread_when_n_is_1`, `test_missing_value_absent_when_median_is_null`, `test_missing_candidate_unknown_for_a_well_formed_absent_id`, `test_unsupported_for_an_unregistered_key_and_the_barred_key_even_on_an_unknown_candidate`, `test_unsupported_and_missing_consume_no_evals`, `test_error_when_the_parquet_is_absent_consumes_no_evals_and_is_not_cached`, `test_error_is_retried_once_the_parquet_appears`; `## (2) A cache hit leaves evals unchanged` — the PASSED lines of `test_cache_hit_leaves_evals_unchanged_in_memory`, `test_cache_hit_leaves_evals_unchanged_after_jsonl_replay`, `test_cache_key_includes_backend_and_source`, `test_real_second_identical_batch_is_fully_cached_with_zero_evals`; `## (3) A batch of 100 returns 100·k results in request order` — the PASSED lines of `test_batch_of_100_synthetic_entries_returns_300_results_in_request_order` and `test_real_batch_of_100_returns_300_results_in_request_order`, plus one command whose output is counts only: build the 100-entry real request into a temporary directory with a short snippet (never committed; the snippet reads the first 100 ids from the parquet), run `pixi run --manifest-path env/pixi.toml evaluate --request <tmp>/request.json --root .` and pipe stdout through a one-liner printing `len(results)`, the `cost` object and the sorted set of statuses — quote that one line (e.g. `300 {'evals': N, 'cpu_hours': 0.0} ['missing', 'ok']`) and nothing else of the response; `## (4) Every response carries source, backend, provenance_tier, n_replicates, spread, unit and cost` — the PASSED lines of `test_every_result_carries_backend_source_and_provenance_tier`, `test_unit_n_replicates_and_spread_come_from_the_registry_and_the_candidate_table`, `test_a3_budget_meter_keeps_evals_and_cpu_hours_as_separate_currencies`, and the first result object of `protocol/examples/eval-response.example.json` quoted verbatim (invented values, synthetic id); `## (5) Schema validation and the import-linter contracts run inside the gate` — the gate's `llm4polcheck inventory: 3 entries, 3 instances processed` line, the four KEPT lines and `Contracts: 4 kept, 0 broken.` from `lint-imports`, the PASSED lines of `test_both_schemas_are_valid_draft_2020_12`, `test_committed_examples_validate_against_their_schemas`, `test_committed_response_example_equals_evaluator_output_on_the_synthetic_root`, `test_a1_contracts_are_declared_and_kept_by_the_gate_argv`, `test_a2_layers_guard_breaks_on_a_violating_import`, `test_llm_rule_breaks_when_a_non_agents_module_imports_llm`, and the A-1, A-2, A-3 rows printed from INVARIANTS.md with `sed` (`/^| A-1 /,/^| A-3 /p`); then `## Gate and CI` — the local `Steps:` and `SUMMARY: 7/7` lines, `CI run id: <RUN_ID>`, `CI head sha: <sha>`, `CI url: <url>`, `check (windows-latest): success`, `check (ubuntu-latest): success`, the grep'd log lines from both jobs including the `skipped` count; `## Owner flags` — (a) REQUIREMENTS.md EVAL-06 third clause vs CONTEXT R-1: the `loop.agents`-only llm contract was added per EVAL-06 (precedence), reversal is one TOML block and one test; (b) the D-04 charging edge (a new property of an already-cached candidate in a later request charges again — Open Question 2, pinned by `test_new_property_of_a_cached_candidate_charges_one_eval`); (c) Phase 2's open questions that touch the evaluator are resolved by D-03 (no population filter; the 303 twins are distinct ids) — stated, not decided; `## Data policy` — `git ls-files protocol/examples data/` output, the Task 2 grep result `0` on the real-file test file, and the statement that every 16-hex token in this evidence file is a synthetic-fixture id. Nothing in the file may contain a real candidate id, a real property value, or a secret-shaped string. Commit as `docs(03-03): record Phase 3 M2 evidence (charter section 13 M2 (1)–(5), CI run <RUN_ID>)` and push that commit too, noting in the summary that the evidence commit post-dates the cited CI run.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL` or `HIT`</fails_when>
    <automated>gh run view $(grep -m1 -oE "CI run id: [0-9]+" .planning/phases/03-evaluator-table-backend/03-EVIDENCE.md | grep -oE "[0-9]+") --json event,headBranch,conclusion,jobs --jq '[.event, .headBranch, .conclusion, ([.jobs[] | .name + "=" + .conclusion] | sort | join(","))] | join(" ")'</automated>
    <fails_when>the output is not `workflow_dispatch main success check (ubuntu-latest)=success,check (windows-latest)=success`</fails_when>
    <automated>gh run view $(grep -m1 -oE "CI run id: [0-9]+" .planning/phases/03-evaluator-table-backend/03-EVIDENCE.md | grep -oE "[0-9]+") --log | grep -c "SUMMARY: 7/7 steps passed"</automated>
    <fails_when>prints a number below 2 — fewer than both platforms printed the seven-step green summary</fails_when>
    <automated>gh run view $(grep -m1 -oE "CI run id: [0-9]+" .planning/phases/03-evaluator-table-backend/03-EVIDENCE.md | grep -oE "[0-9]+") --log | grep -c "Contracts: 4 kept, 0 broken."</automated>
    <fails_when>prints a number below 2 — the import-linter step did not report four kept contracts on both platforms</fails_when>
    <automated>gh run view $(grep -m1 -oE "CI run id: [0-9]+" .planning/phases/03-evaluator-table-backend/03-EVIDENCE.md | grep -oE "[0-9]+") --log | grep -c "llm4polcheck inventory: 3 entries, 3 instances processed"</automated>
    <fails_when>prints a number below 2 — the schema-inventory step did not process both examples on both platforms</fails_when>
    <automated>grep -c -E "^check \((windows|ubuntu)-latest\): success$" .planning/phases/03-evaluator-table-backend/03-EVIDENCE.md</automated>
    <fails_when>prints anything other than 2</fails_when>
    <automated>grep -c "^## (" .planning/phases/03-evaluator-table-backend/03-EVIDENCE.md</automated>
    <fails_when>prints anything other than 5 — a criterion section is missing</fails_when>
    <automated>grep -c "Owner flags" .planning/phases/03-evaluator-table-backend/03-EVIDENCE.md</automated>
    <fails_when>prints 0 — the R-1 vs EVAL-06 flag was not carried into the evidence</fails_when>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -c "import re, json; ev = open('.planning/phases/03-evaluator-table-backend/03-EVIDENCE.md', encoding='utf-8').read(); ok = {e['candidate_id'] for e in json.load(open('protocol/examples/eval-request.example.json', encoding='utf-8'))['batch']}; print(len([t for t in re.findall(r'(?:^|[^0-9a-f])([0-9a-f]{16})(?![0-9a-f])', ev, re.M) if t not in ok]))"</automated>
    <fails_when>prints anything other than 0 — a 16-hex token in the evidence is not a synthetic-fixture id (a real candidate id leaked; the 40-hex shas are not matched by the boundary-anchored pattern)</fails_when>
    <automated>git status --porcelain --branch</automated>
    <fails_when>the first line is not `## main...origin/main` (an `[ahead N]` or `[behind N]` suffix means the evidence commit was not pushed or the remote moved); tracked-file changes on further lines mean the working tree is dirty (the untracked GSD runtime artifacts noted in Phase 1 are acceptable)</fails_when>
  </verify>
  <acceptance_criteria>
    - One commit exists: `docs(03-03): record Phase 3 M2 evidence (charter section 13 M2 (1)–(5), CI run <RUN_ID>)`, plus any `fix(03-03): …` commit a red CI run forced (each named in the summary with the log line that caused it).
    - `03-EVIDENCE.md` contains `CI run id: <digits>`, `CI head sha: <40 hex>`, `check (windows-latest): success`, `check (ubuntu-latest): success`, two job-prefixed `SUMMARY: 7/7 steps passed` lines, two `Contracts: 4 kept, 0 broken.` lines, two `llm4polcheck inventory: 3 entries, 3 instances processed` lines, a pytest line with `skipped` from CI, the five criterion sections with the named PASSED lines, the counts-only line of the real CLI run, the A-1..A-3 INVARIANTS rows, the `## Owner flags` section and the `## Data policy` section.
    - The cited run's `headSha` equals the pushed tip of STEP 1 (or of the last fix commit), its event is `workflow_dispatch`, its branch `main`, both jobs `success`.
    - `origin/main` equals local `main` after the final push; no data file is tracked; the 16-hex check on the evidence prints 0.
  </acceptance_criteria>
  <done>The seven-step gate is green on both CI platforms with the real-file tests skipping by design, and `03-EVIDENCE.md` pairs every charter section 13 M2 exit criterion (1)–(5) with the command and verbatim output the Phase 3 verifier will cite, including the flagged R-1 vs EVAL-06 question for the owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `[tool.importlinter]` -> the gate | A contract that is KEPT because its modules are absent is indistinguishable from a vacuous one |
| scratch copy of `src/llm4pol` -> negative probes | The probes must never touch the real tree |
| pinned parquet -> real-file tests -> committed files | Real ids and values must not leave `tmp_path` |
| local history -> public remote | The push publishes every reachable commit |
| CI logs -> evidence | The cited run must be the dispatched run on the pushed tip |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Repudiation | vacuous KEPT contracts (A-2, llm rule) | high | mitigate | `test_a2_layers_guard_breaks_on_a_violating_import` and `test_llm_rule_breaks_when_a_non_agents_module_imports_llm` prove BROKEN on a violating scratch copy (Pattern 7, F-22, F-23) |
| T-03-15 | Tampering | a radonpy/loop/llm stub created "so the contract has a target" | medium | mitigate | `test_evaluate_package_init_imports_no_backend_and_no_radonpy_stub_exists` asserts none exists; probes write only under `tmp_path` |
| T-03-16 | Information Disclosure | real candidate ids or values in `tests/test_evaluate_real_file.py`, a committed request or `03-EVIDENCE.md` | medium | mitigate | ids are read from the parquet at run time; the test-file grep for 16-hex tokens prints 0; the evidence's 16-hex check accepts synthetic ids only; the real CLI output is reduced to counts before quoting |
| T-03-17 | Information Disclosure | secret in history published by the push | critical | mitigate | the seven-step gate's `history-secret-scan` runs immediately before the push; zero hits required |
| T-03-18 | Spoofing | a CI run on the wrong commit or event cited as evidence | low | mitigate | the evidence names run id, head sha, event, branch and both job conclusions; the verify re-queries the run by id and requires `workflow_dispatch main success` |
| T-03-19 | Denial of Service | the real-file tests in CI (no data) | low | mitigate | `real_candidates` skips with the stated reason; CI records 16 skipped |
| T-03-SC | Tampering | package installs | low | accept | This plan installs nothing |
</threat_model>

## Artifacts this phase produces

**New files (this plan):** `tests/test_import_boundary.py` (5 tests), `tests/test_evaluate_real_file.py`
(4 tests), `.planning/phases/03-evaluator-table-backend/03-EVIDENCE.md`.

**Modified files:** `pyproject.toml` (three `[[tool.importlinter.contracts]]` blocks; comment on the
Phase 2 block), `scripts/check.py` (comment in `step_import_linter` only), `docs/governance/INVARIANTS.md`
(A-1, A-2 rows; preamble sentence).

**Commits (in order):** `test(03-03): add failing import-boundary …` -> `feat(03-03): freeze the import
boundary …` -> `test(03-03): prove the batch of 100 …` -> (Task 3) `docs(03-03): record Phase 3 M2
evidence …` (plus any `fix(03-03): …`).

**Produced by the other plans:** 03-01 (contract, schemas, examples, inventory, `TableBackend`,
`Evaluator`, CLI, fixtures, contract/table/CLI tests); 03-02 (`JsonlCache`, `BudgetMeter`,
`BudgetExceeded`, `error` path, `--cache`/`--evals-limit`, A-3 row).

**Not produced, by decision:** `backends/radonpy.py`, `config/hpc.json` (M9); `llm4pol.loop`, `llm`,
`run` (Phases 4–6); the A-2 `forbidden` rewrite (Phase 5, once `loop` exists); the converse "`llm`
imports nothing of `llm4pol`" contract (Phase 6); a resolution of R-1 vs EVAL-06 (owner); the Phase 3
`VERIFICATION.md` (written by the verifier from `03-EVIDENCE.md`).

## Source audit (whole phase; the plan column names the task)

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | one contract backed by `table`; four statuses; cache; two-currency budget | 03-01 T1; 03-02 T1, T2 | COVERED | |
| REQ | EVAL-01 | schemas; suite loads them | 03-01 T1, T2 | COVERED | |
| REQ | EVAL-02 | one status each; free `unsupported`/`missing`; `error` retryable | 03-01 T1, T3; 03-02 T2 | COVERED | |
| REQ | EVAL-03 | provenance fields on every response | 03-01 T1, T3; 03-03 T2 (real) | COVERED | |
| REQ | EVAL-04 | cache key; hit leaves `evals` unchanged | 03-02 T1; 03-03 T2 (real) | COVERED | |
| REQ | EVAL-05 | batch of 100 in request order with matching count | 03-01 T3 (synthetic); 03-03 T2 (real, + CLI) | COVERED | |
| REQ | EVAL-06 | `loop` ↛ `evaluate.backends.radonpy`; `evaluate` ↛ `loop`/`llm`; `llm` only from `loop.agents` | 03-03 T1 | COVERED | third clause deviates from R-1 by precedence; flagged |
| CONTEXT | D-01 | shapes, examples, ordering | 03-01 T1, T2, T3 | COVERED | |
| CONTEXT | D-02 | taxonomy; `error` | 03-01 T1, T3; 03-02 T2 | COVERED | |
| CONTEXT | D-03 | no population filter; `md_simulated` | 03-01 T1, T3 | COVERED | |
| CONTEXT | D-04 | cache, charging, meter, refusal | 03-01 T1; 03-02 T1 | COVERED | |
| CONTEXT | D-05 | contract, Protocol, `TableBackend`, no radonpy | 03-01 T1; 03-03 T1 (Test 5) | COVERED | |
| CONTEXT | D-06 | (1) `evaluate` forbidden; (2) `loop` ↛ radonpy; (3) data contract kept | 03-03 T1 | COVERED | (2) in the R-1 layers form |
| CONTEXT | D-07 | CLI, exit codes, batch of 100 | 03-01 T1; 03-02 T2; 03-03 T2 | COVERED | |
| CONTEXT | D-08 | fixtures, skip, listed tests, INVARIANTS rows | 03-01; 03-02 T1 (A-3); 03-03 T1 (A-1, A-2), T2 | COVERED | |
| CONTEXT | R-1 | A-2 layers form now; `forbidden` at Phase 5 | 03-03 T1 | COVERED | llm-rule deferral overridden by EVAL-06 — flagged |
| CONTEXT | R-2 | per-candidate-per-request charging | 03-01 T1, T3; 03-02 T1 | COVERED | |
| CONTEXT | R-3 | tier enum; malformed id exit 2 | 03-01 T1, T2 | COVERED | |
| CONTEXT | R-4 | flush per request; never cache `error`; stored `cached: false`; NaN -> None | 03-02 T1, T2 | COVERED | |
| CONTEXT | R-5 | pandas indexed by `candidate_id`, loaded once | 03-01 T1 | COVERED | |
| CONTEXT | Specific Ideas | reuse `load_registry`; O(1) lookup; module size; evidence + CI | 03-01 T1; 03-03 T3 | COVERED | |
| RESEARCH | Pattern 7 | guard-on-the-guard | 03-03 T1 | COVERED | both absent-module contracts probed |
| RESEARCH | Pitfall 1 | A-2 not as forbidden-with-absent-source | 03-03 T1 | COVERED | prohibition |
| RESEARCH | Pitfall 9 | protocol README | 03-01 T2 | COVERED | |
| RESEARCH | F-49 | `evaluate/__init__` imports no backend | 03-01 T1; 03-03 T1 | COVERED | |
| RESEARCH | Validation Architecture EVAL-05 real | 100 real ids x 3 properties, < 1 s | 03-03 T2 | COVERED | |
| CHARTER | section 13 M2 (1) | each status; free; `error` retried | 03-01 T3; 03-02 T2 | COVERED | evidence (1) |
| CHARTER | section 13 M2 (2) | cache hit leaves `evals` unchanged | 03-02 T1; 03-03 T2 | COVERED | evidence (2) |
| CHARTER | section 13 M2 (3) | batch of 100 in order | 03-01 T3; 03-03 T2 | COVERED | evidence (3) |
| CHARTER | section 13 M2 (4) | provenance on every response | 03-01 T1, T3 | COVERED | evidence (4) |
| CHARTER | section 13 M2 (5) | schema validation + first contracts in the gate | 03-01 T2; 03-03 T1 | COVERED | evidence (5) |
| CHARTER | section 7 freeze | schemas; `[tool.importlinter]` | 03-01 T1; 03-03 T1 | COVERED | |
| CHARTER | section 9 | milestone done when reproduced; VERIFICATION cites criteria | 03-03 T3 | COVERED | evidence file |
| SCOPE | — | CI green without data; split at the push boundary | 03-03 T2, T3 | COVERED | 16 skipped expected |
| DEFERRED | — | radonpy, hpc.json, second tables, populations | — | EXCLUDED | 03-CONTEXT Deferred Ideas |

<verification>
- `lint-imports` prints four KEPT lines and `Contracts: 4 kept, 0 broken.`; the two negative probes print BROKEN with the exact chains; INVARIANTS.md has A-1, A-2, A-3, A-7.
- On the pinned table: 300 ordered results for 100 entries, no `error`, cached second batch with `evals` 0, CLI exit 0.
- The seven-step gate is green locally and in a dispatched CI run on both platforms (real-file tests skipped there); `03-EVIDENCE.md` cites (1)–(5) with commands and outputs and carries the owner flag.
</verification>

<success_criteria>
ROADMAP Phase 3 success criteria 1–5 (charter section 13 M2) hold and are evidenced: each status has
a passing test with `unsupported`/`missing` free and `error` retried; a cache hit leaves `evals`
unchanged; a batch of 100 returns its results in order on the synthetic and the pinned table; every
response carries `source`, `backend`, `provenance_tier`, `n_replicates`, `spread`, `unit` and the
two-field `cost`; schema validation and the first import-linter contracts run inside the gate. The
evaluator contract, A-1 and A-2 are frozen.
</success_criteria>

<output>
Create `.planning/phases/03-evaluator-table-backend/03-03-SUMMARY.md` when done. Record the four commit
shas (plus any CI-forced fix commit), the `Contracts: 4 kept, 0 broken.` line, the measured real-file
wall time, the CI run id with both job conclusions and the CI `skipped` count, and the owner flags
surfaced — (1) REQUIREMENTS.md EVAL-06 third clause vs CONTEXT R-1 (the `loop.agents`-only llm contract
added by precedence; one block and one test to reverse), (2) the D-04 charging edge for a new property
of an already-cached candidate (Open Question 2), (3) Phase 2's evaluator-touching questions resolved
by D-03 (no population filter; the 303 twins are distinct ids), stated for the owner.
</output>
