---
phase: 03-evaluator-table-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm4pol/evaluate/__init__.py
  - src/llm4pol/evaluate/contract.py
  - src/llm4pol/evaluate/registry.py
  - src/llm4pol/evaluate/evaluator.py
  - src/llm4pol/evaluate/__main__.py
  - src/llm4pol/evaluate/backends/__init__.py
  - src/llm4pol/evaluate/backends/table.py
  - protocol/schemas/eval-request.json
  - protocol/schemas/eval-response.json
  - protocol/examples/eval-request.example.json
  - protocol/examples/eval-response.example.json
  - protocol/README.md
  - pyproject.toml
  - env/pixi.toml
  - tests/conftest.py
  - tests/test_evaluate_cli.py
  - tests/test_evaluate_contract.py
  - tests/test_evaluate_table.py
  - tests/test_check_inventory.py
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`python -m llm4pol.evaluate --request <json> --root <root>` reads a request that validates against `protocol/schemas/eval-request.json`, answers every (candidate, property) pair from the candidate parquet at `snapshot.candidates_parquet(snapshot.processed_dir(root))` through one `Backend` contract implemented by `TableBackend`, and prints a response that validates against `protocol/schemas/eval-response.json`; exit 0 on success, 2 on a request-schema failure or unreadable input (D-01, D-05, D-07, EVAL-01)."
    - "On the synthetic candidate table the example request yields, in request order, the statuses `ok, ok, ok, missing, missing, ok, missing, unsupported, ok` with `reason` `value_absent` on the first two `missing` and `candidate_unknown` on the third, `evals` charged once per distinct candidate on its first `ok` result (response `cost.evals` 3, `cpu_hours` 0.0), and `unsupported`/`missing` results carrying `cost.evals` 0 (D-02, D-04, R-2, F-40, EVAL-02)."
    - "Every result carries `backend` `table`, `source` equal to `load_registry().snapshot` (`polyomics:general_polymers@041e5834`), `provenance_tier` `md_simulated`, `unit` from the registry, `n_replicates` equal to `<column>_n`, `spread` equal to `<column>_std` when n >= 2 and `null` otherwise, `cached` false and a two-field `cost` (D-03, D-05, R-3, F-11, EVAL-03)."
    - "A 100-entry synthetic batch with three properties each returns exactly 300 results whose `(candidate_id, property)` sequence equals the flattened request, and the response `cost` is the sum of the result costs (D-01, EVAL-05)."
    - "`protocol/examples/eval-request.example.json` and `eval-response.example.json` are committed with invented fixture values only, registered in `[tool.llm4polcheck]`, and the gate's `schema-inventory` step prints `llm4polcheck inventory: 3 entries, 3 instances processed`; the committed response example equals the evaluator's own output on the synthetic table (D-01, F-32, charter section 13 M2 (5))."
    - "No population filter is applied: the physically absurd synthetic candidate (`thermal_conductivity` 15.0, `check_tc` False) still answers `ok` (D-03, F-40)."
  artifacts:
    - path: "src/llm4pol/evaluate/contract.py"
      provides: "Status, MissingReason, Cost, Lookup, Backend (Protocol), BatchEntry, EvalRequest, EvalResult, EvalResponse, RequestError, parse_request, request_validator, response_validator, REQUEST_SCHEMA_PATH, RESPONSE_SCHEMA_PATH"
      contains: "class Backend(Protocol)"
    - path: "src/llm4pol/evaluate/backends/table.py"
      provides: "TableBackend over the candidate parquet (pandas frame indexed by candidate_id, loaded once per instance, snapshot metadata asserted), SnapshotMismatch"
      contains: "class TableBackend"
    - path: "src/llm4pol/evaluate/evaluator.py"
      provides: "Evaluator.evaluate(request) -> EvalResponse with the status decision order unsupported -> candidate_unknown -> error -> value_absent -> ok and per-candidate charging"
      contains: "class Evaluator"
    - path: "src/llm4pol/evaluate/registry.py"
      provides: "PropertyTable adapter over llm4pol.data.registry.load_registry(): supported(key), spec(key), columns, snapshot"
      contains: "class PropertyTable"
    - path: "src/llm4pol/evaluate/__main__.py"
      provides: "CLI --request/--root; exit 0 / 2"
      contains: "def main("
    - path: "protocol/schemas/eval-response.json"
      provides: "Draft 2020-12 response schema with per-status conditional null-ness, reason enum, cost const 0 rules, provenance_tier enum"
      contains: "md_simulated"
    - path: "protocol/schemas/eval-request.json"
      provides: "Draft 2020-12 request schema: batch 1..1000, unique non-empty properties, candidate_id ^[0-9a-f]{16}$"
      contains: "^[0-9a-f]{16}$"
    - path: "protocol/examples/eval-response.example.json"
      provides: "The evaluator's output on the synthetic table for the example request (invented values)"
      contains: "candidate_unknown"
    - path: "tests/test_evaluate_table.py"
      provides: "Status taxonomy, provenance, no-population-filter and 100-batch order tests on the synthetic table"
      contains: "test_batch_of_100_synthetic_entries_returns_300_results_in_request_order"
    - path: "tests/test_evaluate_contract.py"
      provides: "Schema validity, example validation, negative schema cases, dataclass round trip, example byte-equality"
      contains: "test_committed_response_example_equals_evaluator_output_on_the_synthetic_root"
  key_links:
    - from: "src/llm4pol/evaluate/__main__.py"
      to: "src/llm4pol/evaluate/evaluator.py"
      via: "parse_request -> Evaluator(TableBackend(candidates_parquet, properties=PropertyTable.load()), PropertyTable).evaluate -> to_json -> stdout"
      pattern: "Evaluator("
    - from: "src/llm4pol/evaluate/backends/table.py"
      to: "src/llm4pol/data/registry.py"
      via: "units and the snapshot id come from the PropertyTable adapter over load_registry(); the parquet metadata llm4pol.snapshot must equal PropertyTable.snapshot"
      pattern: "SnapshotMismatch"
    - from: "src/llm4pol/evaluate/contract.py"
      to: "protocol/schemas/eval-request.json"
      via: "parse_request validates with Draft202012Validator before any dataclass is built"
      pattern: "Draft202012Validator"
    - from: "pyproject.toml"
      to: "protocol/examples/eval-response.example.json"
      via: "[tool.llm4polcheck] entries make the gate's schema-inventory step validate both examples"
      pattern: "eval-response.example.json"
  prohibitions:
    - statement: "No task creates `llm4pol.loop`, `llm4pol.run`, `llm4pol.llm` or `src/llm4pol/evaluate/backends/radonpy.py`; the radonpy module is named only by the Phase 3 import-linter contract (03-03) and is built at M9 (D-05, CLAUDE.md: a stub is a blocker)."
      status: unverified
      verification: null
    - statement: "`llm4pol.evaluate` never re-reads `protocol/property-registry_v1.yaml` and carries no unit or snapshot literal; every such fact comes from `load_registry()` through `PropertyTable` (CONTEXT Specific Ideas, CLAUDE.md one-place rule)."
      status: unverified
      verification: null
    - statement: "No README-triple or `check_tc` filter is applied anywhere under `llm4pol.evaluate` (D-03); `llm4pol.data.filters` is not imported by the evaluator."
      status: unverified
      verification: null
    - statement: "`protocol/examples/*.json` carry invented fixture values only: every `candidate_id` in them is a synthetic-fixture id or the unknown id `0123456789abcdef`, never a PolyOmics row (data policy, T-03-05)."
      status: unverified
      verification: null
---

<objective>
Lead Phase 3 with the tracer: one request JSON goes through `parse_request` (schema-validated),
`Evaluator.evaluate`, `TableBackend.lookup` over the candidate parquet, back out as a schema-valid
response JSON on stdout — proven end to end by `python -m llm4pol.evaluate --request` on the
synthetic 9-candidate table, producing `ok`, `missing` (both reasons) and `unsupported` in request
order with per-candidate charging (Task 1). Then expand horizontally from that proven slice: the
committed example instances registered in the gate's `[tool.llm4polcheck]` inventory, the schema
negative cases, the dataclass round trip and the protocol index (Task 2); and the full status /
provenance / no-population-filter / 100-batch order tests on the synthetic table (Task 3). Cache,
budget meter, the `error` path and the `--cache` / `--evals-limit` flags are plan 03-02; the
import-linter contracts, the real-file batch proof and the evidence are plan 03-03.

Purpose: charter section 7 freezes the evaluator request/response at M2 and section 13 M2 requires
each status tested, provenance on every response, a 100-entry batch in order and schema validation
inside the gate. Proving the architecture on one path first means a dead end costs one commit.

Output: the `llm4pol.evaluate` package (`contract`, `registry`, `evaluator`, `__main__`,
`backends/table`), both schemas, both examples, the inventory entries, the `evaluate` pixi task,
the `synthetic_candidates` and `real_candidates` fixtures, and three test modules.

Grounding facts (03-RESEARCH.md; cite, never re-derive): F-02 candidate parquet columns
`candidate_id`, then per registry column `<col>_median` (double), `<col>_n` (int64), `<col>_min`,
`<col>_max`, `<col>_std`, then `n_rows`, `canonical_psmiles`, `tacticity`; F-03 parquet metadata key
`llm4pol.snapshot` = `polyomics:general_polymers@041e5834`; F-04 `candidate_id` is exactly 16 lowercase
hex; F-05 `null_median == (n == 0)`; F-06/F-07 `_std` is NaN exactly when n <= 1; F-08 after
`to_pandas()` `_median` float64 (null -> NaN), `_n` int64 (0 when absent); F-11 registry units
(`thermal_conductivity` `W/(m*K)`, `dielectric_const_dc` `1`, `tg` `K`, ...), `Registry.snapshot` =
`polyomics:general_polymers@041e5834`; F-12 `load_registry()` / `Registry.properties[key].column|unit`;
F-13 `snapshot.candidates_parquet(snapshot.processed_dir(root))`; F-16 pandas/pyarrow/jsonschema are
`Any` under mypy strict — convert with `float()` / `int()` at the boundary; F-24..F-31 the verified
schema shapes (Code Example 1); F-32 inventory entry shape; F-37/F-38 the `mypy --strict` contract
skeleton (Code Example 2) and the `Status` narrowing via `frozenset(get_args(Status))` + `cast`;
F-39 `load.load(synthetic_root)` yields a 9-candidate parquet with the writer's 49-column shape and
metadata; F-40 synthetic status cases: `7ec8cb49ff317efc` (`*CC*`/none) `thermal_conductivity`
median 0.32, n 3, std 0.02 -> `ok` with spread; `b3a635a55e1a6645` (`*CC(*)c1ccccc1`/atactic) n 2 ->
`ok` spread 0.007071; `81b997b85ccd2069` (`*CO*`/none) TC NaN, n 0 -> `missing/value_absent`;
`d24805b4ce4c381c` (`*CC(*)F`/none) `tg` NaN -> `missing/value_absent`, TC n 1 -> `ok` with `spread`
null; `182075ab56be81bf` TC 15.0 / `tg` 1.0e6, `check_tc` False -> still `ok`; any well-formed id not
in the table -> `missing/candidate_unknown`; an unregistered key -> `unsupported`; F-45 the
`llm4pol.data.__main__` CLI pattern (argparse, stdout reconfigure, `ERROR:` to stderr, in-process
`main([...])` tests); F-42 baseline 116 tests collected. R-5 (locked): `pyarrow.parquet.read_table`
-> `to_pandas()` -> `set_index("candidate_id")`, loaded once per `TableBackend` instance. In this
plan "the barred key" means the uncorrected static permittivity column A-7 keeps out of the registry;
test code builds its name from fragments (`"static_" + "dielectric_const"`) and no source or example
file under `src/llm4pol/evaluate/` or `protocol/examples/` contains it. Workstation guard (Phase 1): a
hook refuses Bash commands containing git's `-n` short flag — use `--max-count`; write commit messages
to a scratchpad file and commit with `-F`. Ad-hoc commands outside the `check` tasks need
`PYTHONPATH=src` (Pitfall 7; verified: `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m
pytest ...` runs from the repository root).
</objective>

<execution_context>
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-evaluator-table-backend/03-CONTEXT.md
@.planning/phases/03-evaluator-table-backend/03-RESEARCH.md
@.planning/phases/02-data-foundation/02-05-SUMMARY.md
@CLAUDE.md
@docs/MASTER-PLAN.md
@docs/governance/INVARIANTS.md
@src/llm4pol/data/registry.py
@src/llm4pol/data/snapshot.py
@src/llm4pol/data/load.py
@src/llm4pol/data/schema.py
@src/llm4pol/data/identity.py
@src/llm4pol/data/__main__.py
@scripts/check.py
@pyproject.toml
@env/pixi.toml
@protocol/README.md
@protocol/schemas/property-registry.json
@tests/conftest.py
@tests/test_data_cli.py
@tests/test_check_inventory.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "request JSON -> schema -> Evaluator -> TableBackend -> response JSON" — one path only (D-01, D-02, D-03, D-04, D-05, D-07, R-2, R-3, R-5, EVAL-01, EVAL-02, EVAL-03)</name>
  <read_first>
    - `.planning/phases/03-evaluator-table-backend/03-CONTEXT.md` — D-01 (shapes), D-02 (status order; `reason` enum), D-03 (value semantics; no population filter), D-04 (per-candidate charging; `cpu_hours` 0.0), D-05 (contract names; `Backend` Protocol; `Lookup`), D-07 (CLI; exit 0/2), R-2, R-3 (`provenance_tier` enum; malformed id is a schema failure), R-5 (pandas frame indexed by `candidate_id`)
    - `.planning/phases/03-evaluator-table-backend/03-RESEARCH.md` — Architecture Patterns 1, 2, 3, 4 (the two-phase loop without the cache/meter steps, which 03-02 adds), Code Example 1 (both schemas, exact JSON), Code Example 2 (`mypy --strict` skeleton), Code Example 5 (CLI exit codes), Pitfalls 2, 6, 7, 8, F-02..F-13, F-16, F-24..F-31, F-37..F-40, F-45, F-49
    - `src/llm4pol/data/registry.py` (`load_registry`, `Registry.properties`, `PropertySpec.column/unit`, `Registry.snapshot`), `snapshot.py` (`candidates_parquet`, `processed_dir`, `DEFAULT_ROOT`), `load.py` (`load`, `build_candidates` column naming), `identity.py` (`candidate_id(canonical, tacticity)`), `schema.py` (`PROPERTY_COLUMNS`)
    - `src/llm4pol/data/__main__.py` and `tests/test_data_cli.py` — the argparse / stdout-reconfigure / `ERROR:` / in-process `main([...])` pattern this CLI copies
    - `tests/conftest.py` — `SYNTHETIC_ROWS`, `synthetic_root`, `real_csv` (skip wording), `REPO_ROOT`; `pyproject.toml` `[tool.mypy]` overrides (F-16) and `[tool.llm4polcheck]`; `env/pixi.toml` `check` task (cwd `..`, `PYTHONPATH=src`)
    - `docs/MASTER-PLAN.md` section 7 (the evaluator contract JSON) and section 13 M2 row
  </read_first>
  <behavior>
    - Test 1 `tests/test_evaluate_cli.py::test_tracer_request_json_to_response_json_on_the_synthetic_root`: with the `synthetic_candidates` fixture (a fake repository root whose `data/processed/candidates-041e5834.parquet` holds the 9 synthetic candidates), write the example request (the five-entry batch of STEP 5 below) to `tmp_path / "request.json"`; `cli.main(["--request", str(path), "--root", str(root)])` returns 0; stdout parses as JSON and validates against `protocol/schemas/eval-response.json` with `Draft202012Validator`; `run_id == "example-run"`, `iteration == 0`; `len(results) == 9`; `[r["status"] for r in results] == ["ok", "ok", "ok", "missing", "missing", "ok", "missing", "unsupported", "ok"]`; `[(r["candidate_id"], r["property"]) for r in results]` equals the flattened batch; the `reason` values of the three `missing` results are `value_absent`, `value_absent`, `candidate_unknown` and no `ok`/`unsupported` result has a `reason` key; `[r["cost"]["evals"] for r in results] == [1, 0, 0, 0, 0, 1, 0, 0, 1]`; `payload["cost"] == {"evals": 3, "cpu_hours": 0.0}`; every result has `backend == "table"`, `source == "polyomics:general_polymers@041e5834"`, `provenance_tier == "md_simulated"`, `cached is False`; the first result has `value == 0.32` (within 1e-9), `unit == "W/(m*K)"`, `n_replicates == 3`, `spread` within 1e-6 of 0.02 (F-40); the sixth result (`d24805b4ce4c381c` / `thermal_conductivity`) has `n_replicates == 1` and `spread is None`; the ninth (`b3a635a55e1a6645`) has `n_replicates == 2` and `spread` within 1e-6 of 0.007071.
    - Test 2 `tests/test_evaluate_cli.py::test_cli_exit_2_on_a_request_schema_failure` (parametrised over two requests): (a) a batch entry with `candidate_id` `"ZZZZ"` (R-3: a malformed id is a caller bug, exit 2, never `missing`); (b) a batch entry whose `properties` list `thermal_conductivity` twice (F-29: `uniqueItems`). Each returns 2, prints nothing on stdout and stderr starts with `ERROR:`.
  </behavior>
  <files>src/llm4pol/evaluate/__init__.py, src/llm4pol/evaluate/contract.py, src/llm4pol/evaluate/registry.py, src/llm4pol/evaluate/evaluator.py, src/llm4pol/evaluate/__main__.py, src/llm4pol/evaluate/backends/__init__.py, src/llm4pol/evaluate/backends/table.py, protocol/schemas/eval-request.json, protocol/schemas/eval-response.json, env/pixi.toml, tests/conftest.py, tests/test_evaluate_cli.py</files>
  <action>
    STEP 1 — RED. Add to `tests/conftest.py`: (a) a module-level function `make_synthetic_root(root: Path) -> Path` holding the body of today's `synthetic_root` fixture (CSV, README, manifest, `data/processed`, `docs/audit`), and make the fixture call it — no behaviour change for Phase 2 tests; (b) a fixture `synthetic_candidates(synthetic_root) -> Path` that runs `load.load(synthetic_root)` (F-39: 9 candidates, writer's exact 49-column shape, `llm4pol.snapshot` metadata) and returns `synthetic_root`; (c) a session fixture `real_candidates() -> Path` returning `snapshot.candidates_parquet(snapshot.processed_dir(REPO_ROOT))` and calling `pytest.skip(f"processed candidate parquet absent at {path} (never committed; run python -m llm4pol.data fetch, then load)")` when the file is absent — read-only, it never runs `load` (F-41, 0.05 s). Also add a module constant `SYNTHETIC_EXAMPLE_REQUEST` (the exact five-entry request of STEP 5) so the CLI test, the contract tests and the one-off example generator share one definition. Write Tests 1–2 in a new `tests/test_evaluate_cli.py`; run `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_cli.py -x -q` -> RED (a collection error on the missing package is the RED evidence, as plan 02-01 recorded); commit `test(03-01): add the failing evaluator tracer test on the synthetic candidate table (D-01, D-02, D-07)`.

    STEP 2 — schemas (D-01, R-3; Code Example 1 verbatim, F-24..F-31). Create `protocol/schemas/eval-request.json` and `protocol/schemas/eval-response.json` exactly as Code Example 1 of 03-RESEARCH.md gives them (`$schema` Draft 2020-12, `$id` under `https://github.com/kn1218/llm4pol/protocol/schemas/`, `additionalProperties: false` everywhere, request `batch` `minItems 1` / `maxItems 1000`, `properties` `minItems 1` / `uniqueItems true`, `candidate_id` `pattern ^[0-9a-f]{16}$`, `run_id` `minLength 1`, `iteration` integer `minimum 0`; response `$defs/cost` with exactly `evals` (integer >= 0) and `cpu_hours` (number >= 0), `$defs/result` with the twelve required keys, `provenance_tier` `enum ["md_simulated"]`, optional `reason`, and the five `allOf` `if/then/else` blocks: `ok` forces number/string/integer and forbids `reason`, non-ok forces null on `value`/`unit`/`n_replicates`/`spread`, `missing` requires `reason` from `["value_absent", "candidate_unknown"]`, `error` requires `reason`, non-ok and `cached == true` pin `cost.evals` to `const 0`). Give each schema a one-line `title` naming charter section 7 and the M2 freeze; note in `description` that a later tier value is added by a new schema version (A-5), never by editing this file once results cite it.

    STEP 3 — the package. `src/llm4pol/evaluate/__init__.py`: module docstring (the evaluator contract, charter section 7; package boundary charter section 6 — never imports `loop`, `llm`, `run`) and `__all__` re-exporting the contract names and `Evaluator`/`PropertyTable`; it imports nothing from `backends` (F-49). `backends/__init__.py`: docstring only. `contract.py` (Code Example 2 as the skeleton; every class a frozen dataclass with `slots=True`; pydantic-free): `Status` and `STATUSES`, `MissingReason` Literal, `parse_status`, `Cost(evals: int, cpu_hours: float)` with `to_json()`, `Lookup(status, value, unit, n_replicates, spread, reason)`, the `@runtime_checkable` `Backend` Protocol (`name`, `source`, `provenance_tier` properties; `lookup(candidate_id, property_key) -> Lookup`), `BatchEntry(candidate_id, properties: tuple[str, ...])`, `EvalRequest(run_id, iteration, batch: tuple[BatchEntry, ...])` with `to_json()`, `EvalResult` with the twelve contract fields plus `reason: str | None = None`, `to_json()` (drops `reason` when None; `cost` nested), `from_json(obj)` (type-checks every field, `parse_status` on `status`, raises `ValueError` on a wrong type — the cache replay of 03-02 relies on it), and a classmethod `from_lookup(candidate_id, property_key, lookup, backend, *, cost) -> EvalResult` (`cached=False`; `backend`/`source`/`provenance_tier` from the backend); `EvalResponse(run_id, iteration, results: tuple[EvalResult, ...], cost: Cost)` with `to_json()` / `from_json()`; `RequestError(ValueError)`; `SCHEMA_DIR = DEFAULT_ROOT / "protocol" / "schemas"`, `REQUEST_SCHEMA_PATH`, `RESPONSE_SCHEMA_PATH`, `load_schema(path) -> dict`, `request_validator()` / `response_validator()` (built once with `functools.cache`, `Draft202012Validator`); `parse_request(payload: object) -> EvalRequest` validates first and raises `RequestError` carrying the JSON pointer of `jsonschema.exceptions.best_match(...)` and its message, then builds the tuples. `registry.py`: frozen `PropertyTable(registry: Registry)` with classmethod `load(path=REGISTRY_PATH, *, schema_path=SCHEMA_PATH)` calling `load_registry` (the only call site of the registry loader under `evaluate`), `snapshot` property, `supported(key) -> bool` (`key in registry.properties`), `spec(key) -> PropertySpec`, `columns()`; no key list is duplicated here. `backends/table.py` (Pattern 3 with R-5): `SnapshotMismatch(RuntimeError)`; `TableBackend(path: Path, *, properties: PropertyTable)` with class attributes `name = "table"`, `provenance_tier = "md_simulated"`, instance attribute `source = properties.snapshot`; a private `_frame()` that on first call runs `pyarrow.parquet.read_table(path)`, asserts the metadata `llm4pol.snapshot` equals `self.source` (else raise `SnapshotMismatch` naming both), converts with `to_pandas()` and `set_index("candidate_id")`, and keeps the frame on the instance (a failed load keeps nothing, so the next call retries — the `error` retry of 03-02 depends on this); `lookup(candidate_id, property_key) -> Lookup`: unknown index label -> `Lookup("missing", reason="candidate_unknown")`; `spec = properties.spec(property_key)`; median via `frame.at[candidate_id, spec.column + "_median"]`, NaN (`math.isnan`) -> `Lookup("missing", reason="value_absent")`; else `n = int(frame.at[..., "_n"])`, `std` from `"_std"`, `spread = None if n < 2 or isnan(std) else float(std)`, `Lookup("ok", value=float(median), unit=spec.unit, n_replicates=n, spread=spread)`. No filter of any kind is applied (D-03). `evaluator.py`: `Evaluator(backend: Backend, properties: PropertyTable)` with `evaluate(request: EvalRequest) -> EvalResponse` — nested loops `for entry in request.batch: for key in entry.properties` (this order is the contract's ordering and count guarantee); `_lookup(candidate_id, key)`: `unsupported` when `not properties.supported(key)` (before any backend call — Pattern 1; the barred key and any unregistered key land here even on an unknown candidate), otherwise `backend.lookup(...)` inside `try/except Exception as exc` -> `Lookup("error", reason=type(exc).__name__)`; charging (D-04, R-2): a `charged: set[str]` per request; `evals = 1` when the lookup is `ok` and the candidate is not yet in `charged`, else 0; `cpu_hours` always `0.0`; results built with `EvalResult.from_lookup(...)`; response `cost = Cost(sum(r.cost.evals), sum(r.cost.cpu_hours))`. Write the docstring sentence that `evals` counts distinct candidates per request on their first `ok` result (the loop's 400-eval arithmetic counts candidates). `__main__.py` (F-45, Code Example 5): argparse `--request PATH` (required), `--root PATH` (default `DEFAULT_ROOT`); stdout/stderr reconfigured to UTF-8 with replacement; read + `json.loads` + `parse_request` inside one `try` catching `(OSError, json.JSONDecodeError, RequestError)` -> `print(f"ERROR: {exc}", file=sys.stderr)`, return 2; `RegistryError` -> 2 as well; build `TableBackend(snapshot.candidates_parquet(snapshot.processed_dir(root)), properties=PropertyTable.load())`, evaluate, `print(json.dumps(response.to_json(), indent=2, sort_keys=True, allow_nan=False))`, return 0; `if __name__ == "__main__": sys.exit(main())`. A missing parquet is not exit 2: every result is `error` and the exit code is 0 (D-02, Code Example 5 note; pinned by 03-02).

    STEP 4 — pixi task. In `env/pixi.toml` `[tasks]` add `evaluate = { cmd = "python -m llm4pol.evaluate", cwd = "..", env = { PYTHONPATH = "src" } }` with a comment that it mirrors `check` (cwd is the repository root; extra arguments pass through). No dependency changes; the lock file is untouched.

    STEP 5 — the example request (shared by the tracer test, Task 2's example file and the one-off example generator): `run_id` `"example-run"`, `iteration` 0, batch in this order: `7ec8cb49ff317efc` -> `["thermal_conductivity", "dielectric_const_dc", "tg"]`; `81b997b85ccd2069` -> `["thermal_conductivity"]`; `d24805b4ce4c381c` -> `["tg", "thermal_conductivity"]`; `0123456789abcdef` -> `["thermal_conductivity"]`; `b3a635a55e1a6645` -> `["melting_point", "thermal_conductivity"]`. `melting_point` is the unregistered key used for `unsupported` throughout this phase (the barred key is exercised in Task 3 from fragments only). The test asserts once that `identity.candidate_id("*CC*", "none") == "7ec8cb49ff317efc"` so the F-40 ids are pinned to the identity function rather than copied blindly, and that `0123456789abcdef` is absent from the fixture's candidate frame.

    STEP 6 — GREEN. `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_cli.py -x -q` passes; `pixi run --manifest-path env/pixi.toml evaluate --help` prints usage; run the gate (`check`) -> `SUMMARY: 7/7 steps passed` (mypy strict on the new package: convert pandas scalars with `float()`/`int()`, narrow `Status` with `cast` — Pitfall 8, F-38). Commit `feat(03-01): evaluator tracer — contract dataclasses, eval-request/eval-response schemas, PropertyTable, TableBackend over the candidate parquet, Evaluator and the CLI (D-01, D-02, D-03, D-04, D-05, D-07, R-2, R-3, R-5)`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_cli.py -v</automated>
    <fails_when>exit code is non-zero, or `test_tracer_request_json_to_response_json_on_the_synthetic_root` or `test_cli_exit_2_on_a_request_schema_failure` is not listed as PASSED</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml evaluate --help</automated>
    <fails_when>exit code is non-zero or the output lacks `--request` and `--root`</fails_when>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -c "import json, jsonschema; [jsonschema.Draft202012Validator.check_schema(json.load(open(p, encoding='utf-8'))) for p in ('protocol/schemas/eval-request.json', 'protocol/schemas/eval-response.json')]; print('schemas ok')"</automated>
    <fails_when>the output is not `schemas ok` — a schema is not a valid Draft 2020-12 document (F-24)</fails_when>
    <automated>grep -cE "^(from|import) .*backends" src/llm4pol/evaluate/__init__.py</automated>
    <fails_when>prints anything other than 0 — an import line in the package `__init__` names the backends package (F-49); only import statements are counted, so a docstring cannot trip this</fails_when>
    <automated>grep -rnE "^(from|import) llm4pol\.data\.filters|^from llm4pol\.data import .*filters" src/llm4pol/evaluate/ | wc -l</automated>
    <fails_when>prints anything other than 0 — the population-filter module is imported by the evaluator (D-03)</fails_when>
    <automated>grep -rnE "= *[\"']polyomics:|= *[\"']W/\(m" src/llm4pol/evaluate/ | wc -l</automated>
    <fails_when>prints anything other than 0 — a snapshot or unit literal is assigned in the evaluator instead of read through PropertyTable (CLAUDE.md one-place rule)</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits in order: `test(03-01): add the failing evaluator tracer test on the synthetic candidate table (D-01, D-02, D-07)` then `feat(03-01): evaluator tracer — contract dataclasses, eval-request/eval-response schemas, PropertyTable, TableBackend over the candidate parquet, Evaluator and the CLI (D-01, D-02, D-03, D-04, D-05, D-07, R-2, R-3, R-5)`.
    - `python -m llm4pol.evaluate --request <example request> --root <synthetic root>` exits 0 and prints nine results whose statuses are exactly `ok, ok, ok, missing, missing, ok, missing, unsupported, ok`, per-result `evals` `1, 0, 0, 0, 0, 1, 0, 0, 1`, response `cost` `{"evals": 3, "cpu_hours": 0.0}`, and the payload validates against `protocol/schemas/eval-response.json` (F-40, D-01, D-02, D-04).
    - A request with `candidate_id` `ZZZZ` exits 2 with `ERROR:` on stderr (R-3); a request whose `properties` repeat a key exits 2 (F-29).
    - `src/llm4pol/evaluate/` contains exactly `__init__.py`, `contract.py`, `registry.py`, `evaluator.py`, `__main__.py`, `backends/__init__.py`, `backends/table.py` — no `cache.py`/`budget.py` yet (03-02) and no `backends/radonpy.py` ever in this phase (D-05).
    - `TableBackend.source == PropertyTable.load().snapshot == snapshot.SNAPSHOT_ID` and the three greps in the verify block (backend import in `__init__`, filters import, snapshot/unit literal assignment) each print 0.
    - The gate prints `SUMMARY: 7/7 steps passed` with the test count at or above 118 collected (F-42 baseline 116 + these two).
  </acceptance_criteria>
  <done>The single happy path works end to end and is committed: a schema-validated request reaches the candidate parquet through the `Backend` contract and returns a schema-valid response with `ok`, both `missing` reasons and `unsupported` in request order, charged per candidate, with provenance on every result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Committed examples in the gate's inventory, schema negative cases, dataclass round trip, protocol index (D-01, R-3, EVAL-01, charter section 13 M2 (5))</name>
  <read_first>
    - `pyproject.toml` `[tool.llm4polcheck]` (entry shape F-32) and `scripts/check.py::validate_inventory` (prints `llm4polcheck inventory: N entries, M instances processed`)
    - `tests/test_check_inventory.py::test_repository_inventory_validates` (asserts the `1 entries, 1 instances` line today — this task updates it)
    - `.planning/phases/03-evaluator-table-backend/03-RESEARCH.md` — F-24..F-31 (each negative case with the exact validator message), Code Example 1 notes (a)–(c), Pitfall 9, F-44
    - `protocol/README.md` (the `## Current contents` section that still says `Empty`; the subdirectory table)
    - `tests/conftest.py` (`SYNTHETIC_EXAMPLE_REQUEST`, `make_synthetic_root`, `synthetic_candidates` from Task 1) and `src/llm4pol/evaluate/contract.py` (`parse_request`, `EvalResponse.from_json`)
    - `docs/MASTER-PLAN.md` section 4 A-5 (a schema that produced reported results is versioned, not edited in place)
  </read_first>
  <behavior>
    - Test 1 `tests/test_evaluate_contract.py::test_both_schemas_are_valid_draft_2020_12`: `Draft202012Validator.check_schema` passes on both files; the request schema's `$schema` is the 2020-12 URI; the response schema's `$defs/cost/required == ["evals", "cpu_hours"]` and `$defs/cost/additionalProperties is False`; `$defs/result/properties/provenance_tier/enum == ["md_simulated"]` (R-3).
    - Test 2 `test_committed_examples_validate_against_their_schemas`: `protocol/examples/eval-request.example.json` validates against the request schema and `eval-response.example.json` against the response schema; every `candidate_id` in both files is one of the synthetic fixture ids or `0123456789abcdef` (data policy: invented values only).
    - Test 3 `test_committed_response_example_equals_evaluator_output_on_the_synthetic_root` (uses `synthetic_candidates`): `Evaluator(TableBackend(...), PropertyTable.load()).evaluate(parse_request(SYNTHETIC_EXAMPLE_REQUEST)).to_json()` equals the parsed committed example, with floats compared by `math.isclose(rel_tol=1e-9, abs_tol=0.0)` and everything else by equality (a three-value `std` may differ in the last ulp across platforms); and `json.dumps(parsed, indent=2, sort_keys=True) + "\n"` equals the committed file text exactly (canonical formatting, LF).
    - Test 4 `test_response_schema_rejects_each_status_shape_violation` (parametrised, F-24..F-28): starting from a valid `ok` result, each mutation is INVALID with the quoted message fragment — `ok` with `value: null` (`None is not of type 'number'`), `missing` with `value: 0.3` (`0.3 is not of type 'null'`), `ok` with `reason` present, `missing` without `reason` (`'reason' is a required property`), `missing` with `reason: "nope"` (`is not one of`), `error` without `reason`, `missing` with `cost.evals: 1` (`0 was expected`), `cached: true` with `cost.evals: 1`, `provenance_tier: "experimental"` (`is not one of`), a result with an extra key; and `ok` with `n_replicates: 1`, `spread: null` is VALID (F-27).
    - Test 5 `test_request_schema_rejects_dup_props_empty_batch_1001_batch_bool_iteration_and_malformed_id` (F-29, F-30, R-3): duplicate properties (`has non-unique elements`), empty batch, 1001-entry batch, `iteration: true`, `candidate_id: "ZZZZ"`, `run_id: ""` are each INVALID; `parse_request` raises `RequestError` whose message names the JSON path (e.g. `batch/0/properties`).
    - Test 6 `test_dataclasses_round_trip_through_json`: `EvalResponse.from_json(response.to_json()) == response` for the synthetic example response; `EvalResult.from_json` raises `ValueError` on a wrong-typed field (`"cached": "yes"`) and on an unknown status; `to_json()` of an `ok` result has no `reason` key; `Cost(1, 0.0).to_json() == {"evals": 1, "cpu_hours": 0.0}`; the dataclasses are frozen (`dataclasses.FrozenInstanceError` on assignment).
    - Test 7 — extend `tests/test_check_inventory.py::test_repository_inventory_validates`: the live inventory prints `llm4polcheck inventory: 3 entries, 3 instances processed` and returns no failures.
  </behavior>
  <files>protocol/examples/eval-request.example.json, protocol/examples/eval-response.example.json, protocol/README.md, pyproject.toml, tests/test_evaluate_contract.py, tests/test_check_inventory.py</files>
  <action>
    STEP 1 — RED: write Tests 1–6 in a new `tests/test_evaluate_contract.py` and update Test 7; run both files -> RED (the examples do not exist; the inventory still prints `1 entries`); commit `test(03-01): add failing schema, example and round-trip tests for the evaluator contract (D-01, R-3, EVAL-01)`.

    STEP 2 — examples. Create `protocol/examples/eval-request.example.json` as the pretty-printed (`indent=2`, `sort_keys=True`, trailing LF) form of `SYNTHETIC_EXAMPLE_REQUEST`. Generate `protocol/examples/eval-response.example.json` with a one-off Python snippet run from the repository root with `PYTHONPATH=src` (not committed): insert `tests` into `sys.path`, `from conftest import SYNTHETIC_EXAMPLE_REQUEST, make_synthetic_root`, build the synthetic root under a temporary directory, `load.load(root)`, evaluate the parsed example request with `Evaluator(TableBackend(snapshot.candidates_parquet(snapshot.processed_dir(root)), properties=PropertyTable.load()), PropertyTable.load())`, and write `json.dumps(response.to_json(), indent=2, sort_keys=True) + "\n"` with `encoding="utf-8", newline="\n"`. Check the file by eye against the Task 1 expectations (nine results; statuses `ok, ok, ok, missing, missing, ok, missing, unsupported, ok`; `cost.evals` 3) and confirm no value in it is a PolyOmics number (all values come from `conftest.SYNTHETIC_ROWS`, F-40).

    STEP 3 — inventory. Append to `[tool.llm4polcheck].validated` the two entries of Code Example 1's inventory block: `{ name = "evaluator request example (EVAL-01)", instances = "protocol/examples/eval-request.example.json", schema = "protocol/schemas/eval-request.json", required = true }` and the response counterpart; extend the comment above the table with one line saying `protocol/examples/` holds the committed instances of the evaluator schemas (charter section 13 M2 (5)).

    STEP 4 — protocol index (Pitfall 9, F-44). In `protocol/README.md` add an `examples/` row to the subdirectory table (committed example instances of each schema, validated by the `schema-inventory` gate step through `[tool.llm4polcheck]`; every value invented, never a data row) and replace the `## Current contents` paragraph with a short list: `property-registry_v1.yaml`, `property-registry_v1.provenance.yaml`, `schemas/property-registry.json` (Phase 2, M1 freeze); `schemas/eval-request.json`, `schemas/eval-response.json`, `examples/eval-request.example.json`, `examples/eval-response.example.json` (Phase 3, M2 freeze — charter section 7); `prompts/` is populated at M5 (Phase 6). Add the A-5 sentence: a schema or prompt that produced a reported result is never edited in place — a new version is a new file.

    STEP 5 — GREEN: `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_contract.py tests/test_check_inventory.py -x -q` passes; the gate prints `llm4polcheck inventory: 3 entries, 3 instances processed` and `SUMMARY: 7/7 steps passed`; commit `feat(03-01): commit the evaluator request/response examples, register them in the schema inventory and index protocol/ (D-01, EVAL-01, charter section 13 M2 (5))`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_contract.py tests/test_check_inventory.py -v</automated>
    <fails_when>exit code is non-zero, or any of `test_both_schemas_are_valid_draft_2020_12`, `test_committed_examples_validate_against_their_schemas`, `test_committed_response_example_equals_evaluator_output_on_the_synthetic_root`, `test_response_schema_rejects_each_status_shape_violation`, `test_request_schema_rejects_dup_props_empty_batch_1001_batch_bool_iteration_and_malformed_id`, `test_dataclasses_round_trip_through_json`, `test_repository_inventory_validates` is not listed as PASSED</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check | grep -c "llm4polcheck inventory: 3 entries, 3 instances processed"</automated>
    <fails_when>prints anything other than 1 — the gate's schema-inventory step does not process both examples (F-32)</fails_when>
    <automated>grep -c "candidate_unknown" protocol/examples/eval-response.example.json</automated>
    <fails_when>prints 0 — the committed response example lacks the unknown-candidate case</fails_when>
    <automated>grep -c "Empty" protocol/README.md</automated>
    <fails_when>prints anything other than 0 — the protocol index still claims to be empty (Pitfall 9)</fails_when>
    <automated>grep -rc "static_" protocol/examples/ | grep -v ":0$" | wc -l</automated>
    <fails_when>prints anything other than 0 — the barred column's name reached a committed example (A-7)</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits in order: `test(03-01): add failing schema, example and round-trip tests for the evaluator contract (D-01, R-3, EVAL-01)` then `feat(03-01): commit the evaluator request/response examples, register them in the schema inventory and index protocol/ (D-01, EVAL-01, charter section 13 M2 (5))`.
    - `[tool.llm4polcheck].validated` has exactly three entries and the gate prints `llm4polcheck inventory: 3 entries, 3 instances processed` (F-32).
    - `protocol/examples/eval-response.example.json` has nine results, response `cost.evals` 3, and equals the evaluator's output on the synthetic table under the tolerance rule of Test 3; every `candidate_id` in both example files is a synthetic id or `0123456789abcdef`.
    - The ten negative schema cases of Test 4 and the six of Test 5 are each rejected with the quoted validator message fragments (F-24..F-30); the `ok / n_replicates 1 / spread null` case is accepted (F-27).
    - `protocol/README.md` lists both schemas and both examples and carries the A-5 sentence; the gate prints `SUMMARY: 7/7 steps passed`.
  </acceptance_criteria>
  <done>Charter section 13 M2 (5) is reproducible by command: both schemas are valid 2020-12 documents, their committed examples are validated by the gate's inventory step, the evaluator's own output is what the response example shows, and the protocol index says what the directory holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Status taxonomy, provenance, no-population-filter and 100-batch order on the synthetic table (D-02, D-03, D-04, D-05, R-2, EVAL-02, EVAL-03, EVAL-05, charter section 13 M2 (1), (3), (4))</name>
  <read_first>
    - `src/llm4pol/evaluate/evaluator.py`, `backends/table.py`, `contract.py`, `registry.py` (Task 1)
    - `.planning/phases/03-evaluator-table-backend/03-RESEARCH.md` — F-05..F-08, F-11, F-40 (each synthetic case with its numbers), Pattern 1 (decision order), Pitfall 6 (`unsupported` before `missing`), Validation Architecture rows EVAL-02, EVAL-03, EVAL-05
    - `tests/conftest.py` — `SYNTHETIC_ROWS` (the 14 rows; tacticity `None` normalises to `unknown`), `synthetic_candidates`; `src/llm4pol/data/identity.py::candidate_id`
    - `docs/governance/INVARIANTS.md` — the A-7 row (the barred column is assembled from fragments in test bodies; the literal is permitted on `def test_` lines only — Phase 2 W-1 rule)
  </read_first>
  <behavior>
    - Test 1 `tests/test_evaluate_table.py::test_ok_with_spread_when_n_ge_2`: `7ec8cb49ff317efc` / `thermal_conductivity` -> `ok`, `value` 0.32, `n_replicates` 3, `spread` within 1e-6 of 0.02, `unit` `W/(m*K)`; `b3a635a55e1a6645` -> n 2, spread within 1e-6 of 0.007071; the value and spread equal `frame.loc[id, "thermal_conductivity_median"]` / `_std` read from the fixture parquet (the same writer, no re-derived number).
    - Test 2 `test_ok_with_null_spread_when_n_is_1`: `d24805b4ce4c381c` / `thermal_conductivity` -> `ok`, `n_replicates` 1, `spread is None`, `value` 0.22 (F-06/F-07).
    - Test 3 `test_missing_value_absent_when_median_is_null`: `81b997b85ccd2069` / `thermal_conductivity` -> `missing`, `reason` `value_absent`, `value`/`unit`/`n_replicates`/`spread` all None, `cost.evals` 0; `d24805b4ce4c381c` / `tg` likewise.
    - Test 4 `test_missing_candidate_unknown_for_a_well_formed_absent_id`: `0123456789abcdef` / `tg` -> `missing`, `reason` `candidate_unknown`, `cost.evals` 0.
    - Test 5 `test_unsupported_for_an_unregistered_key_and_the_barred_key_even_on_an_unknown_candidate`: `melting_point` on a known candidate and the barred key (built from fragments) on `0123456789abcdef` both return `unsupported` with no `reason`, `cost.evals` 0 — proving the decision order of Pattern 1 (Pitfall 6): the backend is not consulted (use a counting wrapper around `TableBackend.lookup` or a fake `Backend` whose `lookup` raises, and assert it is never called for these keys).
    - Test 6 `test_unsupported_and_missing_consume_no_evals`: a request with only `unsupported` and `missing` results has response `cost == Cost(0, 0.0)`.
    - Test 7 `test_every_result_carries_backend_source_and_provenance_tier`: for a request covering all four synthetic outcomes, every result has `backend == "table"`, `source == PropertyTable.load().snapshot`, `provenance_tier == "md_simulated"`, `cached is False`, and `cost` with exactly the two fields (`dataclasses.fields(Cost)` names `evals`, `cpu_hours`); `isinstance(TableBackend(...), Backend)` is True (F-37).
    - Test 8 `test_unit_n_replicates_and_spread_come_from_the_registry_and_the_candidate_table`: for every synthetic candidate and every registry key, an `ok` result's `unit == load_registry().properties[key].unit`, `n_replicates == frame.loc[id, column + "_n"]`, and `spread` equals `column + "_std"` when `n >= 2` else None; a `missing/value_absent` result coincides exactly with `frame.loc[id, column + "_n"] == 0` (F-05).
    - Test 9 `test_no_population_filter_is_applied`: `182075ab56be81bf` (`thermal_conductivity` 15.0, `tg` 1.0e6, `check_tc` False) answers `ok` for both keys (D-03).
    - Test 10 `test_batch_of_100_synthetic_entries_returns_300_results_in_request_order`: 100 entries cycling the nine synthetic ids plus `0123456789abcdef`, each with `["thermal_conductivity", "dielectric_const_dc", "tg"]` -> `len(results) == 300`; `[(r.candidate_id, r.property) for r in results] == [(e.candidate_id, p) for e in batch for p in e.properties]`; every status in `{"ok", "missing"}`; `response.cost.evals == 9` (each of the nine synthetic candidates has at least one non-null median among the three; a duplicate of a candidate inside the same request is charged 0 — D-04 per distinct candidate per request); the response validates against the schema.
    - Test 11 `test_response_cost_is_the_sum_of_result_costs`: `response.cost.evals == sum(r.cost.evals)` and `response.cost.cpu_hours == sum(r.cost.cpu_hours) == 0.0` on the 100-batch.
  </behavior>
  <files>tests/test_evaluate_table.py</files>
  <action>
    STEP 1 — RED: write Tests 1–11 in a new `tests/test_evaluate_table.py` using the `synthetic_candidates` fixture, a helper `_evaluator(root)` building `TableBackend(snapshot.candidates_parquet(snapshot.processed_dir(root)), properties=PropertyTable.load())` and `Evaluator`, and a helper `_request(pairs)` building `EvalRequest` via `parse_request` on a dict; read the fixture parquet once per test with `pd.read_parquet(...).set_index("candidate_id")` for the cross-checks. Run `PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_table.py -x -q`. Expect most tests GREEN immediately (the tracer already implements the taxonomy) and any RED to reveal a Task 1 defect, which is fixed in `evaluator.py` / `backends/table.py` in the same task — never by weakening the test (the F-40 values are the fixture's arithmetic). Commit `test(03-01): pin the status taxonomy, provenance, no-population-filter and 100-batch order on the synthetic table (D-02, D-03, D-04, EVAL-02, EVAL-03, EVAL-05)`; if a source fix was needed, commit it first as `fix(03-01): <what the test revealed>`.

    STEP 2 — the gate: `pixi run --manifest-path env/pixi.toml check` -> `SUMMARY: 7/7 steps passed`.
  </action>
  <verify>
    <automated>PYTHONPATH=src pixi run --manifest-path env/pixi.toml python -m pytest tests/test_evaluate_table.py -v</automated>
    <fails_when>exit code is non-zero, or fewer than 11 tests are listed as PASSED, or any of `test_unsupported_for_an_unregistered_key_and_the_barred_key_even_on_an_unknown_candidate`, `test_every_result_carries_backend_source_and_provenance_tier`, `test_no_population_filter_is_applied`, `test_batch_of_100_synthetic_entries_returns_300_results_in_request_order` is absent</fails_when>
    <automated>grep -v "def test_" tests/test_evaluate_table.py | grep -c "static_dielectric_const"</automated>
    <fails_when>prints anything other than 0 — the barred literal appears in a test body instead of being assembled from fragments (Phase 2 W-1 rule)</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - One commit `test(03-01): pin the status taxonomy, provenance, no-population-filter and 100-batch order on the synthetic table (D-02, D-03, D-04, EVAL-02, EVAL-03, EVAL-05)` (plus any `fix(03-01): …` commit a red test forced, named in the summary with the failing assertion).
    - `tests/test_evaluate_table.py` has eleven passing tests; the 100-entry batch returns 300 results in request order with `cost.evals` 9; `unsupported` and `missing` results carry `cost.evals` 0; the barred key on an unknown candidate is `unsupported`, not `missing` (Pitfall 6).
    - Every result's `unit`, `n_replicates` and `spread` equal the registry unit and the fixture parquet's `_n` / `_std` columns; `missing/value_absent` occurs exactly where `_n == 0` (F-05).
    - The gate prints `SUMMARY: 7/7 steps passed` with at least 135 tests collected (116 baseline + 2 + 6 + 11; the inventory test is updated, not added).
  </acceptance_criteria>
  <done>Charter section 13 M2 (1) for `ok`/`unsupported`/`missing`, (3) and (4) are reproducible by command on the synthetic table: each status has a passing test, the 100-entry batch is ordered and counted, and every response carries `source`, `backend`, `provenance_tier`, `n_replicates`, `spread`, `unit` and the two-field `cost`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request JSON -> `parse_request` | Caller-supplied payload; the only place untrusted structure enters the library |
| candidate parquet -> `TableBackend` | A file at a conventional path; a substituted table would serve wrong values under the right `source` |
| evaluator -> committed examples | Values written into the repository; must be invented fixture values, never PolyOmics rows |
| pandas / pyarrow scalars -> typed contract | `Any`-typed values cross into frozen dataclasses (F-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | `parse_request` | medium | mitigate | `batch maxItems 1000`, `properties uniqueItems`, `additionalProperties: false` in `eval-request.json` (F-29); validated before any lookup |
| T-03-02 | Tampering | `TableBackend._frame` | high | mitigate | parquet metadata `llm4pol.snapshot` must equal `PropertyTable.snapshot`, else `SnapshotMismatch` (Pattern 3, F-03); pinned by 03-02 Task 2 |
| T-03-03 | Information Integrity | `TableBackend.lookup`, CLI stdout | medium | mitigate | NaN medians/std become `None` before any dataclass; `json.dumps(..., allow_nan=False)` on stdout (F-34) |
| T-03-04 | Information Disclosure | `protocol/examples/*.json` | medium | mitigate | examples are generated from `conftest.SYNTHETIC_ROWS` only; Test 2 of Task 2 asserts every `candidate_id` is a synthetic id or the unknown id; the `static_` grep on `protocol/examples/` prints 0 |
| T-03-05 | Spoofing | `Status` strings from JSON | low | mitigate | `parse_status` narrows through `frozenset(get_args(Status))` (F-38); `EvalResult.from_json` type-checks every field |
| T-03-06 | Elevation of Privilege | `--request` / `--root` paths | low | accept | developer CLI running as the local user; paths opened as given, never globbed, never passed to a shell; no privilege boundary exists (RESEARCH Security Domain) |
| T-03-SC | Tampering | package installs | low | accept | This plan installs nothing (F-01; Package Legitimacy Audit: no packages) |
</threat_model>

## Artifacts this phase produces

**New files (this plan):** `src/llm4pol/evaluate/__init__.py`, `contract.py` (`Status`, `MissingReason`,
`Cost`, `Lookup`, `Backend`, `BatchEntry`, `EvalRequest`, `EvalResult`, `EvalResponse`, `RequestError`,
`parse_request`, `request_validator`, `response_validator`), `registry.py` (`PropertyTable`),
`evaluator.py` (`Evaluator`), `__main__.py` (`main`), `backends/__init__.py`, `backends/table.py`
(`TableBackend`, `SnapshotMismatch`); `protocol/schemas/eval-request.json`, `eval-response.json`;
`protocol/examples/eval-request.example.json`, `eval-response.example.json`; `tests/test_evaluate_cli.py`,
`tests/test_evaluate_contract.py`, `tests/test_evaluate_table.py`.

**Modified files:** `pyproject.toml` (two `[tool.llm4polcheck]` entries); `env/pixi.toml` (`evaluate`
task); `protocol/README.md` (`examples/` row; `## Current contents`); `tests/conftest.py`
(`make_synthetic_root`, `synthetic_candidates`, `real_candidates`, `SYNTHETIC_EXAMPLE_REQUEST`);
`tests/test_check_inventory.py` (`3 entries, 3 instances`).

**Commits (in order):** `test(03-01): add the failing evaluator tracer test …` -> `feat(03-01): evaluator
tracer …` -> `test(03-01): add failing schema, example and round-trip tests …` -> `feat(03-01): commit the
evaluator request/response examples …` -> `test(03-01): pin the status taxonomy …` (plus any
`fix(03-01): …`).

**Produced by the other plans of this phase:** `cache.py` (`JsonlCache`, `CacheError`), `budget.py`
(`BudgetMeter`, `BudgetExceeded`), the `error` path tests, `--cache` / `--evals-limit`, the A-3 row
(03-02); the four `[tool.importlinter]` contracts, `tests/test_import_boundary.py`, the A-1 / A-2
rows, `tests/test_evaluate_real_file.py`, `03-EVIDENCE.md`, CI (03-03).

**Not produced, by decision:** `backends/radonpy.py`, `config/hpc.json` (M9); OpenPoly / PoLyInfo
backends (M8); population selection and percentile metrics (Phase 5); `llm4pol.loop`, `run`, `llm`.

## Source audit (whole phase; the plan column names the task)

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | every lookup through one evaluator contract backed by `table`; four statuses; cache that never double-charges; two-currency budget | 03-01 T1 (contract, backend, statuses); 03-02 T1 (cache, budget) | COVERED | |
| REQ | EVAL-01 | payloads validate against both schemas; the suite loads them | 03-01 T1 (schemas, `parse_request`), T2 (examples, inventory, negative cases) | COVERED | gate step prints `3 entries, 3 instances` |
| REQ | EVAL-02 | exactly one status; `unsupported`/`missing` free; `error` retryable | 03-01 T1, T3 (`ok`/`missing`/`unsupported`, evals 0); 03-02 T2 (`error`, retry, never cached) | COVERED | |
| REQ | EVAL-03 | `backend`, `source`, `provenance_tier`, `n_replicates`, `spread`, `unit`, `cost` on every response | 03-01 T1, T3 (Test 7, Test 8) | COVERED | |
| REQ | EVAL-04 | cache keyed by `(candidate_id, property, backend, source)`; hit leaves `evals` unchanged | 03-02 T1 | COVERED | in-memory and JSONL replay |
| REQ | EVAL-05 | batch of 100 in request order with matching count | 03-01 T3 (synthetic); 03-03 T2 (real file) | COVERED | |
| REQ | EVAL-06 | import-linter: `loop` ↛ `evaluate.backends.radonpy`; `evaluate` ↛ `loop`/`llm`; `llm` only from `loop.agents` | 03-03 T1 | COVERED | third clause conflicts with CONTEXT R-1 (deferred to Phase 6); REQUIREMENTS.md outranks CONTEXT.md (CLAUDE.md precedence) so it is planned now in the F-23 form and flagged for the owner |
| CONTEXT | D-01 | schemas, examples in the inventory, shapes, ordering and count | 03-01 T1, T2, T3 | COVERED | |
| CONTEXT | D-02 | status taxonomy, `reason` enum, `error` retryable | 03-01 T1, T3; 03-02 T2 | COVERED | |
| CONTEXT | D-03 | value semantics; no population filter; `md_simulated` | 03-01 T1, T3 (Test 8, Test 9) | COVERED | |
| CONTEXT | D-04 | cache key, JSONL, per-candidate charging, `BudgetMeter`, `BudgetExceeded` | 03-01 T1 (charging); 03-02 T1 (the rest) | COVERED | |
| CONTEXT | D-05 | contract dataclasses, `Backend` Protocol, `TableBackend`, no radonpy | 03-01 T1 | COVERED | prohibition on a radonpy stub |
| CONTEXT | D-06 | three import-linter clauses; `evaluate` may import `data` | 03-03 T1 | COVERED | clause 2 in the R-1 layers form |
| CONTEXT | D-07 | CLI flags and exit codes; batch of 100 | 03-01 T1 (`--request`, 0/2); 03-02 T2 (`--cache`, `--evals-limit`, 3); 03-03 T2 (real 100) | COVERED | |
| CONTEXT | D-08 | synthetic fixture, real-file skip, the listed contract tests, INVARIANTS rows | 03-01 T1 (fixtures), T3; 03-02 T1/T2; 03-03 T1/T2 | COVERED | A-3 row in 03-02, A-1/A-2 rows in 03-03 |
| CONTEXT | R-1 | A-2 in the `layers` form | 03-03 T1 | COVERED | the deferred `llm` clause is planned anyway per EVAL-06 — flagged |
| CONTEXT | R-2 | charge once per distinct candidate per request | 03-01 T1, T3 (Test 10); 03-02 T1 (Pitfall 5 test) | COVERED | |
| CONTEXT | R-3 | `provenance_tier` enum; malformed id is exit 2 | 03-01 T1 (Test 2), T2 (Tests 1, 5) | COVERED | |
| CONTEXT | R-4 | flush per request, never cache `error`, stored `cached: false`, NaN -> None | 03-02 T1, T2 | COVERED | |
| CONTEXT | R-5 | pyarrow -> pandas indexed by `candidate_id`, loaded once | 03-01 T1 | COVERED | |
| CONTEXT | Discretion | persistence off unless `cache_path`; module split | 03-02 T1 (off by default); 03-01 T1 (seven modules) | COVERED | |
| RESEARCH | Pattern 1 | decision order; `unsupported` never touches the table | 03-01 T1, T3 (Test 5) | COVERED | |
| RESEARCH | Pattern 4 | two-phase evaluate | 03-01 T1 (ordering, charging); 03-02 T1 (commit after the budget check) | COVERED | |
| RESEARCH | Pattern 7 | guard-on-the-guard negative test | 03-03 T1 | COVERED | |
| RESEARCH | Pitfalls 1–9 | as listed | 03-03 T1 (1); 03-01 T1 (2, 6, 7, 8); 03-02 T1 (3, 4, 5); 03-01 T2 (9) | COVERED | |
| RESEARCH | F-49 | `evaluate/__init__` imports no backend | 03-01 T1 (grep); 03-03 T1 (ast test) | COVERED | |
| CHARTER | section 13 M2 (1) | each status tested; `unsupported`/`missing` free; `error` retried | 03-01 T3; 03-02 T2 | COVERED | |
| CHARTER | section 13 M2 (2) | cache hit leaves `evals` unchanged | 03-02 T1; 03-03 T2 (real) | COVERED | |
| CHARTER | section 13 M2 (3) | batch of 100 in order | 03-01 T3; 03-03 T2 | COVERED | |
| CHARTER | section 13 M2 (4) | provenance fields on every response | 03-01 T1, T3 | COVERED | |
| CHARTER | section 13 M2 (5) | schema validation and the first import-linter contracts inside the gate | 03-01 T2 (inventory); 03-03 T1 (contracts) | COVERED | |
| CHARTER | section 7 freezes | `eval-request.json`, `eval-response.json`, `[tool.importlinter]` | 03-01 T1; 03-03 T1 | COVERED | |
| SCOPE | — | CI green on both platforms without data; evidence | 03-03 T2, T3 | COVERED | real-file tests skip |
| DEFERRED | — | `backends/radonpy.py`, `config/hpc.json`, second-table backends, population selection | — | EXCLUDED | 03-CONTEXT Deferred Ideas |

<verification>
- `python -m llm4pol.evaluate --request protocol/examples/eval-request.example.json --root <synthetic root>` exits 0 with nine schema-valid results in request order (statuses `ok, ok, ok, missing, missing, ok, missing, unsupported, ok`, `cost.evals` 3).
- The gate prints `llm4polcheck inventory: 3 entries, 3 instances processed` and `SUMMARY: 7/7 steps passed`; at least 135 tests collected.
- `tests/test_evaluate_table.py` pins each of `ok` (n 1 and n >= 2), `missing/value_absent`, `missing/candidate_unknown`, `unsupported`; provenance on every result; no population filter; 300 results for 100 entries.
</verification>

<success_criteria>
ROADMAP Phase 3 success criteria 1 (for `ok`, `unsupported`, `missing`), 3, 4 and the schema half of 5
hold on the synthetic table and are evidenced by named tests; the evaluator request/response schemas
are frozen with committed examples validated inside the gate; the `Backend` contract, `TableBackend`,
`Evaluator` and the CLI exist as the skeleton the remaining plans extend without restructuring.
</success_criteria>

<output>
Create `.planning/phases/03-evaluator-table-backend/03-01-SUMMARY.md` when done. Record the five (or
more) commit shas, the RED evidence of the tracer (collection error, as 02-01 did), the exact
`llm4polcheck inventory` line, the test count collected, the nine statuses and `cost.evals` of the
example response, and any Task 3 assertion that revealed a tracer defect together with its fix commit.
Note for 03-02: `Evaluator(backend, properties)` gains `cache`, `meter` and `evals_limit` keyword
arguments there; `TableBackend._frame` retries after a failed load by design.
</output>
