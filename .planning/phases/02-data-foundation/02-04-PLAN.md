---
phase: 02-data-foundation
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/llm4pol/data/registry.py
  - src/llm4pol/data/filters.py
  - src/llm4pol/data/replicates.py
  - src/llm4pol/data/validate.py
  - scripts/check.py
  - pyproject.toml
  - tests/test_check_inventory.py
  - tests/test_data_registry.py
  - tests/test_data_invariants.py
  - docs/governance/INVARIANTS.md
autonomous: true
requirements: [DATA-03, DATA-05, DATA-07, DATA-10]
coupling_justified:
  - plan: "02-03"
    reason: "Task 2's noise_floor reads `<column>_n` and `<column>_std` from the candidate table that `load.build_candidates` (plan 02-01) writes. Wave-mate 02-03 Task 2 restates `<column>_n` as the non-null count, but pandas `count()` in the 02-01 `agg` is already the non-null count, so no value this plan depends on changes whichever plan lands first; the two plans share no file."

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`scripts/check.py` has a seventh step `schema-inventory` after `import-linter` that reads `[tool.llm4polcheck] validated` from `pyproject.toml`, validates every listed instance (YAML or JSON) against its JSON Schema, fails on a missing table, a missing schema path or a zero-match `required` glob, and prints `llm4polcheck inventory: 1 entries, 1 instances processed`; the gate prints `SUMMARY: 7/7 steps passed` (D-06)."
    - "`llm4pol.data.registry.load_registry()` returns a typed `Registry` whose nine `PropertySpec`s carry column, unit, unit_status, role, physical_range, and the `check_tc` / `tg_rmse_ladder` filters; its `snapshot` equals `SNAPSHOT_ID`, its columns equal `schema.PROPERTY_COLUMNS`, and an instance carrying the barred key is refused with `RegistryError` (D-05, DATA-03, A-7)."
    - "The validator report carries a `## Replicate structure and noise floor` section: rows per candidate (78,676 candidates, 12,983 multi-row, max 17, size histogram, same-version replicates vs cross-version re-runs) and, per registry property, the noise floor as median absolute std, median relative std and p90 relative std over candidates with n >= 2 — on all in-scope rows (TC 0.0369, ε_dc 0.0084, Tg 0.0577 / 30.2 K, density 0.0034) and on the README-triple rows (TC 0.0431, ε_dc 0.0112, Tg 0.0540), every table naming its population (D-04, D-07, DATA-05, DATA-07, charter §13 M1 (4), F-50, F-52, F-55, F-56)."
    - "Invariant D-9 is a test inside the gate — `test_d9_report_states_noise_floor_per_property_with_population` — and `docs/governance/INVARIANTS.md` names it in the D-9 guard cell; the D-7 test introduced by plan 02-01 (`test_d7_candidate_table_dedups_replicates_before_any_metric`) is extended here with the replicate-structure assertions, not duplicated (D-08, DATA-10, charter §13 M1 (6))."
  artifacts:
    - path: "src/llm4pol/data/registry.py"
      provides: "RegistryError, PropertySpec, Registry, REGISTRY_PATH, SCHEMA_PATH, load_registry"
      contains: "def load_registry("
    - path: "src/llm4pol/data/filters.py"
      provides: "readme_triple_mask(df, registry) — TC non-null AND dielectric_const_dc within its registry physical_range AND tg within its registry physical_range (inclusive)"
      contains: "def readme_triple_mask("
    - path: "src/llm4pol/data/replicates.py"
      provides: "ReplicateStructure, NoiseFloor, VERSION_COLUMNS, replicate_structure, noise_floor, noise_floor_on_rows, same_version_split"
      contains: "def noise_floor("
    - path: "src/llm4pol/data/validate.py"
      provides: "the section `Replicate structure and noise floor` and its findings appended to EXPECTED_POLYOMICS; run() takes registry=None and loads the protocol registry"
      contains: "Replicate structure and noise floor"
    - path: "scripts/check.py"
      provides: "validate_inventory(root) and step_schema_inventory registered as `schema-inventory` after `import-linter`"
      contains: "schema-inventory"
    - path: "pyproject.toml"
      provides: "[tool.llm4polcheck] with one validated entry: the property registry v1 against protocol/schemas/property-registry.json, required = true"
      contains: "[tool.llm4polcheck]"
    - path: "tests/test_check_inventory.py"
      provides: "Seven inventory-step tests including the repository inventory and the STEPS order"
      contains: "test_check_steps_include_schema_inventory_after_import_linter"
    - path: "tests/test_data_invariants.py"
      provides: "D-9 promoted test, the extended D-7 test, replicate-structure and noise-floor tests on the fixture and the pinned file (file created by plan 02-01)"
      contains: "test_d9_report_states_noise_floor_per_property_with_population"
    - path: "docs/governance/INVARIANTS.md"
      provides: "D-9 guard cell naming the test and the report section (D-7 and D-10 cells were promoted by plan 02-01)"
      contains: "test_d9_report_states_noise_floor_per_property_with_population"
  key_links:
    - from: "scripts/check.py"
      to: "pyproject.toml"
      via: "validate_inventory reads tool.llm4polcheck.validated with tomllib; missing table is a failure"
      pattern: "llm4polcheck"
    - from: "src/llm4pol/data/registry.py"
      to: "protocol/property-registry_v1.yaml"
      via: "yaml.safe_load + jsonschema.validate against protocol/schemas/property-registry.json"
      pattern: "jsonschema.validate"
    - from: "src/llm4pol/data/validate.py"
      to: "src/llm4pol/data/replicates.py"
      via: "run() calls replicate_structure, noise_floor (all in-scope candidates) and noise_floor_on_rows(readme_triple_mask) and renders both populations"
      pattern: "noise_floor_on_rows"
    - from: "src/llm4pol/data/filters.py"
      to: "src/llm4pol/data/registry.py"
      via: "the triple mask reads physical_range from the loaded registry, never a literal"
      pattern: "physical_range"
    - from: "docs/governance/INVARIANTS.md"
      to: "tests/test_data_invariants.py"
      via: "the D-9 guard cell names the test function"
      pattern: "test_d9_"
  prohibitions:
    - statement: "No range or filter literal (`1`, `20`, `100`, `900`, `0.1`) is written into `filters.py`, `replicates.py` or `validate.py`; they come from the loaded registry (a number is published in one place)."
      status: unverified
      verification: null
    - statement: "The noise floor is computed on the candidate table (dedup first, D-7), never on raw rows grouped ad hoc; the mean is never used as the candidate aggregate (F-57)."
      status: unverified
      verification: null
---

<objective>
Wire the frozen registry into the gate and the code, and give the validator its replicate section.
The `schema-inventory` check step (ported from the CALF20 precedent's `validate_inventory`) makes the
registry's schema validation part of every gate run; `llm4pol.data.registry` loads the same instance for
the code; `replicates.py` computes the replicate structure and the per-property noise floor on the
candidate table, on all in-scope rows and on the README-triple population; the report gains the section
charter §13 M1 exit criterion (4) asks for; D-9 becomes a named test and its INVARIANTS.md row says so,
while the D-7 test that plan 02-01 introduced with `build_candidates` is extended, not duplicated.

Purpose: D-06 and D-08 of the context; DATA-05, DATA-07 and half of DATA-10. The noise floor is the
number every later plot must carry (charter §9) and the D-16 gate reads it (D-15/D-16 stay gated).

Output: `src/llm4pol/data/registry.py`, `filters.py`, `replicates.py`, an extended `validate.py`;
`scripts/check.py` + `pyproject.toml` (`[tool.llm4polcheck]`); `tests/test_check_inventory.py`,
additions to `tests/test_data_registry.py` and to `tests/test_data_invariants.py` (created by 02-01);
the D-9 row of `docs/governance/INVARIANTS.md`.

Grounding facts: CALF20 `scripts/check.py::validate_inventory` (read it; port with `calfcheck` →
`llm4polcheck`); its tests in `CALF20_DiscoveryLoop/tests/test_check_inventory.py` (four cases). `scripts/`
is outside mypy's `packages`, so the step may import `yaml` and `jsonschema` without stubs; `ruff` does
lint it. Research: F-50 (by `candidate_id`: 78,676 candidates, 12,983 multi-row, max 17; sizes 1:
65,698 / 2: 11,368 / 3: 578 / 4: 55 / 5: 964 / 6: 16 / 7: 2 / 17: 1 were counted on (`smiles_list`,
tacticity∪unknown) groups — by `candidate_id` the histogram may differ by the 5 merges of F-35, so the
histogram is printed, not pinned), F-52 (1,888 same-version / 11,096 cross-version on smiles groups —
pinned with tolerance 10), F-53 (spread does not depend on the split), F-55 (noise floor on all
candidates n ≥ 2: TC n 9,439 abs 0.0082 rel 0.0369 p90 0.090; ε_dc n 12,842 abs 0.0227 rel 0.0084; Tg
n 2,377 abs 30.2 K rel 0.0577 p90 0.46; density n 12,984 rel 0.0034; Rg rel 0.035; r2 rel 0.163;
ffv rel 0.024; sp_ced rel 0.022; refractive_index rel 0.0014), F-56 (triple population, filter rows
then group, 1,725 groups: TC rel 0.0431 abs 0.0098; ε_dc rel 0.0112 abs 0.031; Tg rel 0.0540 abs 27.5
K), F-57 (median not mean), F-61 (filter-then-median order). On the synthetic fixture: the two
multi-row candidates are `*CC*`/none (TC 0.30, 0.34, 0.32 → median 0.32, std 0.02, rel 0.0625) and
`*CC(*)c1ccccc1`/atactic (TC 0.15, 0.16 → median 0.155, std 0.0070711, rel 0.045620); median rel over
the two = 0.054060; abs median = 0.0135355; the README-triple mask on the 12 in-scope rows selects 8
rows (u01–u08) and its multi-row candidates are the same two; `RadonPy_ver` is the only version column
in the fixture and is constant, so the same-version split is (2, 0).

Wave note: plan 02-03 (wave 2) shares no file with this plan; if both run concurrently in one working
tree, a wave-mate's sanctioned RED commit can turn the whole-repo gate red until its GREEN commit —
re-run the gate before treating a failure as this plan's own.
</objective>

<execution_context>
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-foundation/02-CONTEXT.md
@.planning/phases/02-data-foundation/02-RESEARCH.md
@.planning/phases/02-data-foundation/02-01-SUMMARY.md
@.planning/phases/02-data-foundation/02-02-SUMMARY.md
@CLAUDE.md
@docs/governance/INVARIANTS.md
@scripts/check.py
@pyproject.toml
@src/llm4pol/data/validate.py
@src/llm4pol/data/report.py
@src/llm4pol/data/load.py
@src/llm4pol/data/schema.py
@protocol/property-registry_v1.yaml
@protocol/schemas/property-registry.json
@tests/conftest.py
@tests/test_data_registry.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry loader and the `schema-inventory` check step (D-05, D-06, DATA-03, A-7)</name>
  <read_first>
    - `C:/Users/molsim/Desktop/CALF20_DiscoveryLoop/scripts/check.py` — `validate_inventory` (lines ~102–184: tomllib load, absent-table failure, per-entry `name` / `schema` / `instances` / `required`, YAML-or-JSON dispatch on suffix, `jsonschema.validate`, the summary print) and `step_schema_inventory`
    - `C:/Users/molsim/Desktop/CALF20_DiscoveryLoop/tests/test_check_inventory.py` — the four throwaway-pyproject cases and the `_write_pyproject` helper
    - `C:/Users/molsim/Desktop/CALF20_DiscoveryLoop/pyproject.toml` — the `[tool.calfcheck]` comment header and `[[tool.calfcheck.validated]]` entry shape
    - `scripts/check.py` (this repo) — `STEPS`, `_run`, `_print_output`, the docstring's tool list; `tests/conftest.py` puts `scripts/` on `sys.path` so tests can `from check import validate_inventory, STEPS`
    - `.planning/phases/02-data-foundation/02-CONTEXT.md` D-05 (registry keys and fields), D-06 (inventory step: fails on missing table or zero-match required glob; first entry the property registry; the `config/*.json` pairing test stays)
    - `.planning/phases/02-data-foundation/02-RESEARCH.md` Pattern 6 (port notes; append after `import-linter`; `scripts/` outside mypy), F-45, F-46
    - `src/llm4pol/data/snapshot.py` (`DEFAULT_ROOT`, `SNAPSHOT_ID`, `POLYOMICS_REVISION`), `src/llm4pol/data/schema.py` (`PROPERTY_COLUMNS`), `protocol/property-registry_v1.yaml`, `protocol/schemas/property-registry.json`
  </read_first>
  <behavior>
    - `tests/test_check_inventory.py` — Test 1 `test_missing_schema_path_fails`, Test 2 `test_required_entry_matching_zero_instances_fails`, Test 3 `test_absent_llm4polcheck_table_fails` (message contains `llm4polcheck`), Test 4 `test_well_formed_entry_with_matching_instance_passes` — the precedent's four cases on throwaway `pyproject.toml` files under `tmp_path`, with `[tool.llm4polcheck]`. Test 5 `test_yaml_instance_is_validated_not_skipped`: a `.yaml` instance violating `{"type": "object", "required": ["ok"]}` yields exactly one failure naming the entry. Test 6 `test_repository_inventory_validates`: `validate_inventory(REPO_ROOT) == []` and the captured stdout contains `llm4polcheck inventory: 1 entries, 1 instances processed`. Test 7 `test_check_steps_include_schema_inventory_after_import_linter`: `[name for name, _ in check.STEPS]` equals `["ruff check", "ruff format --check", "mypy", "import-linter", "schema-inventory", "history-secret-scan", "pytest"]`.
    - `tests/test_data_registry.py` additions — Test 15 `test_load_registry_returns_typed_specs_for_nine_keys`: `reg = load_registry()`; `list(reg.properties) == NINE_KEYS`; `reg.properties["tg"].physical_range == (100.0, 900.0)`; `reg.properties["tg"].tg_rmse_ladder == (0.05, 0.1, 0.2, 0.5, 1.0)`; `reg.properties["tg"].tg_rmse_unit == "(g/cm^3)^2"`; `reg.properties["thermal_conductivity"].check_tc is True`; `reg.properties["dielectric_const_dc"].check_tc is None`; `reg.properties["r2"].unit_status == "unverified"` and its `unit_note` is non-empty; `reg.properties["ffv"].column == "fractional_free_volume"`. Test 16 `test_registry_snapshot_equals_snapshot_id_constant`: `reg.snapshot == SNAPSHOT_ID` and `reg.revision == POLYOMICS_REVISION`. Test 17 `test_registry_columns_equal_loader_property_columns`: `reg.columns() == PROPERTY_COLUMNS` (same order) and `reg.spec_for_column("Rg").key == "rg"`. Test 18 `test_load_registry_rejects_an_instance_with_the_barred_key`: a tmp copy of the instance with the barred key injected (fragments) → `load_registry(tmp_path)` raises `RegistryError`. Test 19 `test_load_registry_rejects_a_missing_file`: `load_registry(tmp_path / "absent.yaml")` raises `RegistryError` naming the path.
  </behavior>
  <files>src/llm4pol/data/registry.py, scripts/check.py, pyproject.toml, tests/test_check_inventory.py, tests/test_data_registry.py</files>
  <action>
    STEP 1 — RED: write `tests/test_check_inventory.py` (module docstring: why an explicit inventory
    beats sibling-schema discovery — a deleted schema would silently remove its own validation;
    `from check import STEPS, validate_inventory`) and append Tests 15–19 to
    `tests/test_data_registry.py` (this file now imports `llm4pol.data.registry`, `llm4pol.data.schema`,
    `llm4pol.data.snapshot`). Run both files → RED (`ImportError` on `validate_inventory`,
    `ModuleNotFoundError` on `llm4pol.data.registry`). Commit `test(02-04): add failing
    schema-inventory step and registry-loader tests (D-05, D-06)`.

    STEP 2 — the check step. In `scripts/check.py` add module imports `json`, `tomllib`, `jsonschema`,
    `yaml`; port `validate_inventory(root: str | os.PathLike[str]) -> list[str]` from the precedent
    with `calfcheck` → `llm4polcheck` everywhere in identifiers, messages and the summary line
    (`llm4polcheck inventory: N entries, M instances processed`), keeping the four failure classes
    (absent table; missing schema path; `required = true` with zero matches; instance fails
    validation) and the YAML/JSON dispatch on suffix with `yaml.safe_load`; add
    `step_schema_inventory() -> bool` that prints each failure as `  - <failure>` and returns `not
    failures`; insert `("schema-inventory", step_schema_inventory)` into `STEPS` immediately after
    `("import-linter", step_import_linter)`; extend the module docstring's tool list with the
    schema inventory (`[tool.llm4polcheck]`, every listed protocol instance validated against its JSON
    Schema — invariant "a payload with no schema has no guard", CLAUDE.md). In `pyproject.toml` add,
    after `[tool.importlinter]`, a comment header in the precedent's style (the table is explicit
    by construction; a missing table is a failure; entry fields `name`, `instances`, `schema`,
    `required`) and `[tool.llm4polcheck]` with one `[[tool.llm4polcheck.validated]]` entry: `name =
    "property registry v1 (DATA-03, A-7)"`, `instances = "protocol/property-registry_v1.yaml"`,
    `schema = "protocol/schemas/property-registry.json"`, `required = true`.

    STEP 3 — the loader `src/llm4pol/data/registry.py`: `class RegistryError(ValueError)`;
    `REGISTRY_PATH = DEFAULT_ROOT / "protocol" / "property-registry_v1.yaml"`, `SCHEMA_PATH =
    DEFAULT_ROOT / "protocol" / "schemas" / "property-registry.json"`; frozen dataclass
    `PropertySpec(key, column, unit, unit_status, role, physical_range: tuple[float, float], check_tc:
    bool | None, tg_rmse_ladder: tuple[float, ...] | None, tg_rmse_unit: str | None, unit_note: str |
    None)`; frozen dataclass `Registry(schema_version: int, registry_version: str, snapshot: str,
    revision: str, properties: dict[str, PropertySpec])` with methods `columns() -> tuple[str, ...]`
    (registry order), `spec_for_column(column) -> PropertySpec` (`KeyError` → `RegistryError`),
    `by_role(role) -> tuple[PropertySpec, ...]`; `load_registry(path: Path = REGISTRY_PATH, *,
    schema_path: Path = SCHEMA_PATH) -> Registry`: read both files (`FileNotFoundError` →
    `RegistryError(f"registry file not found: {path}")`), `yaml.safe_load`, `json.load`,
    `jsonschema.validate` (`ValidationError` → `RegistryError(exc.message)`), then build the specs
    (ranges and ladders as tuples of `float`). The module imports `yaml` (typed by `types-pyyaml`)
    and `jsonschema` (Any under the override); mypy strict clean; under 150 lines.

    STEP 4 — GREEN: run `pixi run --manifest-path env/pixi.toml python -m pytest
    tests/test_check_inventory.py tests/test_data_registry.py -v` → 7 + 19 PASSED; run the gate →
    `Steps: ruff check, ruff format --check, mypy, import-linter, schema-inventory,
    history-secret-scan, pytest`, the line `llm4polcheck inventory: 1 entries, 1 instances
    processed`, `SUMMARY: 7/7 steps passed`. Commit `feat(02-04): add the schema-inventory check step
    with the property registry as its first entry, and the typed registry loader (D-05, D-06,
    DATA-03)`.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the `Steps:` line is not `Steps: ruff check, ruff format --check, mypy, import-linter, schema-inventory, history-secret-scan, pytest`, or the output lacks `llm4polcheck inventory: 1 entries, 1 instances processed`, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_check_inventory.py tests/test_data_registry.py -v</automated>
    <fails_when>exit code is non-zero, or fewer than 26 tests are collected, or any of `test_absent_llm4polcheck_table_fails`, `test_required_entry_matching_zero_instances_fails`, `test_yaml_instance_is_validated_not_skipped`, `test_repository_inventory_validates`, `test_check_steps_include_schema_inventory_after_import_linter`, `test_load_registry_returns_typed_specs_for_nine_keys`, `test_registry_snapshot_equals_snapshot_id_constant`, `test_registry_columns_equal_loader_property_columns`, `test_load_registry_rejects_an_instance_with_the_barred_key` is not listed as PASSED</fails_when>
    <automated>grep -c '"schema-inventory"' scripts/check.py</automated>
    <fails_when>prints 0 — the step is not registered in STEPS</fails_when>
    <automated>grep -c "^\[tool.llm4polcheck\]" pyproject.toml</automated>
    <fails_when>prints anything other than 1</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -c "from llm4pol.data.registry import load_registry; r = load_registry(); print(r.snapshot, len(r.properties), r.properties['tg'].physical_range, r.properties['thermal_conductivity'].check_tc)"</automated>
    <fails_when>the output is not `polyomics:general_polymers@041e5834 9 (100.0, 900.0) True`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits: `test(02-04): add failing schema-inventory step and registry-loader tests (D-05, D-06)` then `feat(02-04): add the schema-inventory check step with the property registry as its first entry, and the typed registry loader (D-05, D-06, DATA-03)`.
    - `scripts/check.py` defines `validate_inventory` and `step_schema_inventory`; `STEPS` order is exactly the seven names in Test 7; the step's failure classes are: absent `[tool.llm4polcheck]` table, missing `schema` path, `required = true` with zero matches, instance validation failure (each proven by a named test).
    - `pyproject.toml` `[tool.llm4polcheck]` lists exactly one entry with `required = true` for `protocol/property-registry_v1.yaml` against `protocol/schemas/property-registry.json`; `tests/test_governance.py::test_every_config_file_has_a_schema` is unchanged and still passes (D-06: the `config/*.json` pairing test stays).
    - `src/llm4pol/data/registry.py` exposes `RegistryError`, `PropertySpec`, `Registry`, `REGISTRY_PATH`, `SCHEMA_PATH`, `load_registry`; `load_registry().columns() == schema.PROPERTY_COLUMNS`; `load_registry().snapshot == snapshot.SNAPSHOT_ID`.
    - The gate prints `SUMMARY: 7/7 steps passed` and the pytest step reports 0 failed.
  </acceptance_criteria>
  <done>The registry is guarded twice — by the gate's `schema-inventory` step on every run and by a typed loader that refuses an invalid instance — and the code reads ranges, units and filters from the protocol file instead of literals; the gate is seven steps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replicate structure and noise floor in the report; D-9 promoted, D-7 extended (D-04, D-07, D-08, DATA-05, DATA-07, DATA-10)</name>
  <read_first>
    - `src/llm4pol/data/validate.py`, `report.py`, `load.py` (`build_candidates`, the candidate column naming `<column>_median|_n|_min|_max|_std`, `n_rows`), `schema.py` (`PROPERTY_COLUMNS`) — from 02-01
    - `src/llm4pol/data/registry.py` (Task 1)
    - `tests/conftest.py` — `SYNTHETIC_ROWS`, `synthetic_root`, `real_load`; `tests/test_data_invariants.py` (from 02-01) — the existing `test_d7_candidate_table_dedups_replicates_before_any_metric` and `test_d10_…` this task extends and must keep green
    - `.planning/phases/02-data-foundation/02-CONTEXT.md` D-04 (noise floor = median of within-candidate relative and absolute spread over candidates with n ≥ 2, per property), D-07 (section list: replicate structure and noise floor), D-08 (D-7 dedup before any metric; D-9 report contains the noise floor), § Specific Ideas
    - `.planning/phases/02-data-foundation/02-RESEARCH.md` — F-50..F-58, F-61, Q3, Code Examples (candidate table + noise expression), Anti-Patterns (mean vs median; `groupby` dropna)
    - `docs/governance/INVARIANTS.md` — row D-9 (its wording stays; only the Guard cell changes), the already-promoted D-7 and D-10 rows (from 02-01; do not edit), and the Promotion policy
    - `docs/MASTER-PLAN.md` §9 (the noise floor goes on every plot; effects below it are not effects), §13 M1 (4)
  </read_first>
  <behavior>
    - Test 1 — extend the existing `test_d7_candidate_table_dedups_replicates_before_any_metric` (introduced by plan 02-01; keep its current assertions: 9 unique ids, `n_rows.sum() == 12`, the candidate parquet is read by `validate`) with: `replicates.replicate_structure(candidates)` returns `n_candidates == 9`, `n_multi_row == 2`, `rows_in_multi_row == 5`, `max_rows == 3`, `size_histogram == {1: 7, 2: 1, 3: 1}`; `replicates.noise_floor(candidates, PROPERTY_COLUMNS)` accepts only the candidate table (its signature takes the candidate frame, not rows) and the report's noise-floor population labels begin with `candidates with n >= 2`. No second D-7 test is introduced.
    - Test 2 `test_noise_floor_values_on_synthetic_are_medians_of_relative_std`: `noise_floor(candidates, ("thermal_conductivity",))["thermal_conductivity"]` has `n_groups == 2`, `median_rel_std == pytest.approx(0.054060, abs=1e-5)`, `median_abs_std == pytest.approx(0.0135355, abs=1e-6)`, `p90_rel_std` between 0.0456 and 0.0625; `density` has `n_groups == 2` and `median_rel_std == 0.0` (constant values); the aggregate is the median (a candidate with one 100× outlier row in the fixture would not move it — assert `noise_floor` on a frame where one candidate's `_std` is 1e6 still returns the median of the others).
    - Test 3 `test_readme_triple_mask_uses_registry_ranges`: on the synthetic rows parquet (12 rows), `filters.readme_triple_mask(rows, load_registry()).sum() == 8`; passing a registry whose `dielectric_const_dc` range is `(1.0, 30.0)` (constructed in the test by `dataclasses.replace`) gives 9 (row u09 with 25.0 enters) — proving the bounds come from the registry object, not a literal.
    - Test 4 `test_noise_floor_on_readme_triple_rows_filters_then_groups`: `noise_floor_on_rows(rows, readme_triple_mask(rows, reg), ("thermal_conductivity", "dielectric_const_dc", "tg"))` returns `n_groups == 2` for each of the three (the same two multi-row candidates survive the mask).
    - Test 5 `test_same_version_split_uses_only_present_version_columns`: `same_version_split(rows, candidates)` on the synthetic frame returns `(2, 0)` and does not raise although only `RadonPy_ver` of the eight `VERSION_COLUMNS` exists.
    - Test 6 `test_d9_report_states_noise_floor_per_property_with_population`: `validate.run(synthetic_root, expected={}, report_path=tmp)` returns 0 and the report contains `## Replicate structure and noise floor`; a table with header `| quantity | population | value |` containing rows `| candidates | in-scope rows | 9 |`, `| multi-row candidates | in-scope rows | 2 |`, `| rows in multi-row candidates | in-scope rows | 5 |`, `| max rows per candidate | in-scope rows | 3 |`, `| same-version replicate candidates | multi-row candidates | 2 |`, `| cross-version re-run candidates | multi-row candidates | 0 |`; a table with header `| property | population | n_groups | median_abs_std | median_rel_std | p90_rel_std |` with nine rows whose population is `candidates with n >= 2 (in-scope rows)` and whose `thermal_conductivity` row has `n_groups` 2; and a second such table with three rows (`thermal_conductivity`, `dielectric_const_dc`, `tg`) whose population is `candidates with n >= 2 (README-triple rows)`.
    - Test 7 `test_real_replicate_structure_and_noise_floor_match_research` (skip without the file): on `real_load`, `validate.run(REPO_ROOT, processed_dir=real_load.processed_dir, report_path=tmp)` returns 0 and the findings include `replicate_multi_row_candidates` 12,983 `reproduced`, `replicate_max_rows` 17 `reproduced`, `noise_floor_rel_thermal_conductivity` 0.0369 `documented`, `noise_floor_rel_dielectric_const_dc` 0.0084 `documented`, `noise_floor_rel_tg` 0.0577 `documented`, `noise_floor_abs_tg` 30.2 `documented`, `noise_floor_rel_density` 0.0034 `documented`, `noise_floor_triple_rel_thermal_conductivity` 0.0431 `documented`, `noise_floor_triple_rel_dielectric_const_dc` 0.0112 `documented`, `noise_floor_triple_rel_tg` 0.0540 `documented`, `same_version_replicate_candidates` 1,888 `documented`, `cross_version_rerun_candidates` 11,096 `documented` (F-50, F-52, F-55, F-56).
  </behavior>
  <files>src/llm4pol/data/filters.py, src/llm4pol/data/replicates.py, src/llm4pol/data/validate.py, tests/test_data_invariants.py, docs/governance/INVARIANTS.md</files>
  <action>
    STEP 1 — RED: in `tests/test_data_invariants.py` (created by plan 02-01 with the D-7 and D-10
    tests) extend `test_d7_…` per Test 1 and append Tests 2–7 (Test 7 uses the `real_load` fixture
    and the skip pattern); update the module docstring to list D-9. Run it → RED on the missing
    modules (the untouched `test_d10_…` stays green). Commit `test(02-04): add failing D-9 invariant,
    replicate-structure and noise-floor tests; extend the D-7 test (D-08, DATA-05, DATA-07)`.

    STEP 2 — `src/llm4pol/data/filters.py`: `readme_triple_mask(df, registry) -> Series` = TC column
    non-null AND `dielectric_const_dc` between the registry's `physical_range` bounds (inclusive) AND
    `tg` between the registry's `tg` bounds (inclusive); columns are looked up through
    `registry.properties[key].column`; no numeric literal in the module (plan 02-05 extends this
    module with the ladder and feasible-set functions). `src/llm4pol/data/replicates.py`:
    `VERSION_COLUMNS = ("RadonPy_ver", "preset_eq_ver", "preset_tc_ver", "preset_tg_ver",
    "preset_sp_ver", "RDKit_ver", "Psi4_ver", "Python_ver")` (F-52); frozen dataclasses
    `ReplicateStructure(n_candidates, n_multi_row, rows_in_multi_row, max_rows, size_histogram:
    dict[int, int])` and `NoiseFloor(property, n_groups, median_abs_std, median_rel_std, p90_rel_std)`;
    `replicate_structure(candidates) -> ReplicateStructure` from `n_rows`; `noise_floor(candidates,
    columns) -> dict[str, NoiseFloor]`: for each column, rows with `<column>_n >= 2`, `abs =
    <column>_std`, `rel = abs / |<column>_median|` (rows whose median is 0 are dropped from `rel`),
    medians and the 0.9 quantile as builtin floats (NaN → `float("nan")` when `n_groups == 0`);
    `noise_floor_on_rows(rows, mask, columns)`: filter rows by mask, `build_candidates`, then
    `noise_floor` (filter-then-group, F-56/F-61); `same_version_split(rows, candidates) -> tuple[int,
    int]`: over multi-row candidates, count those whose present `VERSION_COLUMNS` (intersection with
    `rows.columns`; NaN treated as a value) are identical across the group vs those that differ. In
    `validate.py`: `run()` gains `registry: Registry | None = None` (default `load_registry()`) and
    appends the section `Replicate structure and noise floor` with the three tables of Test 6 (the
    histogram rendered as one row per size `rows per candidate = k` with population `candidates`),
    plus findings appended to `EXPECTED_POLYOMICS`: `replicate_multi_row_candidates` 12983
    (`reproduce`, population `in-scope rows`), `replicate_max_rows` 17 (`reproduce`),
    `same_version_replicate_candidates` 1888 (`documented`, tolerance 10, note: F-52 counted on
    smiles-list groups), `cross_version_rerun_candidates` 11096 (`documented`, tolerance 10),
    `noise_floor_rel_thermal_conductivity` 0.0369 / `noise_floor_rel_dielectric_const_dc` 0.0084 /
    `noise_floor_rel_tg` 0.0577 / `noise_floor_rel_density` 0.0034 (`documented`, tolerance 0.001,
    population `candidates with n >= 2 (in-scope rows)`), `noise_floor_abs_tg` 30.2 (`documented`,
    tolerance 0.5), `noise_floor_triple_rel_thermal_conductivity` 0.0431 /
    `noise_floor_triple_rel_dielectric_const_dc` 0.0112 / `noise_floor_triple_rel_tg` 0.0540
    (`documented`, tolerance 0.001, population `candidates with n >= 2 (README-triple rows)`). Print
    relative spreads with four decimals and absolute spreads with four significant digits. Keep
    `validate.py` under 400 lines by moving table construction for this section into a function
    `replicate_section(rows, candidates, registry) -> tuple[Section, dict[str, float | int]]` in
    `replicates.py`.

    STEP 3 — INVARIANTS.md: replace only the Guard cell of row D-9 with: the `Replicate structure and
    noise floor` section of `docs/audit/polyomics-041e5834-validation.md` (per-property median
    relative and absolute spread over candidates with n ≥ 2);
    `tests/test_data_invariants.py::test_d9_report_states_noise_floor_per_property_with_population`.
    Do not touch the D-7 and D-10 cells plan 02-01 already promoted (the extended D-7 test keeps its
    name, so the D-7 cell stays correct). Leave every other row and the preamble byte-identical.

    STEP 4 — GREEN: `pixi run --manifest-path env/pixi.toml python -m pytest
    tests/test_data_invariants.py -v` → 8 PASSED locally (2 from 02-01, one of them extended, + 6
    new); gate `SUMMARY: 7/7 steps passed`; commit `feat(02-04): report replicate structure and noise
    floor per property; promote D-9 to a test (D-04, D-07, D-08, DATA-05, DATA-07, DATA-10)`. Paste
    the real-file finding lines for the noise floor into the summary.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_invariants.py -v</automated>
    <fails_when>exit code is non-zero, or fewer than 8 tests are collected, or any of `test_d7_candidate_table_dedups_replicates_before_any_metric`, `test_d10_every_count_table_in_report_names_its_population`, `test_noise_floor_values_on_synthetic_are_medians_of_relative_std`, `test_readme_triple_mask_uses_registry_ranges`, `test_d9_report_states_noise_floor_per_property_with_population`, `test_real_replicate_structure_and_noise_floor_match_research` is not listed as PASSED on this machine</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -m llm4pol.data validate</automated>
    <fails_when>exit code is non-zero, or stdout lacks `finding noise_floor_rel_thermal_conductivity: documented expected=0.0369 observed=0.03` (prefix; the observed value must round to within 0.001), or lacks `finding replicate_multi_row_candidates: reproduce expected=12,983 observed=12,983 reproduced`, or the `VALIDATION:` line reports a non-zero failed count</fails_when>
    <automated>sed -n '/^| D-9 /p' docs/governance/INVARIANTS.md | grep -c "test_d9_report_states_noise_floor_per_property_with_population"</automated>
    <fails_when>prints 0 — D-9 still has a prose guard</fails_when>
    <automated>sed -n '/^| D-7 /p' docs/governance/INVARIANTS.md | grep -c "test_d7_candidate_table_dedups_replicates_before_any_metric"</automated>
    <fails_when>prints 0 — the D-7 cell promoted by plan 02-01 was damaged by this plan's edit</fails_when>
    <automated>sed -n '/^| D-7 /p;/^| D-9 /p;/^| D-10 /p' docs/governance/INVARIANTS.md | grep -c "| prose |"</automated>
    <fails_when>prints anything other than 0 — a promoted row still says prose (D-8 alone may still read prose; it is plan 02-05's)</fails_when>
    <automated>grep -c "mean()" src/llm4pol/data/replicates.py</automated>
    <fails_when>prints anything other than 0 — the candidate aggregate or the floor uses the mean (F-57)</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two further commits: `test(02-04): add failing D-9 invariant, replicate-structure and noise-floor tests; extend the D-7 test (D-08, DATA-05, DATA-07)` then `feat(02-04): report replicate structure and noise floor per property; promote D-9 to a test (D-04, D-07, D-08, DATA-05, DATA-07, DATA-10)`; `git show --format= <GREEN_SHA> -- docs/governance/INVARIANTS.md` shows exactly one removed and one added line (the D-9 row).
    - `src/llm4pol/data/replicates.py` defines `VERSION_COLUMNS`, `ReplicateStructure`, `NoiseFloor`, `replicate_structure`, `noise_floor`, `noise_floor_on_rows`, `same_version_split`, `replicate_section`; `src/llm4pol/data/filters.py` defines `readme_triple_mask` and contains no numeric literal other than `0` / `1` / `2` used for counting (the bounds come from the registry).
    - On the pinned file, `python -m llm4pol.data validate` reports `replicate_multi_row_candidates` 12,983 and `replicate_max_rows` 17 as `reproduced`, and the ten noise-floor / version-split findings as `documented` within their tolerances (F-50, F-52, F-55, F-56); no tolerance was widened beyond the plan's values — if a value falls outside, the population definition is checked against the F-fact first and the discrepancy is written into the summary.
    - The report's noise-floor tables list exactly the nine registry keys (the barred column never appears as a property row).
    - The gate prints `SUMMARY: 7/7 steps passed`.
  </acceptance_criteria>
  <done>Charter §13 M1 exit criterion (4) is reproducible by command: the report states rows per candidate, same-version vs re-run replicates, and the median absolute and relative spread of every registry property over candidates with n ≥ 2 on two named populations, with TC 3.7 %, ε_dc 0.8 % and Tg 5.8 % (30 K) documented against the research; D-9 is a named test the gate runs and the D-7 test now also pins the replicate structure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pyproject.toml` inventory → gate | A deleted entry or table would silently remove validation; the step fails on an absent table |
| YAML protocol → `load_registry` | `yaml.safe_load` only; `jsonschema.validate` before any field is read |
| registry values → filters and report | The triple mask and every range come from the loaded registry object |
| candidate table → metrics | Every aggregate is computed after dedup by `candidate_id` (D-7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Tampering | inventory silenced by deletion | medium | mitigate | `validate_inventory` returns a failure on an absent `[tool.llm4polcheck]` table and on a zero-match `required` glob (`test_absent_llm4polcheck_table_fails`, `test_required_entry_matching_zero_instances_fails`) |
| T-02-19 | Elevation of Privilege | YAML deserialisation in the step and the loader | medium | mitigate | `yaml.safe_load` only in `scripts/check.py` and `registry.py` |
| T-02-20 | Tampering | a range literal drifting from the registry | medium | mitigate | `filters.py` reads bounds from `Registry`; `test_readme_triple_mask_uses_registry_ranges` proves a changed registry changes the mask |
| T-02-21 | Repudiation | a noise floor computed on the wrong population | medium | mitigate | two populations named in every table (`candidates with n >= 2 (in-scope rows)` / `(README-triple rows)`); findings pin both against F-55 / F-56 with tolerance |
| T-02-22 | Tampering | outlier runs inflating the floor | low | mitigate | median-of-groups with `<column>_std` per candidate; `mean()` grep-banned from `replicates.py` (F-57) |
| T-02-SC | Tampering | package installs | low | accept | This plan installs nothing |
</threat_model>

## Artifacts this phase produces

**New files (this plan):** `src/llm4pol/data/registry.py` (`RegistryError`, `PropertySpec`, `Registry`,
`REGISTRY_PATH`, `SCHEMA_PATH`, `load_registry`); `src/llm4pol/data/filters.py` (`readme_triple_mask`);
`src/llm4pol/data/replicates.py` (`VERSION_COLUMNS`, `ReplicateStructure`, `NoiseFloor`,
`replicate_structure`, `noise_floor`, `noise_floor_on_rows`, `same_version_split`, `replicate_section`);
`tests/test_check_inventory.py` (7 tests).

**Modified files:** `scripts/check.py` (`validate_inventory`, `step_schema_inventory`, `STEPS` seven
entries; `Steps:` line gains `schema-inventory` after `import-linter`; gate summary `7/7`);
`pyproject.toml` (`[tool.llm4polcheck]` with one entry); `src/llm4pol/data/validate.py` (`run(...,
registry=None)`, section `Replicate structure and noise floor`, twelve new expectations);
`tests/test_data_registry.py` (+5 loader tests, now 19); `tests/test_data_invariants.py` (+6 tests
and the extended D-7 test, now 8); `docs/governance/INVARIANTS.md` (D-9 guard cell only).

**Commits (in order):** `test(02-04): …schema-inventory step and registry-loader tests…` →
`feat(02-04): add the schema-inventory check step …` → `test(02-04): …D-7 / D-9 …` → `feat(02-04):
report replicate structure and noise floor …`.

## Source audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | DATA-03 | registry loader; barred column absent | 02-04 T1 | COVERED | loader half |
| REQ | DATA-05 | replicates grouped by `candidate_id`; median, count, spread | 02-04 T2 | COVERED | noise floor on the candidate table |
| REQ | DATA-07 | replicate structure and noise floor of TC, ε_dc, Tg in the report | 02-04 T2 | COVERED | two populations |
| REQ | DATA-10 | D-9 promoted in the same change; D-7 test extended | 02-04 T2 | COVERED | D-7, D-10 in 02-01; D-8, A-7 in 02-05 |
| CONTEXT | D-04 | noise floor definition | 02-04 T2 | COVERED | |
| CONTEXT | D-05 | registry loader accessors | 02-04 T1 | COVERED | |
| CONTEXT | D-06 | `[tool.llm4polcheck]` + `schema-inventory` step | 02-04 T1 | COVERED | |
| CONTEXT | D-07 | report section: replicate structure and noise floor | 02-04 T2 | COVERED | |
| CONTEXT | D-08 | D-9 test; INVARIANTS.md guard column | 02-04 T2 | COVERED | D-7 promoted by 02-01 |
| RESEARCH | Pattern 6 | port of `validate_inventory` | 02-04 T1 | COVERED | |
| RESEARCH | F-50..F-56, Q3 | replicate facts | 02-04 T2 | COVERED | findings with tolerance |
| RESEARCH | F-61 | filter-then-median order | 02-04 T2 | COVERED | `noise_floor_on_rows` |
| CHARTER | §13 M1 (4) | replicate structure and noise floor recorded | 02-04 T2 | COVERED | |
| CHARTER | §13 M1 (6) | D-9 test | 02-04 T2 | COVERED | partial; D-7, D-10 in 02-01; D-8, A-7 in 02-05 |

<verification>
- The gate prints seven steps and `SUMMARY: 7/7 steps passed` with `llm4polcheck inventory: 1 entries,
  1 instances processed`.
- `python -m llm4pol.data validate` on the pinned file reports the replicate and noise-floor findings
  as reproduced / documented with a zero failed count.
- INVARIANTS.md row D-9 names the test; D-7 and D-10 (from 02-01) are intact; no other row changed.
</verification>

<success_criteria>
The registry is enforced by the gate and consumed by the code through one loader; the report records
the replicate structure and the per-property noise floor on two named populations, reproducing F-50
and documenting F-52, F-55, F-56 within stated tolerances (charter §13 M1 (4)); D-9 runs as a test
inside the seven-step gate and the D-7 test now pins the replicate structure (M1 (6), with D-7 and
D-10 from 02-01 and D-8, A-7 from 02-05).
</success_criteria>

<output>
Create `.planning/phases/02-data-foundation/02-04-SUMMARY.md` when done. Record the four commit shas,
the seven-step `Steps:` line, the `llm4polcheck inventory:` line, the twelve replicate / noise-floor
finding lines from the pinned-file run, and any tolerance the observed values approached (with the
population definition used).
</output>
