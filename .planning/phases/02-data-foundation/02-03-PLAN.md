---
phase: 02-data-foundation
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/llm4pol/data/fetch.py
  - src/llm4pol/data/identity.py
  - src/llm4pol/data/schema.py
  - src/llm4pol/data/load.py
  - tests/test_data_fetch.py
  - tests/test_data_identity.py
  - tests/test_data_schema.py
  - tests/test_manifest.py
  - tests/test_data_real_file.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`fetch` returns 1 and prints `MISMATCH <name> expected sha256=… size=… observed sha256=… size=…` for every file whose bytes or size differ from `data/MANIFEST-open.sha256`, checks every entry before returning, returns 1 on a manifest whose `# revision:` header differs from `POLYOMICS_REVISION`, returns 2 when the manifest is missing or the downloader raises, and calls the downloader only for absent files with `repo_id`, `repo_type=\"dataset\"`, `filename`, `revision` and `local_dir` set exactly (D-01, DATA-01, Q5, F-05)."
    - "One manifest parser serves both manifests: `tests/test_manifest.py` imports `parse_manifest` and `sha256_of` from `llm4pol.data.fetch` and still verifies the PoLyInfo files when present (RESEARCH Don't Hand-Roll)."
    - "`canonical_psmiles` merges `*C(C*)(C)C(=O)OC` and `*C(C*)(C(=O)OC)C` into `*CC(*)(C)C(=O)OC`, turns `*/C(=C/CC*)C` into `*CC/C=C(/*)C`, keeps `@` and `/` stereo marks and both `*` points, returns None for `*C(` and for the comma-joined `*CC*,*OC*`, and is idempotent; `candidate_id(\"*CC*\", None)` is `d220c5c57c4fae2e` (D-03, D-6, DATA-04, F-32..F-35)."
    - "The loader declares only registry, identity and flag columns (`n_atom` and `mol_weight` are never numeric), keeps every undeclared source column as pass-through, writes declared string fields as `string` (not `large_string`), counts second-monomer exclusions before parsing and parse failures after, maps missing tacticity to `unknown`, and raises `LoaderError` naming missing required columns (D-02, D-03, F-41, F-42)."
    - "On the pinned file the rows parquet has 95,332 rows x 262 columns, 78,373 unique `canonical_psmiles`, 78,676 unique `candidate_id`; the candidate parquet's `n_rows` sums to 95,332 and its maximum is 17 (F-07, F-37, F-50)."
  artifacts:
    - path: "tests/test_data_fetch.py"
      provides: "Eleven fetch behaviours on the synthetic root plus the committed-manifest content test"
      contains: "test_fetch_downloads_a_missing_file_then_verifies_it"
    - path: "tests/test_data_identity.py"
      provides: "Seven identity vectors and id-contract tests"
      contains: "d220c5c57c4fae2e"
    - path: "tests/test_data_schema.py"
      provides: "Twelve loader and declared-schema tests on the synthetic root"
      contains: "test_loaded_parquet_schema_has_declared_types_after_cast"
    - path: "tests/test_manifest.py"
      provides: "PoLyInfo manifest test now importing parse_manifest and sha256_of from llm4pol.data.fetch"
      contains: "from llm4pol.data.fetch import"
    - path: "tests/test_data_real_file.py"
      provides: "Real-file identity counts and parquet schema round-trip"
      contains: "test_real_identity_counts_match_research"
  key_links:
    - from: "tests/test_manifest.py"
      to: "src/llm4pol/data/fetch.py"
      via: "parse_manifest(MANIFEST).entries and sha256_of(path) replace the test-local helpers"
      pattern: "parse_manifest"
    - from: "tests/test_data_fetch.py"
      to: "src/llm4pol/data/fetch.py"
      via: "fetch(root, downloader=<fake>) with a recording fake that writes bytes into local_dir/filename"
      pattern: "downloader="
    - from: "tests/test_data_schema.py"
      to: "src/llm4pol/data/schema.py"
      via: "DECLARED_FIELDS types compared with pq.read_schema of the synthetic parquet"
      pattern: "DECLARED_FIELDS"
  prohibitions:
    - statement: "No expected number in a real-file test is changed to make a test pass; a mismatch is a loader defect or a population-definition error to be resolved against the cited F-fact."
      status: unverified
      verification: null
    - statement: "`smiles_search` is never used for identity (F-36) and never declared as an identity column."
      status: unverified
      verification: null
---

<objective>
Expand the tracer's fetch, identity and loader layers from the one proven path to their full
contracts: every fetch failure path with its exit code, the manifest parser shared with the PoLyInfo
manifest test, the RDKit identity vectors from the research, the declared pyarrow schema asserted by a
test, the loader's exclusion and pass-through rules, and the real-file identity counts. No new layer
is added; every behaviour here is a test-first hardening of a module plan 02-01 created.

Purpose: DATA-01, DATA-02 and DATA-04 are contracts (hash verification, declared schema, identity
rule); the tracer proved them on the happy path, this plan pins their edges so plans 02-04 and 02-05
can build the validator on a loader whose behaviour is fixed.

Output: `tests/test_data_fetch.py`, `tests/test_data_identity.py`, `tests/test_data_schema.py`, an
updated `tests/test_manifest.py`, real-file additions in `tests/test_data_real_file.py`, and whatever
fixes the tests force in `fetch.py`, `identity.py`, `schema.py`, `load.py`.

Grounding facts: see 02-01's objective for the RDKit vectors and the sha256 of `*CC*|unknown`; the
synthetic fixture facts (14 rows, 23 columns, 10 unique `smiles_list`, exclusions 1 + 1, 12 in-scope
rows, 7 canonical, 9 candidates, rows u07/u08 merge, `*OC*` → `*CO*`) are stated in 02-01 Task 1 and
repeated here where a test uses them. Real-file facts: F-07 (78,373 canonical, 78,676 ids), F-32 (0
parse failures on single SMILES; the 3 comma-joined rows are the `smiles_2` rows), F-37 (95,332
in-scope rows), F-50 (by `candidate_id`: 78,676 candidates, 12,983 multi-row, max 17), F-42 (pandas 3
`str` arrives as `large_string`; cast to declared `string`), F-44 (`pd.read_parquet` round-trip).

Wave note: plan 02-04 (wave 2) shares no file with this plan; if both run concurrently in one working
tree, a wave-mate's sanctioned RED commit can turn the whole-repo gate red until its GREEN commit —
re-run the gate before treating a failure as this plan's own. Plan 02-04 also adds the
`schema-inventory` gate step, so the gate's step count is 6 or 7 depending on ordering.
</objective>

<execution_context>
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-foundation/02-CONTEXT.md
@.planning/phases/02-data-foundation/02-RESEARCH.md
@.planning/phases/02-data-foundation/02-01-SUMMARY.md
@CLAUDE.md
@src/llm4pol/data/fetch.py
@src/llm4pol/data/identity.py
@src/llm4pol/data/schema.py
@src/llm4pol/data/load.py
@src/llm4pol/data/snapshot.py
@tests/conftest.py
@tests/test_manifest.py
@tests/test_data_cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fetch contract — every mismatch, every exit code, one manifest parser (D-01, D-10, DATA-01)</name>
  <read_first>
    - `src/llm4pol/data/fetch.py` (from 02-01) — `Manifest`, `FileCheck`, `parse_manifest`, `sha256_of`, `verify_file`, `fetch`, the `Downloader` alias
    - `src/llm4pol/data/snapshot.py` — `POLYOMICS_REPO`, `POLYOMICS_REVISION`, `CSV_NAME`, `README_NAME`, `raw_dir`, `manifest_path`
    - `tests/conftest.py` — `synthetic_root` (writes a manifest that matches the synthetic files)
    - `tests/test_manifest.py` — `_parse_manifest`, `_sha256`, `test_manifest_exists_and_is_well_formed`, `test_raw_files_match_manifest` (keep the test names and the skip reason)
    - `.planning/phases/02-data-foundation/02-RESEARCH.md` — Q5 (`hf_hub_download` never hashes; fast path on commit-hash match), F-05 (`.cache/huggingface/download/<file>.metadata` layout — the fetch must not depend on it), Pattern 1, Pattern 2, § Security Domain V5/V6
  </read_first>
  <behavior>
    - Test 1 `test_parse_manifest_reads_revision_header_and_entries`: a tmp manifest with two comment lines, a `# revision: <40 hex>` line, a blank line and two entries → `.revision` equals the 40-hex value and `.entries == {"a.csv": ("<64 hex>", 10), "README.md": ("<64 hex>", 3)}`.
    - Test 2 `test_parse_manifest_rejects_malformed_lines`: a 63-hex sha → `ValueError` whose message contains the 1-based line number; a non-integer size → `ValueError`; a manifest without a `# revision:` header parses with `.revision is None`.
    - Test 3 `test_fetch_verifies_existing_files_and_never_calls_the_downloader`: on `synthetic_root`, `fetch(root, downloader=<raises AssertionError>)` returns 0; stdout has `verified general_polymers_with_sp_abbe_dynamic-dielectric.csv`, `verified README.md` and `snapshot: polyomics:general_polymers@041e5834`.
    - Test 4 `test_fetch_downloads_a_missing_file_then_verifies_it`: delete the synthetic README; a recording fake downloader asserts it is called with `repo_id == POLYOMICS_REPO`, `repo_type == "dataset"`, `filename == "README.md"`, `revision == POLYOMICS_REVISION`, `local_dir == raw_dir(root)`, writes the original bytes to `local_dir / filename` and returns its `str` path; `fetch` returns 0, stdout has `downloaded README.md`, the fake was called exactly once.
    - Test 5 `test_fetch_returns_1_on_sha256_mismatch_and_names_the_file`: append one byte to the synthetic README; downloader = fake that returns the existing path without writing; `fetch` returns 1; stdout contains `MISMATCH README.md expected sha256=<first 12 hex of manifest sha>` and `observed sha256=`; the CSV line still reads `verified …csv`.
    - Test 6 `test_fetch_returns_1_on_size_mismatch_in_the_manifest`: rewrite the synthetic manifest with the CSV size + 1 → `fetch` returns 1 and stdout has `MISMATCH general_polymers_with_sp_abbe_dynamic-dielectric.csv` and `size=`.
    - Test 7 `test_fetch_returns_1_when_manifest_revision_differs_from_the_constant`: replace the header's revision with 40 `f` characters → returns 1, stdout contains `MISMATCH revision`; no `verified` line is printed (the revision is checked first).
    - Test 8 `test_fetch_returns_2_when_manifest_is_missing`: delete the manifest → returns 2; stderr starts with `ERROR:` and names `MANIFEST-open.sha256`.
    - Test 9 `test_fetch_returns_2_when_downloader_raises`: delete the README; downloader raises `OSError("offline")` → returns 2; stderr has `ERROR:` and `README.md`.
    - Test 10 `test_fetch_checks_every_file_before_returning`: corrupt both files (downloader = no-op returning the path) → returns 1 and stdout has one `MISMATCH` line per file (two lines).
    - Test 11 `test_committed_manifest_open_lists_the_two_pinned_files_with_research_hashes`: `parse_manifest(REPO_ROOT / "data" / "MANIFEST-open.sha256")` has `.revision == POLYOMICS_REVISION` and `.entries == {CSV_NAME: ("71e955ac1e90574ad009ccbf1238e595cf42905f3e600523ba2a6ea65c350213", 196910783), "README.md": ("c2340252a720f7b9f56fcc368105d9e1e351c2bd75f31a364f7c2afcb5a6cc13", 15532)}` (F-02, F-03).
    - Test 12 (modified `tests/test_manifest.py`): `test_manifest_exists_and_is_well_formed` and `test_raw_files_match_manifest` use `parse_manifest(MANIFEST).entries` and `sha256_of`; the local `_parse_manifest` and `_sha256` helpers are deleted; the skip reason is unchanged.
  </behavior>
  <files>src/llm4pol/data/fetch.py, tests/test_data_fetch.py, tests/test_manifest.py</files>
  <action>
    STEP 1 — RED: write `tests/test_data_fetch.py` (behaviours 1–11; a module-level `_recording_downloader(bytes_by_name)` helper that returns a callable accepting the exact keyword arguments `hf_hub_download` takes and records each call's kwargs in a list; a `_raise(*a, **k)` helper) and rewrite the two helper usages in `tests/test_manifest.py` (behaviour 12; update the module docstring's "before the charter is approved" sentence to say the file now also proves the shared manifest parser). Run both files; expect RED on whichever behaviours the tracer did not implement (at minimum Tests 2, 6, 7 and 10 are likely RED; Test 11 may already pass). Record the RED lines. Commit `test(02-03): add failing fetch-contract tests and share the manifest parser with test_manifest.py (D-01, DATA-01)`.

    STEP 2 — GREEN in `fetch.py` only: make `parse_manifest` raise `ValueError` with the 1-based line number and the offending text for a sha that is not 64 hex or a size that is not a positive integer, and set `revision` to `None` when the header is absent; check the revision header before any file; verify every entry and collect all `FileCheck`s before deciding the return value (mismatch lines are printed as they are found; return 1 if any failed); print sha256 prefixes as the first 12 hex characters followed by `…`; map any exception raised by the downloader (`OSError`, `huggingface_hub.errors.HfHubHTTPError`, `huggingface_hub.errors.LocalEntryNotFoundError`, or any `Exception` subclass the library raises — catch `Exception` at the downloader call boundary only, re-raise `KeyboardInterrupt`) to `ERROR: download failed for <name>: <exc>` on stderr and return 2; keep the function free of child processes and of any read of the download-metadata sidecar directory the hub library creates under `local_dir` (F-05) — the payload hash is the only verification. Keep `fetch.py` under 200 lines, fully annotated, `ruff format` clean.

    STEP 3 — run `pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_fetch.py tests/test_manifest.py -v` → 11 + 4 PASSED; full gate green; commit `feat(02-03): complete the fetch contract — revision guard, every-file check, exit codes (D-01, D-10)`.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_fetch.py tests/test_manifest.py -v</automated>
    <fails_when>exit code is non-zero, or fewer than 15 tests are collected, or any of `test_fetch_downloads_a_missing_file_then_verifies_it`, `test_fetch_returns_1_on_sha256_mismatch_and_names_the_file`, `test_fetch_returns_1_when_manifest_revision_differs_from_the_constant`, `test_fetch_returns_2_when_downloader_raises`, `test_fetch_checks_every_file_before_returning`, `test_committed_manifest_open_lists_the_two_pinned_files_with_research_hashes`, `test_raw_files_match_manifest` is not listed as PASSED (or, for `test_raw_files_match_manifest`, SKIPPED with the PoLyInfo reason)</fails_when>
    <automated>grep -c "def _parse_manifest\|def _sha256" tests/test_manifest.py</automated>
    <fails_when>prints anything other than 0 — the test still carries its own copy of the parser</fails_when>
    <automated>grep -c "from llm4pol.data.fetch import" tests/test_manifest.py</automated>
    <fails_when>prints 0 — the shared parser is not imported</fails_when>
    <automated>grep -c "\.cache" src/llm4pol/data/fetch.py</automated>
    <fails_when>prints anything other than 0 — fetch reads the download sidecar instead of hashing the payload (Q5)</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final `SUMMARY:` line is not `SUMMARY: N/N steps passed` with N equal to the number of names in the `Steps:` line (6 or 7), or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits: `test(02-03): add failing fetch-contract tests and share the manifest parser with test_manifest.py (D-01, DATA-01)` then `feat(02-03): complete the fetch contract — revision guard, every-file check, exit codes (D-01, D-10)`; the RED commit touches only `tests/test_data_fetch.py` and `tests/test_manifest.py`.
    - `tests/test_data_fetch.py` collects 11 tests; `tests/test_manifest.py` collects 4 and imports `parse_manifest` and `sha256_of` from `llm4pol.data.fetch`.
    - `fetch.fetch` return values: 0 all verified; 1 any `MISMATCH` (file or revision); 2 missing manifest or downloader exception — each proven by a named test above.
    - `pixi run --manifest-path env/pixi.toml python -m llm4pol.data fetch` on this repository still exits 0 with the two `verified` lines and `snapshot: polyomics:general_polymers@041e5834`.
  </acceptance_criteria>
  <done>`fetch` is a complete contract: every entry of the pinned manifest is hashed and sized, every failure names its file with expected and observed values and returns 1, revision drift is refused before any file is touched, I/O failures return 2, and the PoLyInfo manifest test proves the same parser the fetch uses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Identity vectors, declared schema and loader rules, proven on the fixture and the pinned file (D-02, D-03, D-04, DATA-02, DATA-04)</name>
  <read_first>
    - `src/llm4pol/data/identity.py`, `schema.py`, `load.py` (from 02-01)
    - `tests/conftest.py` — `SYNTHETIC_ROWS` (the 14 rows; u06 has missing tacticity; u07/u08 are the same molecule; u11 is `*OC*`; u12 is unparseable; u13 has `smiles_2`), `synthetic_root`, `real_load`
    - `.planning/phases/02-data-foundation/02-RESEARCH.md` — F-32..F-39 (identity facts and vectors), F-41 (string columns that look numeric), F-42 (large_string cast), F-44, F-48, F-50, Pattern 3, Pattern 4, Anti-Patterns (`smiles_search`, mean vs median, `pa.string()` without cast, numeric `n_atom`)
    - `docs/MASTER-PLAN.md` §6 (candidate definition; D-6 stereo-aware key)
  </read_first>
  <behavior>
    - `tests/test_data_identity.py` — Test 1 `test_canonical_psmiles_merges_equivalent_raw_strings`: `canonical_psmiles("*C(C*)(C)C(=O)OC") == canonical_psmiles("*C(C*)(C(=O)OC)C") == "*CC(*)(C)C(=O)OC"`; `canonical_psmiles("*/C(=C/CC*)C") == "*CC/C=C(/*)C"`; `canonical_psmiles("C(C*)(*)c1ccccc1") == "*CC(*)c1ccccc1"`; `canonical_psmiles("*OC*") == "*CO*"`. Test 2 `test_canonical_psmiles_keeps_stereo_and_both_polymerisation_points`: `canonical_psmiles("*C[C@H](*)C")` contains `@` and has exactly two `*`; `canonical_psmiles("*C[C@@H](*)C")` differs from it; `canonical_psmiles("*/C(=C/CC*)C")` contains `/`. Test 3 `test_canonical_psmiles_returns_none_for_unparseable_and_comma_joined_lists`: `*C(`, `*CC*,*OC*` and `""` → None. Test 4 `test_canonical_psmiles_is_idempotent`: for every parseable `smiles_list` in `SYNTHETIC_ROWS`, `canonical_psmiles(canonical_psmiles(s)) == canonical_psmiles(s)`. Test 5 `test_candidate_id_is_the_first_16_hex_of_sha256_canonical_pipe_tacticity`: `candidate_id("*CC*", "unknown") == "d220c5c57c4fae2e" == hashlib.sha256(b"*CC*|unknown").hexdigest()[:16]`; matches `^[0-9a-f]{16}$`. Test 6 `test_candidate_id_maps_missing_tacticity_to_unknown`: `candidate_id("*CC*", None) == candidate_id("*CC*", float("nan")) == candidate_id("*CC*", "") == candidate_id("*CC*", "unknown")`; `normalise_tacticity("atactic") == "atactic"`; `UNKNOWN_TACTICITY == "unknown"`. Test 7 `test_candidate_id_differs_across_tacticity_and_canonical`: the three ids for (`*CC*`, none), (`*CC*`, atactic), (`*CC(*)C`, none) are pairwise distinct.
    - `tests/test_data_schema.py` — Test 8 `test_property_columns_are_the_nine_registry_columns_in_charter_order`: `PROPERTY_COLUMNS == ("thermal_conductivity", "dielectric_const_dc", "tg", "Rg", "r2", "fractional_free_volume", "sp_ced", "density", "refractive_index")` and the barred column (built from fragments) is not in it. Test 9 `test_dtypes_never_declare_the_per_chain_string_columns_numeric`: `DTYPES.get("n_atom") in (None, "str")`, `DTYPES.get("mol_weight") in (None, "str")`, `DTYPES["check_tc"] == "boolean"`, `DTYPES["do_TC"] == "boolean"`, `DTYPES["smiles_list"] == "str"`, every `PROPERTY_COLUMNS` entry maps to `"float64"`, `"smiles_search"` is not in `IDENTITY_COLUMNS`. Test 10 `test_declared_fields_cover_registry_identity_and_flag_columns`: names of `DECLARED_FIELDS` ⊇ `PROPERTY_COLUMNS` ∪ `IDENTITY_COLUMNS` ∪ {`UUID`, `smiles_list`, `smiles_2`, `tacticity`, `check_tc`, `tg_rmse`, the barred column}; string fields are `pa.string()`, property fields `pa.float64()`, `check_tc` `pa.bool_()`, `row_index` `pa.int64()`. Test 11 `test_loaded_parquet_schema_has_declared_types_after_cast`: after `load(synthetic_root)`, `pq.read_schema(rows_parquet)` has, for every declared field, `schema.field(name).type == field.type` (in particular `smiles_list` is `string`, not `large_string`), and contains a field `extra_note`. Test 12 `test_parquet_metadata_carries_snapshot_revision_and_source_counts`: rows-parquet metadata has `llm4pol.snapshot` = `polyomics:general_polymers@041e5834`, `llm4pol.revision` = the 40-hex, `llm4pol.source_rows` = `14`, `llm4pol.source_columns` = `23`, `llm4pol.excluded_second_monomer` = `1`, `llm4pol.excluded_parse_failure` = `1`, `llm4pol.source_sha256` = `sha256_of(csv_path(root))`; candidates-parquet metadata has `llm4pol.snapshot`. Test 13 `test_loader_excludes_second_monomer_rows_before_parsing_and_counts_parse_failures`: `LoadResult` has `excluded_second_monomer == 1`, `excluded_parse_failure == 1`, `rows_written == 12`; the rows parquet's `row_index` set is `{0..13} − {11, 12}` and `UUID` contains neither `u12` nor `u13`. Test 14 `test_loader_maps_missing_tacticity_to_unknown_and_ids_are_total`: no null `candidate_id`; the row with `UUID == "u06"` has `tacticity == "unknown"`; every id matches `^[0-9a-f]{16}$`. Test 15 `test_loader_merges_equivalent_raw_smiles_into_one_candidate`: rows `u07` and `u08` share `candidate_id` and both have `canonical_psmiles == "*CC(*)c1ccccc1"`; `u11` has `canonical_psmiles == "*CO*"`; `candidate_id.nunique() == 9`. Test 16 `test_candidate_table_has_median_n_min_max_std_per_property`: the candidates parquet has `<column>_median`, `_n`, `_min`, `_max`, `_std` for all nine columns plus `n_rows`, `canonical_psmiles`, `tacticity`, `candidate_id`; the candidate with `canonical_psmiles == "*CC*"` and `tacticity == "none"` has `n_rows == 3`, `thermal_conductivity_median == 0.32`, `_min == 0.30`, `_max == 0.34`, `_std == pytest.approx(0.02)`, `tg_median == 260`; the `*CO*`/none candidate has `n_rows == 1`, `thermal_conductivity_n == 0` and NaN `thermal_conductivity_std`. Test 17 `test_loader_raises_loader_error_naming_missing_required_columns`: a CSV lacking `tg_rmse` and `check_tc` → `LoaderError` whose message contains both names. Test 18 `test_loader_raises_loader_error_when_csv_is_missing`: `read_source(tmp_path / "absent.csv")` → `LoaderError` containing the path. Test 19 `test_read_source_keeps_every_source_column`: `read_source(csv)` has 23 columns including `extra_note`.
    - `tests/test_data_real_file.py` additions (skip without the file) — Test 20 `test_real_identity_counts_match_research`: from `real_load`, `excluded_second_monomer == 3`, `excluded_parse_failure == 0`; the rows parquet has `canonical_psmiles.nunique() == 78373`, `candidate_id.nunique() == 78676`, no null in either (F-07, F-32, F-37). Test 21 `test_real_parquet_schema_round_trips_with_declared_types`: `pq.read_metadata(rows_parquet).num_rows == 95332`, `num_columns == 262`, every declared field has its declared type. Test 22 `test_real_candidates_have_unique_ids_and_sizes_sum_to_in_scope_rows`: the candidates parquet has 78,676 unique ids, `n_rows.sum() == 95332`, `n_rows.max() == 17`, `(n_rows >= 2).sum() == 12983` (F-50).
  </behavior>
  <files>src/llm4pol/data/identity.py, src/llm4pol/data/schema.py, src/llm4pol/data/load.py, tests/test_data_identity.py, tests/test_data_schema.py, tests/test_data_real_file.py</files>
  <action>
    STEP 1 — RED: write `tests/test_data_identity.py` (Tests 1–7) and `tests/test_data_schema.py`
    (Tests 8–19; build the barred column name from fragments `"static_" + "dielectric_const"` as in
    plan 02-02), append Tests 20–22 to `tests/test_data_real_file.py`. Run the three files; record
    which tests are RED (Tests 9, 17, 18 and the `_n == 0` clause of Test 16 are the likely ones).
    Commit `test(02-03): add failing identity, declared-schema and loader tests (D-02, D-03, D-04)`.

    STEP 2 — GREEN, touching only the three modules: in `identity.py` make `normalise_tacticity`
    treat `None`, float NaN (`value != value`), and the empty string as `unknown`, and guard
    `canonical_psmiles("")` to return None; in `schema.py` make sure `DTYPES` maps `do_TC` to
    `boolean` and contains no entry for `n_atom` or `mol_weight`, that `DECLARED_FIELDS` includes
    every name Test 10 lists with the listed Arrow types, and that `IDENTITY_COLUMNS` excludes the
    cyclised-oligomer search column of F-36; in `load.py` make `read_source` raise `LoaderError(f"missing source file:
    {path}")` and `LoaderError("missing required columns: a, b")` (sorted names), make
    `build_candidates` compute `<column>_n` as the non-null count so a candidate whose single row
    lacks a property reports `_n == 0`, and keep `std` as pandas `ddof=1` so `n == 1` yields NaN.
    Do not touch `report.py`, `validate.py` or `__main__.py` (plan 02-04 edits `validate.py` in this
    wave). Keep each module under 300 lines and mypy-strict clean (convert pandas scalars with
    `int()` / `float()` before they leave a typed function).

    STEP 3 — run `pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_identity.py
    tests/test_data_schema.py tests/test_data_real_file.py -v` → 7 + 12 + (3 + 3) PASSED locally;
    full gate green; commit `feat(02-03): pin identity vectors, declared schema and loader exclusion
    rules; prove identity counts on the pinned file (D-02, D-03, D-04, DATA-02, DATA-04)`. Paste the
    real-file PASSED lines and the `--durations=3` block into the summary.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_identity.py tests/test_data_schema.py -v</automated>
    <fails_when>exit code is non-zero, or fewer than 19 tests are collected, or any of `test_canonical_psmiles_merges_equivalent_raw_strings`, `test_candidate_id_is_the_first_16_hex_of_sha256_canonical_pipe_tacticity`, `test_candidate_id_maps_missing_tacticity_to_unknown`, `test_loaded_parquet_schema_has_declared_types_after_cast`, `test_loader_excludes_second_monomer_rows_before_parsing_and_counts_parse_failures`, `test_loader_merges_equivalent_raw_smiles_into_one_candidate`, `test_candidate_table_has_median_n_min_max_std_per_property`, `test_loader_raises_loader_error_naming_missing_required_columns` is not listed as PASSED</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_real_file.py -v --durations=3</automated>
    <fails_when>exit code is non-zero, or `test_real_identity_counts_match_research`, `test_real_parquet_schema_round_trips_with_declared_types`, `test_real_candidates_have_unique_ids_and_sizes_sum_to_in_scope_rows` are not all listed as PASSED on this machine (the file is present; a SKIPPED here means the fixture path is wrong)</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -c "from llm4pol.data.identity import candidate_id, canonical_psmiles; print(candidate_id('*CC*', None), canonical_psmiles('*C(C*)(C(=O)OC)C'), canonical_psmiles('*C('))"</automated>
    <fails_when>the output is not `d220c5c57c4fae2e *CC(*)(C)C(=O)OC None`</fails_when>
    <automated>grep -c "smiles_search" src/llm4pol/data/identity.py src/llm4pol/data/load.py</automated>
    <fails_when>either count is not 0 — the cyclised-oligomer key (F-36) is being used for identity</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final `SUMMARY:` line is not `SUMMARY: N/N steps passed` with N equal to the number of names in the `Steps:` line, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two further commits: `test(02-03): add failing identity, declared-schema and loader tests (D-02, D-03, D-04)` then `feat(02-03): pin identity vectors, declared schema and loader exclusion rules; prove identity counts on the pinned file (D-02, D-03, D-04, DATA-02, DATA-04)`.
    - `tests/test_data_identity.py` collects 7 tests, `tests/test_data_schema.py` 12, and `tests/test_data_real_file.py` now 6 (3 from 02-01 + 3 here); all PASS locally.
    - The candidate table contract is observable on the fixture: `*CC*`/none → `n_rows` 3, TC median 0.32, min 0.30, max 0.34, std ≈ 0.02; `*CO*`/none → `thermal_conductivity_n` 0 and NaN std (D-04).
    - On the pinned file: 95,332 rows x 262 columns; 78,373 unique canonical; 78,676 unique ids; `n_rows` sums to 95,332, max 17, 12,983 multi-row candidates (F-07, F-37, F-50) — none of these expected values was edited.
    - `schema.DTYPES` has no numeric entry for `n_atom` or `mol_weight` (F-41) and `IDENTITY_COLUMNS` is exactly `("candidate_id", "canonical_psmiles", "row_index")`.
  </acceptance_criteria>
  <done>The identity rule of charter §6 is pinned by vectors (merges, stereo kept, `*` kept, unknown tacticity, 16-hex id), the declared parquet schema is asserted field by field after the `large_string` cast, the loader's exclusion, pass-through and error rules are tests, and the pinned file reproduces 78,373 canonical repeat units and 78,676 candidates with replicate sizes summing to the 95,332 in-scope rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest text → `parse_manifest` | A hand-edited committed file; malformed lines must fail fast, not be skipped |
| downloader callable → filesystem | `hf_hub_download` (or a test fake) writes into `raw_dir`; only its on-disk bytes are trusted, after hashing |
| CSV cells → dtype map | Strings that look numeric (`n_atom`) and nullable booleans (`check_tc`) |
| SMILES text → RDKit → identity | Equivalent strings must merge; stereo must not be lost; `smiles_search` must never be the key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Tampering | malformed or edited manifest line | medium | mitigate | `parse_manifest` raises `ValueError` with the line number on a non-64-hex sha or non-integer size (`test_parse_manifest_rejects_malformed_lines`); the committed manifest's content is pinned by `test_committed_manifest_open_lists_the_two_pinned_files_with_research_hashes` |
| T-02-14 | Tampering | partial download accepted | high | mitigate | every downloaded file is re-hashed and re-sized after the downloader returns; every entry is checked before the return value is decided (`test_fetch_checks_every_file_before_returning`) |
| T-02-15 | Spoofing | identity merge of different polymers (D-6) | high | mitigate | canonical SMILES with `isomericSmiles=True`; stereo vectors in `test_canonical_psmiles_keeps_stereo_and_both_polymerisation_points`; `smiles_search` is grep-banned from identity and load |
| T-02-16 | Tampering | silent dtype corruption of `/`-joined columns | medium | mitigate | `test_dtypes_never_declare_the_per_chain_string_columns_numeric` (F-41) |
| T-02-17 | Denial of Service | downloader exception crashing the CLI without an exit code | low | mitigate | caught at the downloader boundary only, printed as `ERROR:`, exit 2 (`test_fetch_returns_2_when_downloader_raises`) |
| T-02-SC | Tampering | package installs | low | accept | This plan installs nothing |
</threat_model>

## Artifacts this phase produces

**New files (this plan):** `tests/test_data_fetch.py` (11 tests; helpers `_recording_downloader`,
`_raise`); `tests/test_data_identity.py` (7 tests); `tests/test_data_schema.py` (12 tests).

**Modified files:** `tests/test_manifest.py` (imports `parse_manifest`, `sha256_of`);
`tests/test_data_real_file.py` (+3 real-file tests); `src/llm4pol/data/fetch.py` (revision guard first,
every-file check, `ValueError` on malformed lines, downloader exception → 2); `src/llm4pol/data/identity.py`
(`normalise_tacticity` edge cases, empty-string guard); `src/llm4pol/data/schema.py` (`do_TC` boolean,
declared field list complete); `src/llm4pol/data/load.py` (`LoaderError` messages, `<column>_n` as non-null
count).

**Commits (in order):** `test(02-03): …fetch-contract…` → `feat(02-03): complete the fetch contract …` →
`test(02-03): …identity, declared-schema and loader tests…` → `feat(02-03): pin identity vectors …`.

## Source audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | DATA-01 | sha256 + size verification against `MANIFEST-open` | 02-03 T1 | COVERED | every failure path |
| REQ | DATA-02 | declared pyarrow schema, identifier | 02-03 T2 | COVERED | schema asserted field by field |
| REQ | DATA-04 | `candidate_id` rule with stereo-aware canonical | 02-03 T2 | COVERED | vectors F-35 |
| CONTEXT | D-01 | fetch and pin | 02-03 T1 | COVERED | |
| CONTEXT | D-02 | declared schema, every column kept | 02-03 T2 | COVERED | pass-through test |
| CONTEXT | D-03 | identity, exclusions counted, NaN → unknown | 02-03 T2 | COVERED | |
| CONTEXT | D-04 | median / n / min / max / std per property | 02-03 T2 | COVERED | Test 16 |
| CONTEXT | D-10 | exit codes 1 / 2 | 02-03 T1 | COVERED | |
| RESEARCH | F-36 | never use `smiles_search` | 02-03 T2 | COVERED | grep gate |
| RESEARCH | F-41, F-42 | string columns, large_string cast | 02-03 T2 | COVERED | |
| RESEARCH | Don't Hand-Roll | one manifest parser | 02-03 T1 | COVERED | `tests/test_manifest.py` imports it |
| CHARTER | §13 M1 (1) | pinned files = manifest | 02-03 T1 | COVERED | failure paths proven |

<verification>
- 11 fetch tests, 4 manifest tests, 7 identity tests, 12 schema tests and 6 real-file tests pass
  locally; the gate is green.
- `python -m llm4pol.data fetch` still verifies both files without downloading.
</verification>

<success_criteria>
DATA-01, DATA-02 and DATA-04 are complete contracts rather than happy paths: a wrong byte, a wrong size,
a drifted revision, a missing manifest or a failing download each produce the documented line and exit
code; the parquet schema is the declared one; the identity rule reproduces the research vectors and the
pinned file's 78,373 / 78,676 / 95,332 counts.
</success_criteria>

<output>
Create `.planning/phases/02-data-foundation/02-03-SUMMARY.md` when done. Record the four commit shas,
which tests were RED before GREEN, the real-file PASSED lines and durations, and any expected value
that a loader defect forced you to investigate (with the F-fact that settled it).
</output>
