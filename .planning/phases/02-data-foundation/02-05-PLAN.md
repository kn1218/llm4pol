---
phase: 02-data-foundation
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - src/llm4pol/data/filters.py
  - src/llm4pol/data/validate.py
  - tests/test_data_invariants.py
  - tests/test_data_real_file.py
  - docs/audit/polyomics-041e5834-validation.md
  - docs/audit/README.md
  - docs/research/databases/README.md
  - docs/governance/INVARIANTS.md
  - .planning/phases/02-data-foundation/02-EVIDENCE.md
autonomous: true
requirements: [DATA-06, DATA-08, DATA-09, DATA-10]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`docs/audit/polyomics-041e5834-validation.md` is committed, generated by `python -m llm4pol.data validate` (byte-identical to a fresh run), and its `## Findings` table reproduces 95,335 rows x 259 columns, 78,379 unique `smiles_list`, the README triple 43,561 under exactly `thermal_conductivity` non-null ∧ `dielectric_const_dc` ∈ [1, 20] ∧ `tg` ∈ [100, 900] K, 43,560 in scope, 42,733 with `check_tc`, 40,212 candidates; and documents the differences: `static_dielectric_const` Maxwell violation 88.83 % on the triple rows (README 88.9 %), `dielectric_const_dc` violation 0 as the identity `ε_dc = ε_s − 1 + n²` with max residual < 1e-5, and 73,045 as the dataset card's description not reproducible from any column (D-07, R-5, DATA-06, charter §13 M1 (2)–(3), F-08, F-13, F-14, F-19)."
    - "The report states the feasible-set size under the development defaults — ε_dc ≤ Q25 of candidate medians (Q25 = 2.6524) ∧ Tg ≥ 400 K → 6,793 of 40,212 candidates (16.89 %) — labelled `input to the D-16 gate`, and never as a decision (DATA-08, charter §13 M1 (5), ADR-0005, F-60..F-62)."
    - "The report records the Tg window (56,064 non-null; 52,753 inside 100–900 K; 3,311 outside) and the `tg_rmse` ladder on the 43,560 in-scope triple rows (≤ 0.05 → 36,592; ≤ 0.1 → 42,232; ≤ 0.2 → 43,316; ≤ 0.5 → 43,521; ≤ 1.0 → 43,545) with no cut applied (DATA-09, R-2, F-64, F-65)."
    - "D-8 and A-7 run as tests inside the gate — `test_d8_no_row_level_split_code_under_llm4pol_data`, `test_a7_validator_and_loader_never_serve_static_dielectric_const` — plus `test_maxwell_identity_holds_on_dielectric_rows`, and the D-10 test from plan 02-01 is extended to the committed report; `docs/governance/INVARIANTS.md` names them in the D-8 row and in a new A-7 row (D-7 and D-10 were promoted by 02-01, D-9 by 02-04) (DATA-10, charter §13 M1 (6))."
    - "The report's Tg section and the plan summary flag, without resolving, the wording discrepancy between REQUIREMENTS.md DATA-09 (`filtered by tg_rmse`) and CONTEXT R-2 (no cut in M1; ladder recorded) for the owner (ADR-0002 precedence)."
    - "`docs/research/databases/README.md` carries a dated correction note (2026-09-22) that points at the validator report and corrects the `broken column` narrative without rewriting the 2026-09-11 text (R-3)."
    - "The seven-step gate is green locally and in a `workflow_dispatch` CI run on windows-latest and ubuntu-latest where the real-file tests skip with their stated reason; `02-EVIDENCE.md` pairs each charter §13 M1 exit criterion (1)–(6) with the command and its verbatim output (charter §9; Phase 1 D-04/D-05 convention)."
  artifacts:
    - path: "src/llm4pol/data/filters.py"
      provides: "readme_triple_ladder, alternative_eps_filters, check_tc_mask, candidate_level_triple (filter-then-median), tg_window_counts, tg_rmse_ladder_counts, feasible_set, static_maxwell_violation_share, dielectric_identity_residual, spearman_table, readme_window; DEV_DEFAULT_TG_MIN_K = 400.0 and DEV_DEFAULT_EPS_QUANTILE = 0.25 cited from charter §13"
      contains: "def feasible_set("
    - path: "src/llm4pol/data/validate.py"
      provides: "sections Coverage per registry property, Physical-range filter counts, Dielectric columns: identity and Maxwell check, README triple ladder, Tg window and tg_rmse ladder, Feasible set under the development defaults (input to the D-16 gate); EXPECTED_POLYOMICS complete"
      contains: "input to the D-16 gate"
    - path: "docs/audit/polyomics-041e5834-validation.md"
      provides: "The committed number authority for the pinned snapshot"
      contains: "041e5834ea1a48682fae12dc39ccd723bcd4f771"
    - path: "docs/research/databases/README.md"
      provides: "Dated correction note pointing at the validator report"
      contains: "Correction (2026-09-22"
    - path: "docs/governance/INVARIANTS.md"
      provides: "D-8 guard cell and an A-7 row naming the tests (D-7, D-10 cells from 02-01 and D-9 from 02-04 untouched)"
      contains: "test_d8_no_row_level_split_code_under_llm4pol_data"
    - path: "tests/test_data_invariants.py"
      provides: "D-8, A-7, Maxwell and committed-report tests, the D-10 test extended to the committed report, plus the synthetic ladder / tg / feasible-set / section-order tests"
      contains: "test_d8_no_row_level_split_code_under_llm4pol_data"
    - path: "tests/test_data_real_file.py"
      provides: "Real-file reproductions of every charter §13 M1 number and the byte-identical committed report"
      contains: "test_real_validate_exits_0_and_committed_report_is_byte_identical_to_a_fresh_run"
    - path: ".planning/phases/02-data-foundation/02-EVIDENCE.md"
      provides: "Command-and-output evidence for charter §13 M1 exit criteria (1)–(6), the CI run id and both job conclusions"
      contains: "CI run id:"
  key_links:
    - from: "src/llm4pol/data/validate.py"
      to: "src/llm4pol/data/filters.py"
      via: "every ladder, window and feasible-set count is a pure function of (rows, candidates, registry) in filters.py; validate.py only names populations and renders"
      pattern: "readme_triple_ladder"
    - from: "src/llm4pol/data/filters.py"
      to: "src/llm4pol/data/registry.py"
      via: "bounds and the check_tc filter come from PropertySpec.physical_range / .check_tc / .tg_rmse_ladder"
      pattern: "tg_rmse_ladder"
    - from: "docs/audit/polyomics-041e5834-validation.md"
      to: "src/llm4pol/data/validate.py"
      via: "test_real_validate_exits_0_and_committed_report_is_byte_identical_to_a_fresh_run regenerates the report into tmp and compares bytes"
      pattern: "## Findings"
    - from: "docs/research/databases/README.md"
      to: "docs/audit/polyomics-041e5834-validation.md"
      via: "the correction note cites the report path as the number authority"
      pattern: "polyomics-041e5834-validation.md"
    - from: ".planning/phases/02-data-foundation/02-EVIDENCE.md"
      to: ".github/workflows/ci.yml"
      via: "the evidence cites the workflow_dispatch run id and both matrix-job conclusions with the skipped real-file tests"
      pattern: "check \\(ubuntu-latest\\): success"
  prohibitions:
    - statement: "No task searches for a filter that yields 88.9 % or treats ε_dc ≥ n² as an independent physics check (Pitfall 1); the two are documented differences with the observed values pinned."
      status: unverified
      verification: null
    - statement: "The report never writes a threshold as a decision: the feasible-set section is labelled `input to the D-16 gate` and the phrase `threshold =` does not appear in it (ADR-0005)."
      status: unverified
      verification: null
    - statement: "The 2026-09-11 text of `docs/research/databases/README.md` is not rewritten; the correction is an appended, dated note (R-3)."
      status: unverified
      verification: null
    - statement: "No push happens before the local seven-step gate prints `SUMMARY: 7/7 steps passed` and the history scan reports zero hits (R-3, R-4)."
      status: unverified
      verification: null
---

<objective>
Complete the validator and commit the number authority. `filters.py` gains the README triple ladder
and its alternatives, the `check_tc` and candidate-level counts, the Tg window and `tg_rmse` ladder, the
dielectric identity and Maxwell shares, the rank correlations, the README window and the feasible set
under the development defaults; `validate.py` renders them as named-population sections and completes
`EXPECTED_POLYOMICS` with `reproduce` and `documented` findings (R-5). D-8 and A-7 become tests, the
D-10 test from 02-01 is extended to the committed report, and INVARIANTS.md says so. The report is
generated on the pinned file and committed (Task 3), the research README gets its dated correction
(Task 3), and — in a separate task so a red CI run never forces the report steps to be redone — CI is
dispatched on both platforms and `02-EVIDENCE.md` records the reproduction of charter §13 M1 exit
criteria (1)–(6) (Task 4). The wording discrepancy between REQUIREMENTS.md DATA-09 ("filtered by
`tg_rmse`") and CONTEXT R-2 (no cut in M1) is flagged in the report and the summary for the owner; it is
not resolved here.

Purpose: charter §10 makes `docs/audit/polyomics-<rev>-validation.md` the authority for every number
cited later, and §13 M1 (2)–(3), (5), (6) are exactly the reproductions this plan makes commands.

Output: extended `filters.py` and `validate.py`; extended `tests/test_data_invariants.py` and
`tests/test_data_real_file.py`; `docs/audit/polyomics-041e5834-validation.md` (committed) and its
index row in `docs/audit/README.md`; the correction note in `docs/research/databases/README.md`; the
D-8 row and a new A-7 row in `docs/governance/INVARIANTS.md`; `02-EVIDENCE.md`.

Grounding facts (RESEARCH, all measured on the pinned bytes; do not re-derive): F-08 the ladder on
all rows 95,335 → TC non-null 81,405 → ∧ ε_dc non-null 79,929 → ∧ ε_dc ∈ [1, 20] → ∧ Tg ∈ [100, 900]
= 43,561; alternatives ε_dc non-null only 45,821, ≤ 10 → 42,186, ≤ 50 → 45,134, ≤ 100 → 45,704; F-09
`tg_rmse` plays no part in 43,561; F-10 all 43,561 carry `refractive_index`; F-11 Spearman on the
43,561 rows TC–ε_dc 0.170, TC–Tg 0.010, ε_dc–Tg 0.199, `sp_ced`–TC 0.355, ffv–TC −0.069, Rg–TC 0.304,
density–TC −0.270; F-12 window (top-decile TC ∧ bottom-quartile ε_dc on the 43,561) 1,140 rows =
2.62 %; F-13 static Maxwell violation (`static < n²`, `<` and `≤` agree) 88.83 % = 38,693 / 43,561 on
the triple, 84.30 % = 78,807 / 93,488 on all rows with both columns; F-14 identity `ε_dc = ε_s − 1 +
n²` on all 93,488 rows, max residual 5.7e-7; F-15 min `static` 1.00016 so `ε_dc ≥ n²` everywhere is
an identity; F-17 Spearman static vs n² 0.003 (all) / −0.144 (triple), README's −0.05 not reproduced;
F-18 ε_dc vs n² 0.515 (all) / 0.476 (triple); F-19 73,045 matches no column (unique smiles 78,379;
`monomer_ID` 78,336; `UUID` 95,335); F-38 tacticity none 48,790 / atactic 45,032 / isotactic 951 /
syndiotactic 8 / NaN 554; F-39 multi-tacticity `smiles_list`: 2 without unknown, 303 with; F-59
`check_tc` True 79,927 / False 15,376 / None 32; triple ∧ `check_tc` 42,733 (42,732 in scope); TC >
1 rows 109 of which 108 `check_tc == False`; F-60 ε_dc Q25: triple rows 2.6427, candidates 2.6524;
F-61 candidate-level triple filter-then-median 40,212 (median-then-filter 40,426 — printed as the
alternative, not used); F-62 feasible: 6,793 of 40,212 candidates (16.89 %), 7,317 of 43,560 rows
(16.80 %); F-63 Tg medians 488.5 K all rows, 472.2 K triple rows; F-64 Tg 56,064 non-null, 52,753 inside,
3,311 outside; F-65 ladder on 43,560 in-scope triple rows ≤ 0.05 → 36,592, ≤ 0.1 → 42,232, ≤ 0.2 →
43,316, ≤ 0.5 → 43,521, ≤ 1.0 → 43,545; F-23 `tg_rmse` median 0.029, p95 0.099, p99 0.236, max 36.3;
F-67 ε_dc > 20 on 5,123 rows, > 10 on 7,742; coverage on all source rows TC 81,405, ε_dc 93,488, Tg
56,064, ffv 87,849, `sp_ced` 71,848, Rg 95,335, density 95,335, `refractive_index` 93,488 (CONTEXT
table). Synthetic-fixture expectations: all-rows ladder 14 → 13 → 13 → 12 → 10; in-scope triple 8;
∧ `check_tc` 8; candidate-level triple 5; Tg non-null 13 / inside 12 / outside 1; `tg_rmse` ladder on
the 8 in-scope triple rows 5 / 7 / 8 / 8 / 8; ε_dc candidate medians on the 5 triple candidates
2.33, 2.34, 2.20, 2.25, 2.61 → Q25 = 2.25 → feasible (ε ≤ 2.25 ∧ Tg ≥ 400) = 2 of 5 (40.00 %); identity
residual 0 and every `static` ≥ 1.05 by construction. In this plan "the barred column" means the
uncorrected static permittivity column A-7 keeps out of the registry; test code builds its name from
fragments (`"static_" + "dielectric_const"`). Workstation guard (Phase 1): a hook refuses Bash
commands containing git's `-n` short flag — use `--max-count`; write commit messages to a scratchpad
file and commit with `-F`.
</objective>

<execution_context>
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-foundation/02-CONTEXT.md
@.planning/phases/02-data-foundation/02-RESEARCH.md
@.planning/phases/02-data-foundation/02-03-SUMMARY.md
@.planning/phases/02-data-foundation/02-04-SUMMARY.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@CLAUDE.md
@docs/MASTER-PLAN.md
@docs/governance/INVARIANTS.md
@docs/governance/ADR/0004-problem-definition-and-hidden-table.md
@docs/governance/ADR/0005-staged-charter-approval.md
@docs/research/databases/README.md
@docs/audit/README.md
@src/llm4pol/data/validate.py
@src/llm4pol/data/filters.py
@src/llm4pol/data/replicates.py
@src/llm4pol/data/report.py
@src/llm4pol/data/registry.py
@.planning/REQUIREMENTS.md
@tests/conftest.py
@tests/test_data_invariants.py
@tests/test_data_real_file.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The README numbers — triple ladder, check_tc, candidate-level triple, dielectric identity and Maxwell shares, coverage, ranges, correlations, 73,045, tacticity twins (D-07, R-1, R-3, R-5, DATA-06)</name>
  <read_first>
    - `src/llm4pol/data/filters.py` (`readme_triple_mask`), `validate.py`, `report.py` (`Expected`, `evaluate_finding`, `CountTable`), `replicates.py`, `registry.py`, `load.py` (`build_candidates`) — as left by 02-03 and 02-04
    - `.planning/phases/02-data-foundation/02-CONTEXT.md` — D-07 (section list), R-1 (print with-twins and folded counts), R-2 (both triples side by side), R-3 (narrative correction: identity, 88.83 %), R-5 (classes and tolerances: Maxwell share 88.83 ± 0.05, identity residual < 1e-5), § Specific Ideas (numbers to reproduce; correlations optional confirmation)
    - `.planning/phases/02-data-foundation/02-RESEARCH.md` — F-06..F-19, F-38, F-39, F-59, F-61, F-67, Q2, Pattern 5, Pitfall 1 (never chase 88.9 %), Pitfall 2 (three triple counts), Pitfall 3 (303 twins), Pitfall 5 (`check_tc`), Code Examples (Maxwell identity)
    - `docs/governance/ADR/0004-problem-definition-and-hidden-table.md` — Decision 2 and the Consequences bullet about the loader and a physics consistency check (the report's dielectric section is where that check lives; A-7 stands, the explanation changes per R-3)
  </read_first>
  <behavior>
    - Test 1 `test_readme_ladder_counts_on_synthetic` (tests/test_data_invariants.py): `filters.readme_triple_ladder(source_rows, registry)` returns an ordered list of `(label, count)` with counts `[14, 13, 13, 12, 10]` for labels `all rows`, `thermal_conductivity non-null`, `∧ dielectric_const_dc non-null`, `∧ dielectric_const_dc in [1.0, 20.0]`, `∧ tg in [100.0, 900.0] K`; `filters.check_tc_mask(rows, registry)` on the 12 in-scope rows ∧ triple gives 8; `filters.candidate_level_triple(rows, registry)` returns a candidate frame with 5 rows (filter-then-median) and `filters.candidate_level_triple(rows, registry, order="median_then_filter")` returns 5 as well on the fixture (both printed; the report uses filter-then-median).
    - Test 2 `test_alternative_eps_filters_are_printed_not_used`: `filters.alternative_eps_filters(source_rows, registry)` returns the counts for `non-null only`, `<= 10`, `<= 50`, `<= 100`, each applied on all 14 source rows together with TC non-null ∧ Tg ∈ [100, 900] (11 rows: u01–u08, u09, u12, u13 — u10 and u14 fail the Tg window, u11 has no TC); ε_dc non-null keeps all 11; `<= 10` drops u09 (25.0) → 10; `<= 50` and `<= 100` keep all 11; assert `[11, 10, 11, 11]`.
    - Test 3 `test_maxwell_identity_holds_on_dielectric_rows` (parametrised over the synthetic rows and, when present, the real rows): `filters.dielectric_identity_residual(rows)` max is `< 1e-9` on the fixture and `< 1e-5` on the pinned file; `filters.static_minimum(rows)` is `>= 1.0` on both (1.00016 ± 1e-4 on the pinned file); `filters.static_maxwell_violation_share(rows, mask)` on the fixture's all-rows triple (10 rows) is `100.0` (every synthetic `static` is below `n²` except u09, which is not in the triple) and the ε_dc violation count (`ε_dc < n²`) is 0 on both.
    - Test 4 `test_multi_tacticity_counts_on_synthetic`: `filters.multi_tacticity_counts(source_rows)` returns `by_smiles_with_unknown == 2`, `by_smiles_without_unknown == 1`, `unknown_twins == 1`; `filters.multi_tacticity_counts_canonical(rows)` on the in-scope parquet rows returns `2` and `1` as well (R-1: both printed).
    - Test 5 `test_card_count_73045_matches_no_column_on_synthetic`: `filters.card_count_candidates(source_rows, rows)` returns a mapping `{"unique smiles_list": 10, "unique canonical_psmiles (in scope)": 7, "unique UUID": 14, "source rows": 14}` and `73045 not in values` — the same function on the pinned file returns 78,379 / 78,373 / 95,335 / 95,335 with `monomer_ID` 78,336 added when the column exists.
    - Test 6 `test_findings_documented_class_uses_tolerance_and_reproduce_is_exact` (unit test of `report.evaluate_finding`): `Expected("reproduce", 43561)` with observed 43561 → `reproduced`, with 43560 → `FAILED`; `Expected("documented", 88.9, tolerance=0.05, note="README rounds")` with observed 88.83 → `FAILED` (88.9 − 88.83 = 0.07 > 0.05) — so the plan pins the documented class as *observed-value pinned*: `Expected("documented", 88.83, tolerance=0.05, note="README states 88.9 %")` with observed 88.83 → `documented`, with 88.75 → `FAILED`; a non-numeric `Expected("documented", "not reproducible from any column")` with observed `"no column equals 73,045"` → `documented`; observed `None` → `MISSING`; `report.exit_code` is 1 when any `FAILED`/`MISSING` exists.
    - Test 7 `test_real_readme_numbers_reproduce_or_are_documented` (tests/test_data_real_file.py, skip without the file): `validate.run(REPO_ROOT, processed_dir=real_load.processed_dir, report_path=tmp)` returns 0 and the findings table contains, with status `reproduced`: `readme_triple_all_rows` 43,561, `readme_triple_in_scope` 43,560, `triple_check_tc_all_rows` 42,733, `candidate_triple_filter_then_median` 40,212, `eps_alternative_non_null_only` 45,821, `eps_alternative_le_10` 42,186, `eps_alternative_le_50` 45,134, `eps_alternative_le_100` 45,704, `coverage_thermal_conductivity` 81,405, `coverage_dielectric_const_dc` 93,488, `coverage_tg` 56,064, `coverage_fractional_free_volume` 87,849, `coverage_sp_ced` 71,848, `coverage_refractive_index` 93,488, `coverage_density` 95,335, `coverage_Rg` 95,335, `tacticity_none` 48,790, `tacticity_atactic` 45,032, `tacticity_isotactic` 951, `tacticity_syndiotactic` 8, `tacticity_unknown` 554, `multi_tacticity_smiles_with_unknown` 303, `multi_tacticity_smiles_without_unknown` 2, `raw_string_merges` 5, `eps_outside_physical_range` 5,123, `dc_maxwell_violations` 0, `check_tc_true` 79,927, `check_tc_false` 15,376; and with status `documented`: `static_maxwell_violation_pct_triple` 88.83 (tolerance 0.05; note names the README's 88.9 %), `static_maxwell_violation_pct_all` 84.30 (0.05), `dc_identity_max_residual` 5.7e-7 (tolerance 1e-5, note: identity `ε_dc = ε_s − 1 + n²`, F-14, so "0 violations" is algebra), `static_minimum` 1.00016 (1e-4), `spearman_tc_eps` 0.170, `spearman_tc_tg` 0.010, `spearman_eps_tg` 0.199, `spearman_sp_ced_tc` 0.355, `spearman_ffv_tc` −0.069, `spearman_rg_tc` 0.304, `spearman_density_tc` −0.270 (all tolerance 0.001, population `README-triple rows (all source rows)`), `spearman_static_vs_n2_triple` −0.144 (0.01; note: README's −0.05 not reproduced), `spearman_eps_vs_n2_triple` 0.476 (0.01), `readme_window_rows` 1,140 (tolerance 2), `readme_window_pct` 2.62 (0.01), `multi_tacticity_canonical_with_unknown` 303 (tolerance 5), `card_count_73045` text (documented); and the report contains the paragraph beginning `The dataset card's 73,045` and the sentence containing `the corrected static dielectric constant` (R-3 wording).
  </behavior>
  <files>src/llm4pol/data/filters.py, src/llm4pol/data/validate.py, tests/test_data_invariants.py, tests/test_data_real_file.py</files>
  <action>
    STEP 1 — RED: append Tests 1–6 to `tests/test_data_invariants.py` and Test 7 to
    `tests/test_data_real_file.py`; run → RED on the missing functions; commit `test(02-05): add
    failing README-number ladder, dielectric identity and finding-class tests (D-07, R-3, R-5)`.

    STEP 2 — `filters.py` (pure functions; every bound from the registry; no numeric literal except
    the alternative-filter rungs `10`, `50`, `100` and the quantile constants below, each with a
    comment citing its F-fact or charter row): `readme_triple_ladder(source_rows, registry) ->
    list[tuple[str, int]]` (the five steps of Test 1 on ALL source rows — the README's population —
    with labels naming the registry bounds); `alternative_eps_filters(source_rows, registry) ->
    list[tuple[str, int]]` (`non-null only`, `<= 10`, `<= 50`, `<= 100`, each ∧ TC non-null ∧ Tg in
    range; F-08); `check_tc_mask(df, registry)` (uses `PropertySpec.check_tc` of the objective; True
    means keep `check_tc == True`); `candidate_level_triple(rows, registry, *, order="filter_then_median")`
    → candidate frame built with `load.build_candidates` on the masked rows, or (`median_then_filter`)
    `build_candidates` on all rows then the mask applied to `<column>_median`; `multi_tacticity_counts
    (source_rows)` grouping `smiles_list` with tacticity mapped through `normalise_tacticity` (F-48:
    never a default `groupby` on NaN keys) → `by_smiles_with_unknown`, `by_smiles_without_unknown`,
    `unknown_twins`; `multi_tacticity_counts_canonical(rows)` on `canonical_psmiles`;
    `raw_string_merges(source_rows, rows)` = unique in-scope `smiles_list` − unique `canonical_psmiles`;
    `card_count_candidates(source_rows, rows) -> dict[str, int]`; `dielectric_identity_residual(df) ->
    Series` = `|ε_dc − (static − 1 + n²)|` over rows with all three columns; `static_minimum(df)`;
    `static_maxwell_violation_share(df, mask) -> tuple[int, int, float]` (violations `static < n²`,
    population size, percentage with two decimals); `dc_maxwell_violations(df, mask) -> int` (`ε_dc <
    n²`); `spearman_table(df, mask, pairs)` via `DataFrame.corr(method="spearman")` on the masked rows
    (pandas only — no additional scientific library enters the typed package); `readme_window(df, mask, registry) -> tuple[int, float]` (rows
    with TC ≥ its 0.9 quantile ∧ ε_dc ≤ its 0.25 quantile within the mask; F-12); `coverage(source_rows,
    registry) -> dict[str, int]`; `physical_range_counts(source_rows, registry) -> dict[str,
    tuple[int, int, int]]` (inside, outside, missing); `tacticity_counts(source_rows) -> dict[str, int]`
    with NaN → `unknown`.

    STEP 3 — `validate.py`: add, in this order after `Scope exclusions and identity`, the sections
    `Coverage per registry property` (table `| property | population | non-null | share |`, population
    `all source rows`; plus the tacticity table `| tacticity | population | rows |`), `Physical-range
    filter counts` (table `| property | population | range | inside | outside | missing |`, population
    `all source rows`; note that only `dielectric_const_dc` and `tg` ranges enter the README ladder),
    `Dielectric columns: identity and Maxwell check` (population `rows with dielectric_const_dc and
    refractive_index`: identity max residual, median residual, `static` minimum; the static Maxwell
    share on `README-triple rows` and on all rows; `dc_maxwell_violations` 0 with the sentence that it
    follows algebraically from the identity and `static ≥ 1` (F-15); the two Spearman values vs n²;
    a paragraph in R-3's words: the uncorrected static permittivity column is the orientational
    permittivity of non-polarisable MD, `dielectric_const_dc` adds the electronic part `n² − 1` and is
    the paper's *corrected static dielectric constant* (Table S3, F-14); A-7 stands — the registry
    serves `dielectric_const_dc` only; ADR-0004's consequence stands while its "broken column" wording
    is superseded by this section), `README triple ladder` (table `| step | population | rows |
    expected | status |` with the five steps, then `in-scope subset`, `∧ check_tc == True (protocol
    filter, not in the README)`, `in-scope ∧ check_tc`, `candidates (filter then median)` and the
    alternative order; then the alternative ε_dc filters table; then the Spearman table `| pair |
    population | spearman |` and the window row). Extend the identity section with the multi-tacticity
    rows (by `smiles_list` with/without unknown — populations `all source rows` — by canonical, unknown
    twins with R-1's sentence that the twins are a separate candidate in M1 and are raised for the
    owner before Phase 3) and `raw_string_merges`. Extend the `Source shape` section's 73,045 paragraph
    to start with `The dataset card's 73,045` and to list `card_count_candidates` in a table `| column
    | population | unique values |`, stating that none equals 73,045 and that the closest explanation
    (paper Table S2 per-class unique-structure sum) is an assumption (A1). Add every finding of Test 7
    to `EXPECTED_POLYOMICS` with the classes, values, tolerances and notes listed there — `documented`
    findings pin the *observed* research value (88.83, 84.30, 5.7e-7, 1.00016, …) with the README's
    figure in the note, so a further drift fails (R-5). Print `%` values with two decimals, Spearman
    with three, residuals in scientific notation with two significant digits.

    STEP 4 — GREEN: `pixi run --manifest-path env/pixi.toml python -m pytest
    tests/test_data_invariants.py tests/test_data_real_file.py -v` → all PASSED locally; `python -m
    llm4pol.data validate` exits 0 with `VALIDATION: … 0 failed`; gate `7/7`; commit `feat(02-05):
    reproduce the README numbers — triple ladder 43,561, check_tc 42,733, candidates 40,212; document
    88.83 %, the ε_dc identity and 73,045 (D-07, R-1, R-3, R-5, DATA-06)`. If a `reproduce` finding
    fails on the pinned file, check the population against the F-fact (Pitfall 2 names the three
    triple counts) before touching anything; a corrected population is a code fix, a changed expected
    value is forbidden without the F-fact that supports it, written into the summary.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_invariants.py -v -k "ladder or alternative or maxwell or tacticity or 73045 or findings_documented"</automated>
    <fails_when>exit code is non-zero, or any of `test_readme_ladder_counts_on_synthetic`, `test_alternative_eps_filters_are_printed_not_used`, `test_maxwell_identity_holds_on_dielectric_rows`, `test_multi_tacticity_counts_on_synthetic`, `test_card_count_73045_matches_no_column_on_synthetic`, `test_findings_documented_class_uses_tolerance_and_reproduce_is_exact` is not listed as PASSED</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_real_file.py -v -k readme_numbers</automated>
    <fails_when>exit code is non-zero or `test_real_readme_numbers_reproduce_or_are_documented` is not listed as PASSED on this machine</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -m llm4pol.data validate</automated>
    <fails_when>exit code is non-zero, or stdout lacks `finding readme_triple_all_rows: reproduce expected=43,561 observed=43,561 reproduced`, or lacks `finding static_maxwell_violation_pct_triple: documented expected=88.83 observed=88.83 documented`, or lacks `finding dc_maxwell_violations: reproduce expected=0 observed=0 reproduced`, or the `VALIDATION:` line reports a non-zero failed count</fails_when>
    <automated>grep -c "scipy" src/llm4pol/data/filters.py src/llm4pol/data/validate.py</automated>
    <fails_when>either count is not 0 — an unstubbed import entered the typed package; correlations use pandas</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits: `test(02-05): add failing README-number ladder, dielectric identity and finding-class tests (D-07, R-3, R-5)` then `feat(02-05): reproduce the README numbers — triple ladder 43,561, check_tc 42,733, candidates 40,212; document 88.83 %, the ε_dc identity and 73,045 (D-07, R-1, R-3, R-5, DATA-06)`.
    - On the pinned file the findings listed in Test 7 all carry status `reproduced` or `documented`; in particular `readme_triple_all_rows` 43,561, `readme_triple_in_scope` 43,560, `triple_check_tc_all_rows` 42,733, `candidate_triple_filter_then_median` 40,212, `static_maxwell_violation_pct_triple` 88.83, `dc_identity_max_residual` ≤ 1e-5, `dc_maxwell_violations` 0, `multi_tacticity_smiles_with_unknown` 303, `multi_tacticity_smiles_without_unknown` 2 (F-08, F-13, F-14, F-39, F-59, F-61).
    - The report's README ladder table lists the five steps with the exact bounds read from the registry (`[1.0, 20.0]`, `[100.0, 900.0]`) and the row `∧ check_tc == True (protocol filter, not in the README)`; the dielectric section contains `corrected static dielectric constant` and states that the registry serves `dielectric_const_dc` only; the 73,045 paragraph begins `The dataset card's 73,045`.
    - `filters.py` imports pandas only (the `scipy` grep in the verify block prints 0) and carries no range literal for ε_dc or Tg; the README's rounded Maxwell figure lives only in a finding note in `validate.py`, never in `filters.py`.
    - The gate prints `SUMMARY: 7/7 steps passed`.
  </acceptance_criteria>
  <done>Charter §13 M1 exit criteria (2) and (3) are reproducible by command: the README's 43,561 is reproduced under the exact filter the README never stated, the two dielectric claims are documented as 88.83 % and as an algebraic identity with the corrected narrative, 73,045 is resolved as the dataset card's text, and every count in the ladder names its population.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tg window and tg_rmse ladder, feasible set as a D-16 input, and the D-8 / D-10 / A-7 guards (D-07, D-08, DATA-08, DATA-09, DATA-10)</name>
  <read_first>
    - `src/llm4pol/data/filters.py`, `validate.py` (after Task 1), `registry.py` (`tg_rmse_ladder`, `tg_rmse_unit`), `report.py` (`CountTable` header rule from 02-01 — read, not edited), `schema.py` (`PROPERTY_COLUMNS`); `tests/test_data_invariants.py` — the existing `test_d7_…` (02-01, extended by 02-04), `test_d9_…` (02-04) and `test_d10_…` (02-01) this task must keep green and extend
    - `.planning/REQUIREMENTS.md` DATA-09 ("Tg rows are filtered by `tg_rmse` and to 100–900 K; the filter counts are recorded") versus 02-CONTEXT.md R-2 (no `tg_rmse` cut in M1; ladder recorded) — the wording discrepancy this task flags in the report and the summary (W-3); precedence per ADR-0002 makes it an owner call, not a planner call
    - `pyproject.toml` `[[tool.importlinter.contracts]]` (the `sklearn` entry of the data contract, from 02-01)
    - `.planning/phases/02-data-foundation/02-CONTEXT.md` — D-07 (feasible-set section stated as an input to the D-16 gate; `tg_rmse` / 100–900 K counts), D-08 (D-8: assert no splitter import under `llm4pol.data`; D-10: every count names its population; A-7: registry has no barred column and the loader never selects it as a property; Maxwell-consistency test), R-2
    - `.planning/phases/02-data-foundation/02-RESEARCH.md` — F-23, F-60..F-66, Anti-Patterns (fixing D-16 in the report), § Validation Architecture (D-8 by AST or grep of source; D-10 by parsing the markdown)
    - `docs/MASTER-PLAN.md` §13 gate-parameter table (D-16 development defaults: constrained single; ε ≤ Q25; Tg ≥ 400 K; fixed at M6 pre-registration) and §4 A-7; `docs/governance/ADR/0005-staged-charter-approval.md` (gated parameters stay gated)
    - `docs/governance/INVARIANTS.md` — rows D-8, D-10; the preamble sentence about A-1..A-7 being declared in the charter (the new A-7 row goes in a new table below the data table, not in the charter)
  </read_first>
  <behavior>
    - Test 1 `test_tg_window_and_tg_rmse_ladder_on_synthetic`: `filters.tg_window_counts(source_rows, registry)` → `(13, 12, 1)` (non-null, inside, outside); `filters.tg_rmse_ladder_counts(rows, mask, registry)` on the 8 in-scope triple rows → `[(0.05, 5), (0.1, 7), (0.2, 8), (0.5, 8), (1.0, 8)]` with rungs read from `registry.properties["tg"].tg_rmse_ladder`; a registry with ladder `(0.06,)` (built with `dataclasses.replace`) yields `[(0.06, 6)]`; the synthetic report's Tg section contains the flag sentence beginning `Wording discrepancy raised for the owner: REQUIREMENTS.md DATA-09 reads "filtered by tg_rmse"` (see STEP 2).
    - Test 2 `test_feasible_set_on_synthetic_is_labelled_as_d16_input`: `filters.feasible_set(candidate_triple, registry)` returns `FeasibleSet(q25_eps=2.25, tg_min_k=400.0, n_feasible=2, n_population=5, pct=40.0)`; `filters.DEV_DEFAULT_TG_MIN_K == 400.0` and `filters.DEV_DEFAULT_EPS_QUANTILE == 0.25`; the synthetic report's feasible section contains `input to the D-16 gate`, `development default`, `| feasible candidates (eps_dc_median <= Q25 and tg_median >= 400.0 K) | candidate-level README triple (filter then median) | 2 |` and `Q25 of dielectric_const_dc candidate medians | candidate-level README triple (filter then median) | 2.2500`, and the section text does not contain the phrase `threshold =`.
    - Test 3 `test_d8_no_row_level_split_code_under_llm4pol_data`: parsing every `src/llm4pol/data/*.py` with `ast`, no `Import`/`ImportFrom` names a module starting with `sklearn`, and no `Name`/`Attribute` identifier contains `train_test_split`, `KFold`, `model_selection` or `ShuffleSplit`; `pyproject.toml`'s `[tool.importlinter]` contract with `source_modules == ["llm4pol.data"]` lists `sklearn` in `forbidden_modules` (D-8: the guard is both the AST test and the KEPT contract).
    - Test 4 — extend the existing `test_d10_every_count_table_in_report_names_its_population` (introduced by plan 02-01 with `report.CountTable`; keep its assertions: every table of the synthetic report has a `population` header cell with non-empty cells, and `CountTable` refuses a header without `population`) with: the synthetic report now has ≥ 12 tables, and, when `docs/audit/polyomics-041e5834-validation.md` exists, the same table rule holds for the committed report. No second D-10 test is introduced.
    - Test 5 `test_a7_validator_and_loader_never_serve_static_dielectric_const`: the barred name (fragments) is not in `PROPERTY_COLUMNS`, not in `load_registry().columns()`, not a property row in any `| property |` table of the synthetic report; and it IS a column of the rows parquet (kept as data, F-14 needs it) and appears in the report only inside the dielectric section and the findings that name `static_` (the identity and Maxwell rows).
    - Test 6 `test_synthetic_report_has_every_section_in_order`: the synthetic report written by `validate.run(synthetic_root, expected={}, report_path=tmp)` contains the headings in order `## Fetch identity`, `## Source shape`, `## Scope exclusions and identity`, `## Coverage per registry property`, `## Physical-range filter counts`, `## Dielectric columns: identity and Maxwell check`, `## README triple ladder`, `## Tg window and tg_rmse ladder`, `## Replicate structure and noise floor`, `## Feasible set under the development defaults (input to the D-16 gate)`, `## Findings`, and no line of it matches `\*[A-Za-z]` (aggregates only — no SMILES-like token even on the synthetic root). The committed-report test is added in Task 3, when the report exists.
    - Test 7 `test_real_tg_window_ladder_and_feasible_set` (skip without the file): findings `tg_non_null` 56,064, `tg_inside_window` 52,753, `tg_outside_window` 3,311, `tg_rmse_le_0.05` 36,592, `tg_rmse_le_0.1` 42,232, `tg_rmse_le_0.2` 43,316, `tg_rmse_le_0.5` 43,521, `tg_rmse_le_1.0` 43,545, `feasible_candidates_dev_defaults` 6,793, `candidate_triple_filter_then_median` 40,212 → `reproduced`; `feasible_pct_dev_defaults` 16.89 (0.02), `eps_q25_candidates` 2.6524 (0.0005), `eps_q25_triple_rows` 2.6427 (0.0005), `tg_median_all_rows` 488.5 (0.1), `tg_median_triple_rows` 472.2 (0.1), `tg_rmse_median` 0.029 (0.001), `tg_rmse_p95` 0.099 (0.001), `tg_rmse_p99` 0.236 (0.002), `tg_rmse_max` 36.3 (0.1), `candidate_triple_median_then_filter` 40,426 (0), `feasible_rows_dev_defaults` 7,317 (documented: the report prints the row-level count under both the row Q25 and the candidate Q25, and the finding's observed value is whichever equals 7,317, else both) → `documented` (F-60..F-65, F-23, F-63).
  </behavior>
  <files>src/llm4pol/data/filters.py, src/llm4pol/data/validate.py, tests/test_data_invariants.py, tests/test_data_real_file.py, docs/governance/INVARIANTS.md</files>
  <action>
    STEP 1 — RED: append Tests 1–3, 5, 6 to `tests/test_data_invariants.py`, extend `test_d10_…`
    per Test 4, and append Test 7 to `tests/test_data_real_file.py`; run → RED; commit `test(02-05):
    add failing tg-ladder, feasible-set, D-8, A-7 and section-order tests; extend the D-10 test
    (D-08, DATA-08, DATA-09, DATA-10)`. Test names carrying the barred literal (`test_a7_…`) are
    permitted by the W-1 rule shared with plans 02-02 and 02-04: the literal may appear on `def
    test_` lines only; test bodies assemble it from fragments.

    STEP 2 — `filters.py`: `DEV_DEFAULT_TG_MIN_K = 400.0` and `DEV_DEFAULT_EPS_QUANTILE = 0.25` with
    a comment: development defaults from the charter §13 gate-parameter table (D-16 row); they are
    inputs to the D-16 gate fixed at the Phase 7 pre-registration (ADR-0005), cited here, decided
    nowhere in this package; `tg_window_counts(source_rows, registry) -> tuple[int, int, int]`;
    `tg_rmse_ladder_counts(rows, mask, registry) -> list[tuple[float, int]]` (rungs from
    `tg_rmse_ladder`; count of masked rows with `tg_rmse <= rung`); `tg_rmse_descriptives(source_rows)
    -> dict[str, float]` (median, p95, p99, max); frozen dataclass `FeasibleSet(q25_eps, tg_min_k,
    n_feasible, n_population, pct)`; `feasible_set(candidates, registry, *, quantile=
    DEV_DEFAULT_EPS_QUANTILE, tg_min_k=DEV_DEFAULT_TG_MIN_K) -> FeasibleSet` on a candidate frame
    (`<eps column>_median <= Q25` ∧ `<tg column>_median >= tg_min_k`, Q25 = pandas `quantile(0.25)`
    of the eps medians, inclusive comparisons); `feasible_rows(rows, mask, q25, tg_min_k) -> int`
    (row-level count under a given Q25). In `validate.py` add the sections `Tg window and tg_rmse
    ladder` (tables: window `| quantity | population | rows |` on `all source rows`; ladder `| tg_rmse
    <= rung | population | rows |` on `in-scope README-triple rows`; descriptives; the sentence from
    R-2 that no cut is applied in M1 and F-23's unit statement; then the flag paragraph, verbatim
    opening: `Wording discrepancy raised for the owner: REQUIREMENTS.md DATA-09 reads "filtered by
    tg_rmse"; CONTEXT R-2 (a development default recorded with the owner unavailable) applies no
    tg_rmse cut in M1 and records the ladder instead. This report records the count at every rung so
    the numbers satisfy either reading; which reading holds is an owner decision (precedence: charter
    > REQUIREMENTS.md > phase context, ADR-0002). Nothing is resolved here.`) and `Feasible set under the development
    defaults (input to the D-16 gate)` (table on population `candidate-level README triple (filter
    then median)`: Q25 of ε_dc candidate medians, Tg minimum, feasible candidates, population size,
    percentage; a second table on `in-scope README-triple rows`: row Q25, feasible rows under the row
    Q25, feasible rows under the candidate Q25; the fixed sentence: `These thresholds are the charter's
    development defaults (§13, D-16 row). This section is an input to the D-16 gate, which is fixed at
    the Phase 7 pre-registration (ADR-0005); nothing here is a decision.`). Add the Test 7 findings to
    `EXPECTED_POLYOMICS` with the stated classes and tolerances. (`report.CountTable` already refuses
    a header without `population` — plan 02-01 added that guard with the type; do not edit
    `report.py` here.)

    STEP 3 — INVARIANTS.md: replace only the Guard cell of D-8 with: no split code exists in
    `llm4pol.data`; `[tool.importlinter]` contract forbidding `sklearn` from `llm4pol.data`
    (`import-linter` step); `tests/test_data_invariants.py::test_d8_no_row_level_split_code_under_
    llm4pol_data`; sampling in later phases is at candidate level after `build_candidates` (charter §9).
    Do not touch the D-7, D-9 and D-10 cells (promoted by plans 02-01 and 02-04; the extended D-10 test
    keeps its name, so the D-10 cell stays correct).
    Append, after the data table and before `## Promotion policy`, a new section `## Architecture
    (charter §4) — guards landed` with a three-column table and one row: `| A-7 | The uncorrected
    static permittivity column is not a registry property; `dielectric_const_dc` only (ADR-0004) |
    `protocol/schemas/property-registry.json` propertyNames enum; `scripts/check.py` schema-inventory
    step; `tests/test_data_registry.py::test_a7_static_dielectric_const_is_rejected_by_the_registry_
    schema`; `tests/test_data_registry.py::test_load_registry_rejects_an_instance_with_the_barred_key`;
    `tests/test_data_invariants.py::test_a7_validator_and_loader_never_serve_static_dielectric_const` |`
    with two sentences above the table: rows are added here when a charter §4 guard lands; the charter
    text is not edited; test names in this table carry the barred column's name by design (so the row,
    the evidence file and `-v` output can cite them) while every grep gate on test bodies excludes
    `def test_` lines. Leave every other line byte-identical.

    STEP 4 — GREEN: invariants and real-file tests PASS; `python -m llm4pol.data validate` exits 0;
    gate `7/7`; commit `feat(02-05): record the Tg window, the tg_rmse ladder and the feasible set as
    a D-16 input; flag the DATA-09 wording; promote D-8 and A-7 to tests (D-07, D-08, DATA-08,
    DATA-09, DATA-10)`.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_invariants.py -v</automated>
    <fails_when>exit code is non-zero, or any of `test_tg_window_and_tg_rmse_ladder_on_synthetic`, `test_feasible_set_on_synthetic_is_labelled_as_d16_input`, `test_d8_no_row_level_split_code_under_llm4pol_data`, `test_d10_every_count_table_in_report_names_its_population`, `test_a7_validator_and_loader_never_serve_static_dielectric_const`, `test_synthetic_report_has_every_section_in_order` is not listed as PASSED</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_real_file.py -v -k "tg_window_ladder_and_feasible"</automated>
    <fails_when>exit code is non-zero or `test_real_tg_window_ladder_and_feasible_set` is not listed as PASSED on this machine</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -m llm4pol.data validate</automated>
    <fails_when>exit code is non-zero, or stdout lacks `finding feasible_candidates_dev_defaults: reproduce expected=6,793 observed=6,793 reproduced`, or lacks `finding tg_rmse_le_0.1: reproduce expected=42,232 observed=42,232 reproduced`, or the `VALIDATION:` line reports a non-zero failed count</fails_when>
    <automated>sed -n '/^| D-8 /p' docs/governance/INVARIANTS.md | grep -c "test_d8_no_row_level_split_code_under_llm4pol_data"</automated>
    <fails_when>prints 0 — D-8 still has a prose guard</fails_when>
    <automated>sed -n '/^| D-10 /p' docs/governance/INVARIANTS.md | grep -c "test_d10_every_count_table_in_report_names_its_population"</automated>
    <fails_when>prints 0 — the D-10 cell promoted by plan 02-01 was damaged by this plan's edit</fails_when>
    <automated>grep -c "Wording discrepancy raised for the owner: REQUIREMENTS.md DATA-09" src/llm4pol/data/validate.py</automated>
    <fails_when>prints 0 — the DATA-09-vs-R-2 flag is not in the Tg section (W-3: flag, do not resolve)</fails_when>
    <automated>grep -c "^| A-7 |" docs/governance/INVARIANTS.md</automated>
    <fails_when>prints anything other than 1 — the A-7 guard row is missing or duplicated</fails_when>
    <automated>sed -n '/^| D-7 /,/^| D-10 /p' docs/governance/INVARIANTS.md | grep -c "| prose |"</automated>
    <fails_when>prints anything other than 0 — a D-7..D-10 row still says prose</fails_when>
    <automated>grep -c "threshold =" src/llm4pol/data/validate.py src/llm4pol/data/filters.py</automated>
    <fails_when>either count is not 0 — a threshold is written as a decision (ADR-0005)</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits: `test(02-05): add failing tg-ladder, feasible-set, D-8, A-7 and section-order tests; extend the D-10 test (D-08, DATA-08, DATA-09, DATA-10)` then `feat(02-05): record the Tg window, the tg_rmse ladder and the feasible set as a D-16 input; flag the DATA-09 wording; promote D-8 and A-7 to tests (D-07, D-08, DATA-08, DATA-09, DATA-10)`; `git show --format= <GREEN_SHA> -- docs/governance/INVARIANTS.md` shows exactly one changed table row (D-8) plus the appended A-7 section and nothing else; `git show --stat --format= <GREEN_SHA>` does not list `src/llm4pol/data/report.py`.
    - The synthetic and pinned-file reports' Tg section contains the paragraph beginning `Wording discrepancy raised for the owner: REQUIREMENTS.md DATA-09 reads "filtered by tg_rmse"`; the ladder counts are printed at every rung and no `tg_rmse` cut is applied (R-2); the discrepancy is listed in this plan's summary under owner questions.
    - On the pinned file: `tg_non_null` 56,064, `tg_inside_window` 52,753, `tg_outside_window` 3,311, the five ladder rungs 36,592 / 42,232 / 43,316 / 43,521 / 43,545, `feasible_candidates_dev_defaults` 6,793 of 40,212, `eps_q25_candidates` 2.6524 ± 0.0005 (F-60..F-65).
    - The feasible section's text contains `input to the D-16 gate` and `development default`; `grep -c "threshold =" docs/audit/polyomics-041e5834-validation.md` prints 0 after Task 3.
    - `filters.py` reads the ladder rungs from `PropertySpec.tg_rmse_ladder` (proved by Test 1's replaced-registry case) and defines `DEV_DEFAULT_TG_MIN_K` and `DEV_DEFAULT_EPS_QUANTILE` with the charter citation comment.
    - `report.CountTable` still refuses a header without `population` (the 02-01 guard, re-asserted by the extended Test 4).
  </acceptance_criteria>
  <done>Charter §13 M1 exit criterion (5) is reproducible by command and stated as an input, not a decision; DATA-09's window and ladder counts are in the report with no cut applied and the DATA-09-vs-R-2 wording discrepancy is flagged for the owner; D-8 and A-7 are named tests the gate runs, the D-10 test now also covers the committed report, and INVARIANTS.md records all five promoted guards (D-7, D-10 from 02-01; D-9 from 02-04; D-8, A-7 here).</done>
</task>

<task type="auto">
  <name>Task 3: Generate and commit the number authority; correct the research README (D-07, R-3, charter §9, §10)</name>
  <precondition>The pinned CSV is present locally (196,910,783 bytes) and `python -m llm4pol.data validate` exited 0 at the end of Task 2; `git status --porcelain` is clean apart from the untracked GSD runtime artifacts noted in Phase 1.</precondition>
  <read_first>
    - `docs/MASTER-PLAN.md` §9 (milestone done = reproduction), §10 (the report is the number authority and carries aggregates only)
    - `docs/research/databases/README.md` § "A broken column, and the right one" (the section the note follows) and § "The industrial property triple is large and the trade-off is weak"
    - `docs/audit/README.md` § Files (the index table to extend)
    - `.planning/phases/02-data-foundation/02-CONTEXT.md` R-3 (dated correction note; ADR-0004's consequence sentence stays; "broken column" wording superseded by the report)
    - `tests/test_data_real_file.py` and `tests/test_data_invariants.py` (Task 2's Test 6 lists the eleven headings the committed-report test reuses; the D-10 test already covers the committed report when present)
    - `.gitattributes` (LF pinned) and `src/llm4pol/data/report.py` (the writer's newline argument) — byte-identity across platforms depends on both
  </read_first>
  <files>docs/audit/polyomics-041e5834-validation.md, docs/audit/README.md, docs/research/databases/README.md, tests/test_data_invariants.py, tests/test_data_real_file.py</files>
  <action>
    STEP 1 — generate the number authority. Run `pixi run --manifest-path env/pixi.toml python -m
    llm4pol.data fetch`, then `… load`, then `… validate`; the third writes
    `docs/audit/polyomics-041e5834-validation.md` and must exit 0 with `VALIDATION: <n> reproduced, <m>
    documented, 0 failed -> exit 0`. Check the file for aggregates only with the three greps of this
    task's verify block (SMILES-like token, bare 16-hex candidate-id shape, UUID shape — each must
    print 0); if any prints more than 0 the writer is emitting a row-level value and must be fixed
    before the file is committed. Add the real-file test
    `test_real_validate_exits_0_and_committed_report_is_byte_identical_to_a_fresh_run` to
    `tests/test_data_real_file.py`: `validate.run(REPO_ROOT, processed_dir=real_load.processed_dir,
    report_path=tmp)` returns 0 and `tmp.read_bytes() == (REPO_ROOT /
    "docs/audit/polyomics-041e5834-validation.md").read_bytes()` (line endings: the writer opens the
    file with the LF newline argument; `.gitattributes` pins LF, so the committed bytes equal the
    fresh bytes on both platforms — if the comparison fails only on line endings, fix the writer,
    never the test). Add to `tests/test_data_invariants.py` the CI-runnable test
    `test_committed_validator_report_exists_and_names_revision_and_sections`: the committed file
    exists, contains `041e5834ea1a48682fae12dc39ccd723bcd4f771` and `polyomics:general_polymers@041e5834`,
    the eleven headings of Task 2's Test 6 in order, a findings table with no `FAILED` or `MISSING`
    status, and — the aggregates-only guard — no line matching the SMILES-token pattern, no token
    matching a bare sixteen-hex-digit string bounded by non-hex characters (a `candidate_id` shape;
    the 40-hex revision and 64-hex sha256 are longer and therefore not matched), no token matching
    the hyphenated 8-4-4 hex shape of a UUID, and no table cell longer than 80 characters (a label,
    never a value dump). Use the exact regular expressions of this task's verify block for the three
    patterns. Add a row to the `## Files` table of `docs/audit/README.md`:
    `polyomics-041e5834-validation.md` — generated by `python -m llm4pol.data validate` on the pinned
    PolyOmics snapshot; the number authority for every PolyOmics figure cited in this repository
    (charter §10); regenerated, never hand-edited. Run the gate → `7/7`. Commit the report, the two
    test files and the audit index as `docs(02-05): commit the PolyOmics 041e5834 validator report —
    the number authority for the snapshot (D-07, charter §10)`.

    STEP 2 — the correction note (R-3). In `docs/research/databases/README.md`, insert immediately
    after the paragraph ending `Any work using PolyOmics must pick \`dielectric_const_dc\` and say so.`
    (end of § "A broken column, and the right one") a new sub-section headed `#### Correction
    (2026-09-22, Phase 2 validator)` with, in this order, four short paragraphs: (1) the number
    authority is now `docs/audit/polyomics-041e5834-validation.md`, generated by `python -m
    llm4pol.data validate` on the pinned revision `041e5834…`; the 2026-09-11 text above is kept as
    written; (2) `static_dielectric_const` is not a broken column: it is the uncorrected orientational
    permittivity of non-polarisable MD, and `dielectric_const_dc = static_dielectric_const − 1 +
    refractive_index²` holds on all 93,488 rows (max residual < 1e-5; arXiv:2511.11626 Table S3 calls
    it the corrected static dielectric constant) — so "zero Maxwell violations" is an identity, not a
    check; A-7 stands: the registry serves `dielectric_const_dc` only; (3) the 88.9 % is 88.83 %
    (38,693 / 43,561) on the triple rows and 84.30 % on all rows; the rank correlation −0.05 did not
    reproduce (0.003 all rows, −0.144 triple rows); (4) the 43,561 is exactly `thermal_conductivity`
    non-null ∧ `dielectric_const_dc` ∈ [1, 20] ∧ `tg` ∈ [100, 900] K, with no `tg_rmse` term; the
    protocol `check_tc` filter gives 42,733 alongside. Do not edit any other line of the file. Run the
    gate → `7/7`. Commit `docs(02-05): add the dated correction note to the database survey, pointing
    at the validator report (R-3)`.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or the `Steps:` line is not the seven-step line, or any line begins with `FAIL` or `HIT`</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_real_file.py tests/test_data_invariants.py -v</automated>
    <fails_when>exit code is non-zero, or `test_real_validate_exits_0_and_committed_report_is_byte_identical_to_a_fresh_run` or `test_committed_validator_report_exists_and_names_revision_and_sections` or `test_d10_every_count_table_in_report_names_its_population` is not listed as PASSED</fails_when>
    <automated>git ls-files docs/audit/polyomics-041e5834-validation.md data/</automated>
    <fails_when>the output is not exactly the four paths `data/MANIFEST-open.sha256`, `data/MANIFEST.sha256`, `data/README.md`, `docs/audit/polyomics-041e5834-validation.md` (in any order) — the report is not committed or a data file is</fails_when>
    <automated>grep -c -E '\*[A-Za-z]' docs/audit/polyomics-041e5834-validation.md</automated>
    <fails_when>prints anything other than 0 — a SMILES-like token is in the committed report (aggregates only, charter §10)</fails_when>
    <automated>grep -c -E '(^|[^0-9a-f])[0-9a-f]{16}([^0-9a-f]|$)' docs/audit/polyomics-041e5834-validation.md</automated>
    <fails_when>prints anything other than 0 — a bare 16-hex token (a `candidate_id`) is in the committed report; the 40-hex revision and 64-hex hashes do not match this boundary-anchored pattern</fails_when>
    <automated>grep -c -E '[0-9a-f]{8}-[0-9a-f]{4}-[0-9a-f]{4}' docs/audit/polyomics-041e5834-validation.md</automated>
    <fails_when>prints anything other than 0 — a UUID-shaped token (a source `UUID` value) is in the committed report</fails_when>
    <automated>grep -c "Correction (2026-09-22, Phase 2 validator)" docs/research/databases/README.md</automated>
    <fails_when>prints anything other than 1</fails_when>
    <automated>grep -c "88.9% of" docs/research/databases/README.md</automated>
    <fails_when>prints 0 — the 2026-09-11 text was rewritten instead of annotated (R-3)</fails_when>
    <automated>grep -c "polyomics-041e5834-validation.md" docs/audit/README.md docs/research/databases/README.md</automated>
    <fails_when>either count is 0 — the report is not indexed or not cited by the correction note</fails_when>
    <automated>grep -c "threshold =" docs/audit/polyomics-041e5834-validation.md</automated>
    <fails_when>prints anything other than 0 — a threshold is written as a decision in the committed report (ADR-0005)</fails_when>
  </verify>
  <acceptance_criteria>
    - Two commits exist in order: `docs(02-05): commit the PolyOmics 041e5834 validator report — the number authority for the snapshot (D-07, charter §10)` (listing exactly `docs/audit/polyomics-041e5834-validation.md`, `docs/audit/README.md`, `tests/test_data_invariants.py`, `tests/test_data_real_file.py`), then `docs(02-05): add the dated correction note to the database survey, pointing at the validator report (R-3)`, which changes `docs/research/databases/README.md` only between the end of § "A broken column, and the right one" and the next `###` heading (`git show --format= <sha> -- docs/research/databases/README.md` shows only added lines).
    - `docs/audit/polyomics-041e5834-validation.md` is byte-identical to a fresh `validate.run` (proved by the real-file test), contains the eleven headings in order, has no `FAILED` / `MISSING` finding, and passes the three aggregates-only greps (SMILES-like token, bare 16-hex, UUID shape) with 0 each; every table cell is at most 80 characters.
    - Nothing is pushed in this task (`git status --porcelain --branch` first line shows `[ahead N]`); the push, CI dispatch and evidence are Task 4, so a red CI run never forces the report steps to be redone.
  </acceptance_criteria>
  <done>The number authority exists and is committed, byte-reproducible from the pinned bytes by one command and provably free of row-level values; the research survey carries its dated correction without a rewrite of its 2026-09-11 text.</done>
</task>

<task type="auto">
  <name>Task 4: Push, dispatch the CI gate on both platforms and record the M1 evidence (charter §9, §13 M1 (1)–(6); Phase 1 D-04/D-05 convention)</name>
  <reversibility rating="one-way">`git push origin main` publishes every reachable commit to the public repository `kn1218/llm4pol`; nothing published can be unpublished. REVERSIBILITY_GATES is off, so no checkpoint is inserted — the substitute guard is the history scan of the seven-step gate, re-run immediately before the push, which must print zero hits and `dotenv-path-in-history: 0 hit(s)`; the committed report passed the aggregates-only greps in Task 3 before any push.</reversibility>
  <precondition>`gh auth status` reports a logged-in account for github.com and `git remote get-url origin` names `kn1218/llm4pol` (verified 2026-09-22); Task 3's two commits are on local `main`; `git rev-parse --is-shallow-repository` prints `false`.</precondition>
  <read_first>
    - `.planning/phases/01-foundation/01-01-PLAN.md` Task 3 and `01-01-SUMMARY.md` § Recorded lines — the push → dispatch → watch → evidence sequence and the workstation hook that refuses git's short `-n` flag (use `--max-count`; commit messages via `-F` from a scratchpad file)
    - `.planning/ROADMAP.md` § Phase 2 — Success Criteria 1–5 (charter §13 M1 (1)–(6)) the evidence must cite by number
    - `docs/MASTER-PLAN.md` §13 row M1 and §9 (VERIFICATION cites exit-criterion numbers; milestone done = reproduction)
    - `.github/workflows/ci.yml` — workflow name `check`; the verifier reads `SUMMARY:` lines from its job logs
    - `docs/audit/polyomics-041e5834-validation.md` (committed in Task 3) — the finding lines the evidence quotes
  </read_first>
  <files>.planning/phases/02-data-foundation/02-EVIDENCE.md</files>
  <action>
    STEP 1 — pre-push guard and push. Run the full gate once more and require `Steps: ruff check, ruff
    format --check, mypy, import-linter, schema-inventory, history-secret-scan, pytest`, `SUMMARY: 7/7
    steps passed`, zero hits per pattern class and `dotenv-path-in-history: 0 hit(s)`. Only then `git
    push origin main`; record `git rev-parse HEAD` as the pushed tip.

    STEP 2 — dispatch and watch (Phase 1 D-05 convention). `gh workflow run check --ref main`; then
    `gh run list --workflow=check --event workflow_dispatch --limit 1 --json databaseId,headSha,status
    --jq '.[0]'` until `headSha` equals the pushed tip (re-run the list command; never fall back to a
    push-triggered run); `gh run watch <RUN_ID> --exit-status`. On a red job read `gh run view
    <RUN_ID> --log-failed`, fix the cause with a new conventional commit (a CI-only failure is most
    likely a path or line-ending difference on ubuntu-latest, or a real-file test that failed to skip
    — that would be a fixture bug, fix the fixture; the committed report of Task 3 is not regenerated
    unless the byte-identical test itself is what failed), re-run the local gate, push, dispatch again;
    never edit the workflow to skip a step. Capture `gh run view <RUN_ID> --json
    databaseId,headSha,conclusion,url,jobs --jq '{databaseId,headSha,conclusion,url,jobs:[.jobs[]|
    {name,conclusion}]}'` and `gh run view <RUN_ID> --log | grep -E "SUMMARY:|llm4polcheck inventory|
    skipped|passed in|scanned [0-9]+ commit"` — both platforms must show `SUMMARY: 7/7 steps passed`,
    `llm4polcheck inventory: 1 entries, 1 instances processed`, and a pytest line with `skipped`
    (the real-file tests and the PoLyInfo manifest test skip in CI; record the skipped count).

    STEP 3 — evidence. Write `.planning/phases/02-data-foundation/02-EVIDENCE.md`: header `# Phase 2 —
    charter §13 M1 evidence`, the UTC date, the pushed tip sha, the snapshot id and revision, and the
    sentence that each section maps to a ROADMAP Phase 2 success criterion = charter §13 M1 exit
    criterion. Sections, each a list of `command` → fenced verbatim output pairs: `## (1) Pinned files
    match data/MANIFEST-open.sha256` — the two `verified` lines and `snapshot:` line of `python -m
    llm4pol.data fetch`, the `test_real_fetch_verifies_pinned_files_without_downloading PASSED` line,
    the `test_committed_manifest_open_lists_the_two_pinned_files_with_research_hashes PASSED` line;
    `## (2) Row count, unique units, 43,561, 88.9 %, 0 — reproduced or documented` — the `finding`
    lines for `source_rows`, `source_columns`, `unique_smiles_list`, `readme_triple_all_rows`,
    `readme_triple_in_scope`, `triple_check_tc_all_rows`, `candidate_triple_filter_then_median`,
    `static_maxwell_violation_pct_triple`, `dc_identity_max_residual`, `dc_maxwell_violations`, and
    the `tg_rmse_le_*` / `tg_outside_window` lines (ROADMAP criterion 2 names the `tg_rmse` / window
    counts) plus the report's `Wording discrepancy raised for the owner` paragraph (W-3, DATA-09 vs
    R-2); `## (3) 95,335 vs 73,045 resolved` — the `card_count_73045` finding line and the report's
    `The dataset card's 73,045` paragraph; `## (4) Replicate structure and noise floor` — the
    `replicate_*`, `same_version_*`, `cross_version_*` and `noise_floor_*` finding lines; `## (5)
    Feasible set under the development defaults` — the `feasible_*` and `eps_q25_*` finding lines and
    the report sentence containing `input to the D-16 gate`; `## (6) D-7..D-10 and A-7 promoted` —
    the `-v` PASSED lines of `test_d7_…`, `test_d8_…`, `test_d9_…`, `test_d10_…`, `test_a7_…` (both
    registry tests and the invariants test; their names carry the barred column's name by design — the
    grep gates on test bodies exclude `def test_` lines), and the D-7..D-10 and A-7 rows printed from
    `docs/governance/INVARIANTS.md` with `sed` (address ranges `/^| D-7 /,/^| D-10 /` and `/^| A-7 /`,
    print mode); then `## Gate and CI` — the local `Steps:` and `SUMMARY: 7/7` lines, `CI run id:
    <RUN_ID>`, `CI head sha: <sha>`, `CI url: <url>`, `check (windows-latest): success`, `check
    (ubuntu-latest): success`, the grep'd log lines from both jobs, and the `VALIDATION:` line; `##
    Data policy` — `git ls-files data/ docs/audit/polyomics-041e5834-validation.md` output and the
    three aggregates-only greps' `0` lines. Nothing in the file may contain a secret-shaped string or
    a data row. Commit as `docs(02-05): record Phase 2 M1 evidence (charter §13 M1 (1)–(6), CI run
    <RUN_ID>)` and push that commit too, noting in the summary that the evidence commit post-dates
    the cited CI run.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final line is not `SUMMARY: 7/7 steps passed`, or any line begins with `FAIL` or `HIT`</fails_when>
    <automated>gh run view $(grep -m1 -oE "CI run id: [0-9]+" .planning/phases/02-data-foundation/02-EVIDENCE.md | grep -oE "[0-9]+") --json event,headBranch,conclusion,jobs --jq '[.event, .headBranch, .conclusion, ([.jobs[] | .name + "=" + .conclusion] | sort | join(","))] | join(" ")'</automated>
    <fails_when>the output is not `workflow_dispatch main success check (ubuntu-latest)=success,check (windows-latest)=success`</fails_when>
    <automated>gh run view $(grep -m1 -oE "CI run id: [0-9]+" .planning/phases/02-data-foundation/02-EVIDENCE.md | grep -oE "[0-9]+") --log | grep -c "SUMMARY: 7/7 steps passed"</automated>
    <fails_when>prints a number below 2 — fewer than both platforms printed the seven-step green summary</fails_when>
    <automated>gh run view $(grep -m1 -oE "CI run id: [0-9]+" .planning/phases/02-data-foundation/02-EVIDENCE.md | grep -oE "[0-9]+") --log | grep -c "llm4polcheck inventory: 1 entries, 1 instances processed"</automated>
    <fails_when>prints a number below 2 — the schema-inventory step did not run on both platforms</fails_when>
    <automated>grep -c -E "^check \((windows|ubuntu)-latest\): success$" .planning/phases/02-data-foundation/02-EVIDENCE.md</automated>
    <fails_when>prints anything other than 2</fails_when>
    <automated>grep -c "input to the D-16 gate" .planning/phases/02-data-foundation/02-EVIDENCE.md</automated>
    <fails_when>prints 0 — exit criterion (5) evidence is missing</fails_when>
    <automated>grep -c "Wording discrepancy raised for the owner" .planning/phases/02-data-foundation/02-EVIDENCE.md</automated>
    <fails_when>prints 0 — the DATA-09-vs-R-2 flag was not carried into the evidence (W-3)</fails_when>
    <automated>git status --porcelain --branch</automated>
    <fails_when>the first line is not `## main...origin/main` (an `[ahead N]` or `[behind N]` suffix means the evidence commit was not pushed or the remote moved); tracked-file changes on further lines mean the working tree is dirty (the untracked GSD runtime artifacts noted in Phase 1 are acceptable)</fails_when>
  </verify>
  <acceptance_criteria>
    - One commit exists: `docs(02-05): record Phase 2 M1 evidence (charter §13 M1 (1)–(6), CI run <RUN_ID>)`, plus any `fix(02-05): …` commit a red CI run forced (each named in the summary with the log line that caused it).
    - `02-EVIDENCE.md` contains `CI run id: <digits>`, `CI head sha: <40 hex>`, `check (windows-latest): success`, `check (ubuntu-latest): success`, two job-prefixed `SUMMARY: 7/7 steps passed` lines, two `llm4polcheck inventory: 1 entries, 1 instances processed` lines, a pytest line with `skipped` from CI, the `VALIDATION:` line, the finding lines named in STEP 3 for criteria (2)–(5), the DATA-09 wording flag, the five promoted-test PASSED lines and the INVARIANTS.md rows for criterion (6), and the `git ls-files` data-policy output with the three aggregates-only greps.
    - The cited run's `headSha` equals the pushed tip of STEP 1 (or of the last fix commit), its event is `workflow_dispatch`, its branch `main`, both jobs `success`.
    - `origin/main` equals local `main` after the final push; no data file is tracked.
  </acceptance_criteria>
  <done>The seven-step gate is green on both CI platforms with the real-file tests skipping by design, and `02-EVIDENCE.md` pairs every charter §13 M1 exit criterion (1)–(6) with the command and verbatim output the Phase 2 verifier will cite, including the flagged DATA-09 wording question for the owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research facts → `EXPECTED_POLYOMICS` | Forty-odd expected values transcribed from RESEARCH.md; a wrong transcription would either fail the validator (caught) or pin a wrong number (not caught) |
| validator → committed report → every later citation | The report is the authority; a stale or hand-edited copy would silently diverge from the code |
| report → repository | Aggregates only; a row-level value or SMILES in the report is a policy breach (charter §10) |
| local history → public remote | The push publishes every reachable commit (R-3) |
| development defaults → D-16 | A threshold stated as a decision would pre-empt the Phase 7 pre-registration (ADR-0005) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-23 | Tampering | hand-edited or stale committed report | high | mitigate | `test_real_validate_exits_0_and_committed_report_is_byte_identical_to_a_fresh_run` regenerates into tmp and compares bytes; the writer emits no timestamp or git sha; `docs/audit/README.md` states it is regenerated, never hand-edited |
| T-02-24 | Information Disclosure | data rows, SMILES, candidate ids or UUIDs in the committed report or evidence | medium | mitigate | the report renders aggregates only; three verify greps (SMILES-like token, boundary-anchored bare 16-hex, UUID shape) must each print 0 before the report is committed; `test_committed_validator_report_exists_and_names_revision_and_sections` asserts the same three patterns are absent and no table cell exceeds 80 characters |
| T-02-25 | Repudiation | a documented difference silently widening | medium | mitigate | `documented` findings pin the observed research value with a tolerance (88.83 ± 0.05, residual ≤ 1e-5, …); further drift exits 1 (R-5) |
| T-02-26 | Tampering | a threshold written as a decision | medium | mitigate | `DEV_DEFAULT_*` constants cite the charter; the feasible section's fixed sentence; `threshold =` grep-banned from `validate.py` / `filters.py`; INVARIANTS and ADR-0005 cited |
| T-02-27 | Spoofing | a CI run on the wrong commit or event cited as evidence | low | mitigate | the evidence names run id, head sha, event, branch and both job conclusions; the verify re-queries the run by id and requires `workflow_dispatch main success` |
| T-02-28 | Information Disclosure | secret in history published by the push | critical | mitigate | the seven-step gate's `history-secret-scan` runs immediately before the push; zero hits required; evidence and report contain no secret-shaped string by construction |
| T-02-SC | Tampering | package installs | low | accept | This plan installs nothing |
</threat_model>

## Artifacts this phase produces

**New files (this plan):** `docs/audit/polyomics-041e5834-validation.md` (eleven sections; `## Findings`
table `| finding | population | expected | observed | class | status |`);
`.planning/phases/02-data-foundation/02-EVIDENCE.md`.

**Modified files:** `src/llm4pol/data/filters.py` (`readme_triple_ladder`, `alternative_eps_filters`,
`check_tc_mask`, `candidate_level_triple`, `multi_tacticity_counts`, `multi_tacticity_counts_canonical`,
`raw_string_merges`, `card_count_candidates`, `dielectric_identity_residual`, `static_minimum`,
`static_maxwell_violation_share`, `dc_maxwell_violations`, `spearman_table`, `readme_window`, `coverage`,
`physical_range_counts`, `tacticity_counts`, `tg_window_counts`, `tg_rmse_ladder_counts`,
`tg_rmse_descriptives`, `FeasibleSet`, `feasible_set`, `feasible_rows`, `DEV_DEFAULT_TG_MIN_K`,
`DEV_DEFAULT_EPS_QUANTILE`); `src/llm4pol/data/validate.py` (six new sections; `EXPECTED_POLYOMICS`
complete; the DATA-09-vs-R-2 flag paragraph in the Tg section); `tests/test_data_invariants.py` (+12
tests and the D-10 test extended to the committed report); `tests/test_data_real_file.py` (+3 tests);
`docs/governance/INVARIANTS.md` (D-8 row; new A-7 section); `docs/research/databases/README.md`
(appended correction note); `docs/audit/README.md` (index row). `src/llm4pol/data/report.py` is read,
not edited (its `population` guard is plan 02-01's).

**Commits (in order):** `test(02-05): …README-number…` → `feat(02-05): reproduce the README numbers …`
→ `test(02-05): …tg-ladder, feasible-set, D-8, A-7 and section-order tests; extend the D-10 test…` →
`feat(02-05): record the Tg window …; flag the DATA-09 wording; promote D-8 and A-7 …` →
`docs(02-05): commit the PolyOmics 041e5834 validator report …` → `docs(02-05): add the dated
correction note …` → (Task 4) `docs(02-05): record Phase 2 M1 evidence …`.

**Not produced here, by decision:** any D-16 threshold (Phase 7 pre-registration, ADR-0005); the Phase 3
hidden-table choice between 43,561 and 42,733 (Open Question 2, owner); folding of the 303 unknown
twins (Open Question 1, owner); a resolution of the DATA-09 wording versus R-2 (flagged in the report
and the summary; owner, per ADR-0002 precedence); an ADR-0004 edit (R-3: its consequence sentence
stays); the Phase 2 `VERIFICATION.md` (written by the verifier from `02-EVIDENCE.md`).

## Source audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | validated by a report that is the authority for every number cited later | 02-05 T1–T4 | COVERED | committed, byte-reproducible |
| REQ | DATA-06 | reproduce or document: rows, unique units, 43,561, 88.9 %, 0; resolve 95,335 vs 73,045 | 02-05 T1 | COVERED | |
| REQ | DATA-08 | feasible-set size under ε ≤ Q25, Tg ≥ 400 K | 02-05 T2 | COVERED | labelled D-16 input |
| REQ | DATA-09 | Tg filtered by `tg_rmse` and 100–900 K; counts recorded | 02-05 T2 | COVERED | window applied; ladder counts recorded at every rung; no cut per R-2 — the wording discrepancy is flagged for the owner (W-3), not resolved |
| REQ | DATA-10 | D-8, A-7 promoted; D-10 test extended | 02-05 T2 | COVERED | D-7, D-10 from 02-01; D-9 from 02-04 |
| CONTEXT | D-07 | every report section | 02-05 T1, T2 | COVERED | eleven sections |
| CONTEXT | D-08 | D-8, A-7 tests; Maxwell-consistency test; INVARIANTS guard column | 02-05 T2 | COVERED | |
| CONTEXT | R-1 | with-twins and folded counts printed | 02-05 T1 | COVERED | |
| CONTEXT | R-2 | both triples side by side; ladder; no cut | 02-05 T1, T2 | COVERED | |
| CONTEXT | R-3 | narrative correction; README note; ADR-0004 sentence stays | 02-05 T1, T3 | COVERED | |
| CONTEXT | R-5 | reproduce / documented classes with tolerances | 02-05 T1 | COVERED | observed value pinned |
| RESEARCH | Pitfall 1 | never chase 88.9 % or treat ε ≥ n² as a check | 02-05 T1 | COVERED | prohibition |
| RESEARCH | Pitfall 2 | three triple counts labelled | 02-05 T1 | COVERED | |
| RESEARCH | Pitfall 3 | 303 twins reported | 02-05 T1 | COVERED | |
| RESEARCH | Pitfall 5 | `check_tc` count alongside | 02-05 T1 | COVERED | |
| RESEARCH | Open Q1, Q2, Q3 | owner decisions | — | EXCLUDED | raised in the report text and the summary; not decided here |
| CHARTER | §13 M1 (2), (3) | README numbers | 02-05 T1 | COVERED | |
| CHARTER | §13 M1 (5) | feasible set | 02-05 T2 | COVERED | |
| CHARTER | §13 M1 (6) | tests promoted | 02-05 T2 | COVERED | D-8, A-7 here; D-7, D-10 in 02-01; D-9 in 02-04 |
| CHARTER | §9, §10 | report is the authority, aggregates only; VERIFICATION cites criteria | 02-05 T3, T4 | COVERED | report + three aggregates-only greps (T3); evidence file (T4) |
| SCOPE | — | CI green on both platforms without the 197 MB download | 02-05 T4 | COVERED | real-file tests skip |
| DEFERRED | — | OpenPoly / PoLyInfo loaders; chi tables; tag vocabulary | — | EXCLUDED | 02-CONTEXT § Deferred Ideas |

<verification>
- `python -m llm4pol.data validate` on the pinned file exits 0 with every finding `reproduced` or
  `documented`; the committed report is byte-identical to a fresh run.
- The seven-step gate is green locally and in a dispatched CI run on both platforms (real-file tests
  skipped there).
- INVARIANTS.md rows D-7..D-10 name tests (D-7, D-10 from 02-01; D-9 from 02-04; D-8 here) and an A-7
  row exists; the research README carries the dated note; the report's Tg section carries the DATA-09
  wording flag; `02-EVIDENCE.md` cites (1)–(6) with commands and outputs.
</verification>

<success_criteria>
ROADMAP Phase 2 success criteria 1–5 (charter §13 M1 (1)–(6)) hold and are evidenced: the pinned files
match the manifest; the report reproduces 95,335 / 78,379 / 43,561 (with the window and `tg_rmse`
counts) and documents 88.83 %, the ε_dc identity and 73,045; the replicate structure and noise floor
are recorded; the feasible set under the development defaults is stated as a D-16 input; D-7..D-10
and A-7 run as tests in the gate. The candidate definition, the registry and the snapshot identifier
are frozen.
</success_criteria>

<output>
Create `.planning/phases/02-data-foundation/02-05-SUMMARY.md` when done. Record the seven commit shas
(plus any CI-forced fix commit), the `VALIDATION:` line, the full findings count (reproduced /
documented), the CI run id with both job conclusions and the CI `skipped` count, the four owner
questions surfaced with the report sections that state them — (1) the 303 unknown-tacticity twins
(R-1, Open Question 1), (2) whether the Phase 3 hidden table applies `check_tc` (R-2, Open Question
2), (3) the `r2` unit (R-4, Open Question 3), (4) the DATA-09 wording "filtered by `tg_rmse`" versus
R-2's no-cut-in-M1 (W-3; precedence per ADR-0002 makes this an owner call) — and any documented
finding whose observed value sat within 20 % of its tolerance.
</output>
