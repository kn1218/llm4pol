---
phase: 02-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - protocol/schemas/property-registry.json
  - protocol/property-registry_v1.yaml
  - protocol/property-registry_v1.provenance.yaml
  - tests/test_data_registry.py
autonomous: true
requirements: [DATA-03, DATA-09]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`protocol/property-registry_v1.yaml` validates against `protocol/schemas/property-registry.json` (JSON Schema draft 2020-12) and declares exactly the nine keys `thermal_conductivity`, `dielectric_const_dc`, `tg`, `rg`, `r2`, `ffv`, `sp_ced`, `density`, `refractive_index`, each with `column`, `unit`, `unit_status`, `condition`, `role`, `physical_range` (D-05, DATA-03, charter §6)."
    - "The schema's `propertyNames` enum makes the uncorrected static permittivity column un-registerable: injecting it into a copy of the instance raises `jsonschema.ValidationError` (A-7 by construction, F-45; ADR-0004)."
    - "Units carry their verification status from the research: `verified` for thermal_conductivity W/(m*K), dielectric_const_dc 1, tg K, rg angstrom, ffv 1, sp_ced MPa, density g/cm^3, refractive_index 1; `unverified` for r2 with `unit: nm^2` and a `unit_note` stating both candidates (R-4, F-20..F-29)."
    - "The registry records the `tg_rmse` ladder `[0.05, 0.1, 0.2, 0.5, 1.0]` with unit `(g/cm^3)^2` and applies no `tg_rmse` cut, and records `check_tc: true` as the thermal-conductivity quality filter whose count is printed alongside the README count (R-2, DATA-09, F-23, F-59, F-65)."
    - "`protocol/property-registry_v1.provenance.yaml` has one entry per leaf of the instance, the two leaf-path sets are equal, every entry's `provenance_class` is one of `paper_or_source`, `protocol_decision`, `unresolved`, and `properties.r2.unit` is the only `unresolved` leaf (precedent triad, F-25)."
    - "Every number in the instance is a YAML number, never a string such as `1e-3` (F-47)."
  artifacts:
    - path: "protocol/schemas/property-registry.json"
      provides: "Draft 2020-12 JSON Schema: root additionalProperties false; properties.propertyNames enum of the nine keys with required + minProperties/maxProperties 9; $defs.property with unit_status/role enums, physical_range prefixItems, filters with check_tc / tg_rmse_ladder / tg_rmse_unit only; $defs.condition with const 300 K / 1 atm / GAFF2_mod / RESP / isotropic_amorphous"
      contains: "propertyNames"
    - path: "protocol/property-registry_v1.yaml"
      provides: "Registry instance v1 for snapshot polyomics:general_polymers@041e5834 with nine entries"
      contains: "registry_version: v1"
    - path: "protocol/property-registry_v1.provenance.yaml"
      provides: "One provenance entry per leaf dotted path with provenance_class, source/fact or rationale"
      contains: "provenance_class"
    - path: "tests/test_data_registry.py"
      provides: "Schema validity, instance validity, nine keys, A-7 rejection, R-4 units, ranges and filters, numbers-not-strings, condition consts, provenance leaf-set equality and class enum"
      contains: "test_a7_static_dielectric_const_is_rejected_by_the_registry_schema"
  key_links:
    - from: "protocol/property-registry_v1.yaml"
      to: "protocol/schemas/property-registry.json"
      via: "jsonschema.validate(yaml.safe_load(instance), json.load(schema)) in the tests (and, from plan 02-04, in the schema-inventory check step)"
      pattern: "propertyNames"
    - from: "protocol/property-registry_v1.provenance.yaml"
      to: "protocol/property-registry_v1.yaml"
      via: "leaf-path set equality asserted by test_provenance_leaf_paths_equal_registry_leaf_paths"
      pattern: "entries:"
  prohibitions:
    - statement: "The barred column named by A-7 appears nowhere in the registry instance, not even in a comment; the registry serves `dielectric_const_dc` only (A-7, R-3)."
      status: unverified
      verification: null
    - statement: "No `tg_rmse` cut is applied by the registry in M1; only the ladder is recorded (R-2)."
      status: unverified
      verification: null
    - statement: "No numeric threshold in the registry is described as a D-16 decision; D-16 is fixed at the Phase 7 pre-registration (ADR-0005)."
      status: unverified
      verification: null
---

<objective>
Freeze the property registry as protocol: the JSON Schema `protocol/schemas/property-registry.json`
(charter §7 names this path as the M1 freeze), the instance `protocol/property-registry_v1.yaml` and
its provenance file, following the CALF20 precedent triad (`<name>_v1.yaml` + schema + provenance).
The schema makes A-7 a structural rule (`propertyNames` enum), the instance records units with their
verification status from the research (R-4), the physical ranges the validator applies, the `check_tc`
quality filter and the `tg_rmse` ladder (R-2), and the provenance file states where every leaf came
from.

Purpose: charter §6 says units and conditions are unverified until M1 compares them with the data
card and arXiv:2511.11626; RESEARCH F-20..F-31 did that comparison. Plan 02-04's loader and check step
and plan 02-05's validator ladders read this file; nothing downstream hardcodes a range or a unit.

Output: the three protocol files and `tests/test_data_registry.py` (tests on the committed files;
they import only `json`, `yaml`, `jsonschema`, `pathlib` — no `llm4pol` import, so this plan does not
depend on 02-01 and can run beside it).

Grounding facts (do not re-derive): jsonschema 4.26.0 selects `Draft202012Validator` from `$schema`
and supports `prefixItems`, `items: false`, `propertyNames`, `if`/`then` (F-45); `yaml.safe_load("x:
1e-3")` yields the string `'1e-3'` (F-47) — write every float with a decimal point; PyYAML 6.0.3 is
importable from the lock today (transitive, F-46). Units and status: F-20 TC W/(m·K) verified; F-21
ε_dc dimensionless verified; F-22 Tg K verified; F-23 `tg_rmse` is a sum of squared density
residuals, `(g/cm³)²`, not kelvin; F-24 Rg Å verified; F-25 `r2` nm² by the data vs Å² in Table S3 —
unverified; F-26 ffv dimensionless verified; F-27 `sp_ced` MPa ≡ J/cm³ verified; F-28 density g/cm³
verified; F-29 refractive_index dimensionless verified; F-30 conditions 300 K, 1 atm, GAFF2_mod, RESP,
isotropic amorphous cell; F-59 `check_tc`; F-65 ladder; F-67 observed ranges. In this plan "the
barred column" means the uncorrected static permittivity column that ADR-0004 and A-7 keep out of the
registry; its name is never written into the registry files.

Wave note: plan 02-01 (wave 1) shares no file with this plan; if both run concurrently in one working
tree, a wave-mate's sanctioned RED commit can turn the whole-repo gate red until its GREEN commit —
re-run the gate before treating a failure as this plan's own.
</objective>

<execution_context>
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/molsim/Desktop/LLM4POL/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-foundation/02-CONTEXT.md
@.planning/phases/02-data-foundation/02-RESEARCH.md
@CLAUDE.md
@docs/MASTER-PLAN.md
@docs/governance/ADR/0004-problem-definition-and-hidden-table.md
@protocol/README.md
@tests/test_governance.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry schema and instance — nine keys, A-7 by construction, units with status, ranges and filters (D-05, R-2, R-4)</name>
  <read_first>
    - `.planning/phases/02-data-foundation/02-CONTEXT.md` — D-05 (keys, entry fields, A-7, unit status rule), R-2 (ladder recorded, no cut), R-4 (unit statuses), § Specific Ideas (precedent triad paths)
    - `.planning/phases/02-data-foundation/02-RESEARCH.md` — F-20..F-31 (units, conditions), F-45, F-47, F-59, F-65..F-67, Pattern 7 (registry skeleton and schema constraints), Pitfall 4 (`tg_rmse` is not kelvin), Pitfall 5 (`check_tc`)
    - `docs/MASTER-PLAN.md` §6 (registry table: key, column, unit, role), §7 (freeze row naming `protocol/schemas/property-registry.json`), §4 A-7, §13 gate-parameter table (D-16 stays gated)
    - `C:/Users/molsim/Desktop/CALF20_DiscoveryLoop/protocol/partition_v1.yaml` and `partition_v1.schema.json` — the triad's header-comment style (what the file is, its change route, the float-formatting rule) and schema strictness (`additionalProperties: false` everywhere)
    - `protocol/README.md` — the repository's own rule for protocol files
    - `tests/test_governance.py` — house test style (`ROOT`, plain functions, f-string messages)
  </read_first>
  <behavior>
    - Test 1 `test_registry_schema_is_a_valid_draft_2020_12_schema`: `jsonschema.Draft202012Validator.check_schema(schema)` raises nothing and `schema["$schema"]` is `https://json-schema.org/draft/2020-12/schema`.
    - Test 2 `test_registry_instance_validates_against_its_schema`: `jsonschema.validate(instance, schema)` raises nothing, where `instance = yaml.safe_load(...)`.
    - Test 3 `test_registry_declares_exactly_the_nine_keys_in_charter_order`: `list(instance["properties"])` equals `["thermal_conductivity", "dielectric_const_dc", "tg", "rg", "r2", "ffv", "sp_ced", "density", "refractive_index"]`.
    - Test 4 `test_a7_static_dielectric_const_is_rejected_by_the_registry_schema`: a deep copy of the instance with the barred column added under `properties` (copying the `dielectric_const_dc` entry with `column` set to the barred name) raises `jsonschema.ValidationError`; the committed instance text (comments included) does not contain the barred name; `instance["properties"]["dielectric_const_dc"]["column"] == "dielectric_const_dc"`.
    - Test 5 `test_registry_units_and_status_match_research_r4`: `{key: (column, unit, unit_status)}` equals `thermal_conductivity → ("thermal_conductivity", "W/(m*K)", "verified")`, `dielectric_const_dc → ("dielectric_const_dc", "1", "verified")`, `tg → ("tg", "K", "verified")`, `rg → ("Rg", "angstrom", "verified")`, `r2 → ("r2", "nm^2", "unverified")`, `ffv → ("fractional_free_volume", "1", "verified")`, `sp_ced → ("sp_ced", "MPa", "verified")`, `density → ("density", "g/cm^3", "verified")`, `refractive_index → ("refractive_index", "1", "verified")`; `r2` has a non-empty `unit_note` mentioning both `nm^2` and `angstrom^2`; no other entry has `unit_note`; the nine `column` values are unique.
    - Test 6 `test_registry_physical_ranges_and_filters_are_pinned`: `physical_range` is `[1.0, 20.0]` for `dielectric_const_dc`, `[100.0, 900.0]` for `tg`, `[0.0, 2.0]` for `thermal_conductivity`, `[0.0, 200.0]` for `rg`, `[0.0, 2000.0]` for `r2`, `[0.0, 1.0]` for `ffv`, `[0.0, 2000.0]` for `sp_ced`, `[0.5, 2.5]` for `density`, `[1.0, 3.0]` for `refractive_index`; every range has `min < max`; `thermal_conductivity["filters"] == {"check_tc": True}`; `tg["filters"] == {"tg_rmse_ladder": [0.05, 0.1, 0.2, 0.5, 1.0], "tg_rmse_unit": "(g/cm^3)^2"}`; no other entry has `filters`; no entry anywhere has a key containing `max` or `cut` inside `filters` (R-2: no cut applied).
    - Test 7 `test_registry_numbers_are_numbers_not_strings`: walking every scalar of the instance, no `str` value matches `^-?[0-9.]+e-?[0-9]+$` and every `physical_range` element and ladder element is `int` or `float` (F-47).
    - Test 8 `test_registry_condition_matches_the_pinned_run_conditions`: every entry's `condition` equals `{"temperature_K": 300, "pressure_atm": 1, "forcefield": "GAFF2_mod", "charges": "RESP", "state": "isotropic_amorphous"}` (F-30).
    - Test 9 `test_registry_roles_follow_charter_table`: roles are `objective` (thermal_conductivity), `constraint` (dielectric_const_dc, tg), `intermediate` (rg, ffv, sp_ced, density), `validator` (r2, refractive_index).
    - Test 10 `test_registry_header_names_snapshot_and_revision`: `instance["schema_version"] == 1`, `instance["registry_version"] == "v1"`, `instance["snapshot"] == "polyomics:general_polymers@041e5834"`, `instance["revision"] == "041e5834ea1a48682fae12dc39ccd723bcd4f771"`.
    - Test 11 `test_schema_rejects_an_unknown_filter_key_and_a_bad_range`: a copy with `filters: {something_else: 1}` on `tg` raises; a copy with `physical_range: [1.0]` raises; a copy with `unit_status: "guessed"` raises; a copy with an extra top-level key raises.
  </behavior>
  <files>protocol/schemas/property-registry.json, protocol/property-registry_v1.yaml, tests/test_data_registry.py</files>
  <action>
    STEP 1 — RED. Write `tests/test_data_registry.py` with module constants `ROOT`, `REGISTRY =
    ROOT / "protocol" / "property-registry_v1.yaml"`, `SCHEMA = ROOT / "protocol" / "schemas" /
    "property-registry.json"`, `PROVENANCE = ROOT / "protocol" / "property-registry_v1.provenance.yaml"`,
    `NINE_KEYS` in charter order, helpers `_load_yaml(path)` (`yaml.safe_load`, never `yaml.load`),
    `_load_schema()`, and the eleven behaviours above (Task 2 appends the provenance tests to this
    file). Build the barred name inside Test 4's body from two fragments joined at runtime
    (`"static_" + "dielectric_const"`) so no line other than the `def test_…` line contains the
    literal — the same fragment discipline Phase 1 used for the dotenv name; the test's *name* keeps
    the literal on purpose so INVARIANTS.md and the evidence can cite it. Run `pixi run --manifest-path env/pixi.toml python
    -m pytest tests/test_data_registry.py -q`: RED with `FileNotFoundError` on the schema. Commit as
    `test(02-02): add failing property-registry schema and instance tests (D-05, A-7, R-4)`.

    STEP 2 — the schema `protocol/schemas/property-registry.json` (draft 2020-12). Root: `$schema`,
    `$id` (`https://github.com/kn1218/llm4pol/protocol/schemas/property-registry.json`), `title`
    (`LLM4POL property registry — charter §6, §7 (M1 freeze), A-7`), `description` (one sentence:
    keys map to PolyOmics columns; the registry serves `dielectric_const_dc` only; ranges are the
    validator's physical-range filters, not D-16 thresholds), `type: object`, `additionalProperties:
    false`, `required: [schema_version, registry_version, snapshot, revision, properties]`,
    `properties`: `schema_version` `{const: 1}`, `registry_version` `{const: "v1"}`, `snapshot`
    `{type: string, pattern: "^polyomics:general_polymers@[0-9a-f]{8}$"}`, `revision` `{type: string,
    pattern: "^[0-9a-f]{40}$"}`, `properties` `{type: object, propertyNames: {enum: [the nine]},
    required: [the nine], minProperties: 9, maxProperties: 9, additionalProperties: {$ref:
    "#/$defs/property"}}`. `$defs.condition`: `type: object`, `additionalProperties: false`,
    `required` all five, `temperature_K` `{const: 300}`, `pressure_atm` `{const: 1}`, `forcefield`
    `{const: "GAFF2_mod"}`, `charges` `{const: "RESP"}`, `state` `{const: "isotropic_amorphous"}`.
    `$defs.property`: `type: object`, `additionalProperties: false`, `required: [column, unit,
    unit_status, condition, role, physical_range]`, `column` `{type: string, pattern:
    "^[A-Za-z_][A-Za-z0-9_]*$"}`, `unit` `{type: string, minLength: 1}`, `unit_status` `{enum:
    [verified, unverified]}`, `unit_note` `{type: string, minLength: 1}`, `condition` `{$ref:
    "#/$defs/condition"}`, `role` `{enum: [objective, constraint, intermediate, validator]}`,
    `physical_range` `{type: array, prefixItems: [{type: number}, {type: number}], items: false,
    minItems: 2, maxItems: 2}`, `filters` `{type: object, additionalProperties: false, minProperties:
    1, properties: {check_tc: {type: boolean}, tg_rmse_ladder: {type: array, items: {type: number,
    exclusiveMinimum: 0}, minItems: 1, uniqueItems: true}, tg_rmse_unit: {const: "(g/cm^3)^2"}}}`,
    and `if: {properties: {unit_status: {const: unverified}}, required: [unit_status]}` /
    `then: {required: [unit_note]}`. Two-space indentation, keys in the order listed, trailing
    newline.

    STEP 3 — the instance `protocol/property-registry_v1.yaml`. Header comment block in the precedent
    style: what the file is (Level: protocol; the M1 freeze of charter §7), that a changed registry
    is a NEW file `property-registry_v2.yaml` never an edit in place once a reported result cites v1
    (A-5), the float-formatting rule (every float carries a decimal point; `1.0e-3` never `1e-3`,
    F-47), that `physical_range` is the validator's physical filter and NOT a D-16 threshold
    (ADR-0005), that `filters.check_tc` and `filters.tg_rmse_ladder` are protocol quality filters
    whose counts the validator prints alongside the unfiltered README reproduction and that no
    `tg_rmse` cut is applied in M1 (R-2), and that the registry serves `dielectric_const_dc` only
    (A-7) — describe the excluded column as "the uncorrected static permittivity column" without
    naming it. Then the four header keys (`schema_version: 1`, `registry_version: v1`, `snapshot:
    polyomics:general_polymers@041e5834`, `revision: 041e5834ea1a48682fae12dc39ccd723bcd4f771`) and
    `properties:` with the nine entries in charter order, each a block mapping (no flow style, no
    anchors) with `column`, `unit`, `unit_status`, `condition` (the five pinned values, repeated per
    entry as D-05 asks), `role`, `physical_range` and, where applicable, `filters` / `unit_note`,
    using exactly the values of Test 5, 6, 8, 9. The `r2` `unit_note` reads: `arXiv:2511.11626 Table
    S3 lists angstrom^2; the data are consistent only with nm^2 (r2 / Rg^2 = 5.58 with r2 in nm^2 and
    Rg in angstrom; ideal chain 6). Unresolved until a second source confirms (F-25); r2 is a
    validator variable only.`

    STEP 4 — GREEN. Run the registry tests → 11 PASSED; run the full gate (`ruff` does not scan
    `protocol/`; the pytest step picks up the new file) → `SUMMARY: 6/6 steps passed` (or `7/7` if
    plan 02-04 has already landed its step). Commit the schema and the instance as `feat(02-02):
    freeze property registry v1 — schema with A-7 by construction, units with status, ranges,
    check_tc and the tg_rmse ladder (D-05, R-2, R-4, DATA-03)`.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_registry.py -v</automated>
    <fails_when>exit code is non-zero, or any of `test_registry_schema_is_a_valid_draft_2020_12_schema`, `test_registry_instance_validates_against_its_schema`, `test_registry_declares_exactly_the_nine_keys_in_charter_order`, `test_a7_static_dielectric_const_is_rejected_by_the_registry_schema`, `test_registry_units_and_status_match_research_r4`, `test_registry_physical_ranges_and_filters_are_pinned`, `test_registry_numbers_are_numbers_not_strings`, `test_registry_condition_matches_the_pinned_run_conditions`, `test_registry_roles_follow_charter_table`, `test_registry_header_names_snapshot_and_revision`, `test_schema_rejects_an_unknown_filter_key_and_a_bad_range` is not listed as PASSED</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -c "import json, yaml, jsonschema; s=json.load(open('protocol/schemas/property-registry.json', encoding='utf-8')); i=yaml.safe_load(open('protocol/property-registry_v1.yaml', encoding='utf-8')); jsonschema.Draft202012Validator.check_schema(s); jsonschema.validate(i, s); print('registry ok', len(i['properties']))"</automated>
    <fails_when>exit code is non-zero or the output is not `registry ok 9`</fails_when>
    <automated>grep -v '^\s*#' protocol/property-registry_v1.yaml | grep -c "static_dielectric_const"</automated>
    <fails_when>prints anything other than 0 — the barred column entered the registry instance (A-7)</fails_when>
    <automated>grep -c "static_dielectric_const" protocol/property-registry_v1.yaml</automated>
    <fails_when>prints anything other than 0 — the barred name appears in a comment of the instance, which the registry test forbids (prohibition 1)</fails_when>
    <automated>grep -v "def test_" tests/test_data_registry.py | grep -c "static_dielectric_const"</automated>
    <fails_when>prints anything other than 0 — the barred literal appears in a test body instead of being assembled from fragments (only `def test_` lines may carry it)</fails_when>
    <automated>grep -c "tg_rmse_max" protocol/property-registry_v1.yaml protocol/schemas/property-registry.json</automated>
    <fails_when>either count is not 0 — a `tg_rmse` cut was written into the protocol against R-2</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final `SUMMARY:` line is not `SUMMARY: N/N steps passed` with N equal to the number of names in the `Steps:` line (6 before plan 02-04 adds `schema-inventory`, 7 after), or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --format='%H %s' --max-count=2` lists `feat(02-02): freeze property registry v1 — schema with A-7 by construction, units with status, ranges, check_tc and the tg_rmse ladder (D-05, R-2, R-4, DATA-03)` above `test(02-02): add failing property-registry schema and instance tests (D-05, A-7, R-4)`; `git show --stat --format= <RED_SHA>` lists exactly `tests/test_data_registry.py`; record both shas in the summary.
    - `protocol/schemas/property-registry.json` contains `"propertyNames"` with an `enum` of exactly the nine keys, `"minProperties": 9`, `"maxProperties": 9`, `"additionalProperties": false` at the root and inside `$defs.property`, `$defs.condition` and `filters`, `"prefixItems"` for `physical_range`, and the `if`/`then` rule requiring `unit_note` when `unit_status` is `unverified`.
    - `protocol/property-registry_v1.yaml` has `snapshot: polyomics:general_polymers@041e5834` and `revision: 041e5834ea1a48682fae12dc39ccd723bcd4f771`; the nine entries appear in charter order; `python -c` loading it with `yaml.safe_load` yields floats for every `physical_range` element and ladder element (F-47).
    - The `tg` entry's `filters` has exactly the keys `tg_rmse_ladder` (value `[0.05, 0.1, 0.2, 0.5, 1.0]`) and `tg_rmse_unit` (value `(g/cm^3)^2`); the `thermal_conductivity` entry's `filters` is exactly `{check_tc: true}`; no other entry has `filters` (R-2, F-59, F-65).
    - The `r2` entry has `unit: nm^2`, `unit_status: unverified` and the `unit_note` text above; every other entry has `unit_status: verified` and no `unit_note` (R-4).
    - The test source builds the barred name from fragments everywhere except in test *names* (which carry it by design so INVARIANTS.md, the evidence file and `-v` output can cite them): `grep -v "def test_" tests/test_data_registry.py | grep -c "static_dielectric_const"` prints 0. Plans 02-04 and 02-05 apply the same rule to their A-7 tests (`test_load_registry_rejects_an_instance_with_the_barred_key`, `test_a7_validator_and_loader_never_serve_static_dielectric_const`): the literal appears only on `def test_` lines.
  </acceptance_criteria>
  <done>The registry is protocol, not code: nine keys with column, unit, status, condition, role and physical range are frozen in a YAML instance that a draft 2020-12 schema constrains tightly enough that the barred column cannot be registered, the `tg_rmse` ladder and the `check_tc` filter are recorded without any cut being applied, and eleven tests pin every value against the research facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provenance file — one entry per leaf, classes pinned, r2 unresolved (D-05 triad)</name>
  <read_first>
    - `C:/Users/molsim/Desktop/CALF20_DiscoveryLoop/protocol/partition_v1.provenance.yaml` — the header comment explaining the three classes and the leaf-set rule, the `entries:` mapping keyed by dotted leaf path, the per-class fields (`source` + `quote`/`fact`; `rationale` (+ `adr`); `target_phase` + `pass_condition`)
    - `protocol/property-registry_v1.yaml` (written in Task 1) — the leaves to cover
    - `.planning/phases/02-data-foundation/02-RESEARCH.md` — the F-fact per unit (F-20..F-29), F-30 (conditions), F-59, F-65, F-66 (`[ASSUMED]` A3 threshold — the ladder is recorded, no default cut), F-67 (observed ranges), Assumptions Log A2, A4
  </read_first>
  <behavior>
    - Test 12 `test_provenance_leaf_paths_equal_registry_leaf_paths`: with `_leaf_paths(mapping)` defined as every dotted path whose value is a scalar or a list, except that a mapping under the key `condition` counts as one leaf (its path ends in `.condition`), `set(provenance["entries"])` equals `_leaf_paths(instance)`; the set has 62 members (4 header leaves + 9 × 6 entry leaves + `properties.thermal_conductivity.filters.check_tc` + `properties.tg.filters.tg_rmse_ladder` + `properties.tg.filters.tg_rmse_unit` + `properties.r2.unit_note`).
    - Test 13 `test_provenance_classes_are_valid_and_complete`: every entry has `provenance_class` in `{"paper_or_source", "protocol_decision", "unresolved"}`; `paper_or_source` entries have non-empty `source` and a `fact` matching `^F-[0-9]{2}$`; `protocol_decision` entries have a non-empty `rationale`; `unresolved` entries have `rationale`, `target_phase` and `pass_condition`; the only `unresolved` entry is `properties.r2.unit`; every `unit` leaf other than `properties.r2.unit` is `paper_or_source`; every `physical_range` leaf, every `filters.*` leaf and every `role` leaf is `protocol_decision`; every `condition` leaf is `paper_or_source` with `fact: F-30`.
    - Test 14 `test_provenance_never_calls_a_range_a_decision`: no `rationale` string in the provenance contains the substrings `D-16` followed by `fixed`, or the phrase `threshold =`; every `physical_range` rationale contains `physical-range filter` (ADR-0005: D-16 is not fixed here).
  </behavior>
  <files>protocol/property-registry_v1.provenance.yaml, tests/test_data_registry.py</files>
  <action>
    STEP 1 — RED: append Tests 12–14 and the `_leaf_paths` helper to `tests/test_data_registry.py`;
    run the file → the three new tests FAIL with `FileNotFoundError` on the provenance path; commit
    `test(02-02): add failing provenance leaf-set and class tests (D-05 triad)`.

    STEP 2 — write `protocol/property-registry_v1.provenance.yaml`: a header comment (what the file
    is; one entry per leaf dotted path of `property-registry_v1.yaml`; the leaf rule including the
    `condition`-is-one-leaf convention; the three classes with their required fields; the sentence
    that this file is never an input to any hash or to the loader — correcting a citation must not
    change a value), then `entries:` with all 62 leaves. Classes and content: header leaves
    `schema_version`, `registry_version` → `protocol_decision` (rationale: the triad convention;
    v1 never edited in place, A-5); `snapshot` → `protocol_decision` (rationale: D-01 identifier form,
    equals `llm4pol.data.snapshot.SNAPSHOT_ID`; a test in plan 02-04 asserts equality); `revision` →
    `paper_or_source` (source: `HfApi.dataset_info` live 2026-09-22, `fact: F-01`). Per property:
    `column` → `paper_or_source` (source: PolyOmics `general_polymers` header; the fact id of the
    unit row); `unit` → `paper_or_source` with the F-fact (F-20, F-21, F-22, F-24, F-26, F-27, F-28,
    F-29) and a `source` string naming the RadonPy module and/or Table S3 and the data identity the
    research used, except `properties.r2.unit` → `unresolved` (rationale: paper says Å², data says
    nm²; `target_phase: "M8 or the first phase that makes r2 a design variable"`; `pass_condition: "a
    second source (RadonPy AutoMD export script or the PolyOmics community tab) states the stored unit"`);
    `unit_status` → `paper_or_source` (same fact as the unit; for r2, `fact: F-25` with source
    "conflict between Table S3 and the data identity"); `condition` → `paper_or_source` (source:
    arXiv:2511.11626 Methods and the columns `temp`, `press`, `forcefield`, `charge`; `fact: F-30`);
    `role` → `protocol_decision` (rationale: charter §6 registry table); `physical_range` →
    `protocol_decision` (rationale must contain `physical-range filter`, cite F-67 for the observed
    span, and for `dielectric_const_dc` and `tg` state that these two ranges are exactly the README's
    filter that reproduces 43,561 (F-08) while the others are Assumptions Log A4 ranges);
    `properties.thermal_conductivity.filters.check_tc` → `protocol_decision` (rationale: F-59 — 108 of
    the 109 rows with TC > 1 W/(m·K) are `check_tc == False`; the validator prints the filtered
    count 42,733 beside the README's 43,561; whether the Phase 3 hidden table applies it is Open
    Question 2, an owner decision); `properties.tg.filters.tg_rmse_ladder` → `protocol_decision`
    (rationale: F-23 — `tg_rmse` is the sum of squared density residuals of the two-line fit, unit
    (g/cm³)², not kelvin; the ladder rungs come from F-65 and the validator prints the count at each
    rung; no cut is applied in M1 per R-2; A3 recorded the rung 0.1 as a candidate default, not a
    decision); `properties.tg.filters.tg_rmse_unit` → `paper_or_source` (source: RadonPy
    `sim/preset/tg.py`, `fact: F-23`); `properties.r2.unit_note` → `protocol_decision` (rationale:
    the note exists so the conflict is visible in the instance). Floats with decimal points; no
    anchors; keys in the instance's order.

    STEP 3 — GREEN: run the registry tests → 14 PASSED; full gate green; commit
    `feat(02-02): add property-registry v1 provenance — 62 leaves, r2 unit unresolved (D-05, F-25)`.
  </action>
  <verify>
    <automated>pixi run --manifest-path env/pixi.toml python -m pytest tests/test_data_registry.py -v</automated>
    <fails_when>exit code is non-zero, or fewer than 14 tests are collected, or any of `test_provenance_leaf_paths_equal_registry_leaf_paths`, `test_provenance_classes_are_valid_and_complete`, `test_provenance_never_calls_a_range_a_decision` is not listed as PASSED</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml python -c "import yaml; p=yaml.safe_load(open('protocol/property-registry_v1.provenance.yaml', encoding='utf-8')); e=p['entries']; print(len(e), sorted({v['provenance_class'] for v in e.values()}), [k for k,v in e.items() if v['provenance_class']=='unresolved'])"</automated>
    <fails_when>the output is not `62 ['paper_or_source', 'protocol_decision', 'unresolved'] ['properties.r2.unit']`</fails_when>
    <automated>pixi run --manifest-path env/pixi.toml check</automated>
    <fails_when>exit code is non-zero, or the final `SUMMARY:` line is not `SUMMARY: N/N steps passed` with N equal to the number of names in the `Steps:` line, or any line begins with `FAIL`</fails_when>
  </verify>
  <acceptance_criteria>
    - Two further commits exist: `test(02-02): add failing provenance leaf-set and class tests (D-05 triad)` then `feat(02-02): add property-registry v1 provenance — 62 leaves, r2 unit unresolved (D-05, F-25)`.
    - `protocol/property-registry_v1.provenance.yaml` has exactly 62 entries whose keys equal the instance's leaf paths; the only `unresolved` entry is `properties.r2.unit`; every `paper_or_source` entry carries a `fact` id from F-01, F-20..F-30 or F-23; every `physical_range` rationale contains `physical-range filter`.
    - `tests/test_data_registry.py` collects 14 tests and imports nothing from `llm4pol` (`grep -c "llm4pol" tests/test_data_registry.py` prints 0 — plan 02-04 adds the loader tests that do).
  </acceptance_criteria>
  <done>Every leaf of the registry states where it came from, in one of three classes, with the single unresolved unit (`r2`) named together with what would resolve it; the leaf-path sets of instance and provenance are equal by test, so a value cannot be added or removed without its provenance moving with it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML text → Python objects | Protocol files are parsed by PyYAML; only `safe_load` is acceptable |
| registry instance → loader / validator (plans 02-04, 02-05) | Every range, unit and filter downstream is read from this file; a wrong value here silently changes every count |
| research facts → protocol values | Units and ranges are transcribed from F-20..F-31, F-65, F-67; a transcription error would be published as protocol |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Elevation of Privilege | YAML deserialisation | medium | mitigate | Tests and (in 02-04) the check step and loader use `yaml.safe_load` only; the instance uses no tags, anchors or flow-style objects |
| T-02-09 | Tampering | registry drift or the barred column entering the registry | high | mitigate | `propertyNames` enum + `required` + `minProperties`/`maxProperties` 9 in the schema; `test_a7_static_dielectric_const_is_rejected_by_the_registry_schema`; the verify greps exclude the barred name from the instance text |
| T-02-10 | Tampering | a float written as a string (`1e-3`) silently changing a comparison | medium | mitigate | `test_registry_numbers_are_numbers_not_strings`; header comment states the decimal-point rule (F-47) |
| T-02-11 | Repudiation | a value with no traceable origin | medium | mitigate | provenance leaf-set equality (`test_provenance_leaf_paths_equal_registry_leaf_paths`) and class completeness; `fact` ids point at RESEARCH.md |
| T-02-12 | Tampering | a protocol range presented as the D-16 decision | medium | mitigate | header comment and `test_provenance_never_calls_a_range_a_decision`; ranges are named physical-range filters (ADR-0005) |
| T-02-SC | Tampering | package installs | low | accept | This plan installs nothing; it reads `yaml` and `jsonschema` already in the committed lock |
</threat_model>

## Artifacts this phase produces

**New files (this plan):** `protocol/schemas/property-registry.json` (draft 2020-12; `$defs.condition`,
`$defs.property`; `propertyNames` enum of nine keys); `protocol/property-registry_v1.yaml` (header
keys `schema_version`, `registry_version`, `snapshot`, `revision`; nine entries); 
`protocol/property-registry_v1.provenance.yaml` (`entries:` with 62 leaf paths);
`tests/test_data_registry.py` (14 tests; helper `_leaf_paths`; constants `REGISTRY`, `SCHEMA`,
`PROVENANCE`, `NINE_KEYS`).

**Commits (in order):** `test(02-02): …schema and instance tests…` → `feat(02-02): freeze property
registry v1 …` → `test(02-02): …provenance…` → `feat(02-02): add property-registry v1 provenance …`.

**Consumed later:** plan 02-04 adds `llm4pol.data.registry` (loader), the `[tool.llm4polcheck]`
inventory entry and the `schema-inventory` check step; plan 02-05's ladders read `physical_range` and
`filters` from the loaded registry.

## Source audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| REQ | DATA-03 | registry declares key/column/unit/condition for nine keys; barred column absent | 02-02 T1 | COVERED | loader half in 02-04 |
| REQ | DATA-09 | Tg filtered by `tg_rmse` and 100–900 K; counts recorded | 02-02 T1 (range + ladder recorded) | COVERED | counts printed by 02-05 |
| CONTEXT | D-05 | registry as protocol triad | 02-02 T1, T2 | COVERED | |
| CONTEXT | R-2 | ladder recorded, no cut in M1 | 02-02 T1 | COVERED | `tg_rmse_max` grep = 0 |
| CONTEXT | R-4 | unit statuses | 02-02 T1 | COVERED | r2 unverified with note |
| RESEARCH | F-20..F-31 | units and conditions | 02-02 T1, T2 | COVERED | provenance cites facts |
| RESEARCH | Pattern 7 | registry skeleton and schema constraints | 02-02 T1 | COVERED | |
| RESEARCH | Pitfall 4 | `tg_rmse` not kelvin | 02-02 T1, T2 | COVERED | unit `(g/cm^3)^2` in filters, meaning in provenance |
| CHARTER | §7 M1 freeze | `protocol/schemas/property-registry.json` | 02-02 T1 | COVERED | |
| CHARTER | §4 A-7 | barred column not in registry | 02-02 T1 | COVERED | schema half; validator half in 02-05 |

<verification>
- `pytest tests/test_data_registry.py -v` lists 14 PASSED.
- `jsonschema.validate(instance, schema)` passes and the barred-key injection raises.
- The provenance file has 62 entries equal to the instance's leaf set; only `properties.r2.unit` is
  `unresolved`.
- The gate is green.
</verification>

<success_criteria>
The property registry is frozen as charter §7 requires: a schema at `protocol/schemas/property-registry.json`
that admits exactly nine keys and structurally excludes the barred column (A-7), an instance carrying
verified units with their status (R-4), the physical ranges the validator applies, the `check_tc`
filter and the `tg_rmse` ladder without a cut (R-2), and a provenance file that names the origin of every
leaf. Fourteen tests inside the gate pin all of it.
</success_criteria>

<output>
Create `.planning/phases/02-data-foundation/02-02-SUMMARY.md` when done. Record the four commit shas,
the `registry ok 9` line, the provenance count line, and any schema constraint that had to be relaxed
from the plan (with the reason).
</output>
